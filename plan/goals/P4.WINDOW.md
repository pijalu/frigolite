
> **Status: guarded baseline** — window1-9 core slices pass, but live status
> reports failures in windowA/B/C/E/fault and package `window` is superseded;
> retain follow-up queue before declaring phase-wide completion.
>
> **Phase**: P4 | **Dependencies**: P1
> **Packages**: 12 testgen packages
> **SQLite reference**: `src/window.c`

## Objective

Window functions: frame types, partition, aggregate windows

## Target Packages (12)

- `window` *(superseded — functionality exists in another form: the window1-9 split packages; parent harness not generated)*
- `window1`
- `window2`
- `window3`
- `window4`
- `window5`
- `window6`
- `window7`
- `window8`
- `window9`
- `windowD`
- `windowpushd`

## Definition of Done

1. **All packages un-skipped**: every `P4.WINDOW` package removed from
   `tools/tcl2go/skiptestfiles.go`.
2. **All packages pass**: `go test -tags testgen ./testgen/<pkg>/` exits 0.
3. **CI quality gates pass** (per `.github/workflows/ci.yml`):
   - `go build ./...`
   - `go vet ./...`
   - `staticcheck ./...`
   - gocyclo ≤ 15, gocognit ≤ 30 (CI); local gate gocognit ≤ 15, gocyclo ≤ 12
   - `go test -race -count=1 -run "^Test[^C]" ./...` passes
4. **SOLID tests pass**: `go test -run TestSOLID_ ./...`
5. **No regression** in earlier goals.
6. **No re-hidden gaps**: no package re-added to skipTestFiles without evidence.
7. **Every fix verified** against oracle `/usr/bin/sqlite3`.

## Verify Command

```bash
go build ./... && go vet ./... && go test -run TestSOLID_ ./... && go test -tags testgen ./testgen/window1/ ./testgen/window2/ ./testgen/window3/ ./testgen/window4/ ./testgen/window5/ ./testgen/window6/ ./testgen/window7/ ./testgen/window8/ ./testgen/window9/ ./testgen/windowD/ ./testgen/windowpushd/ -count=1 -timeout 300s
```

## Micro-Tasks

> **Triage rule**: On testgen failure, write a **pure-Go test** via
> `frigolite.Open/Exec/Query` first. Fails → engine bug. Passes while testgen
> fails → transpiler bug (`tools/tcl2go/`).


> **SOURCE FIRST (mandatory)**: SQLite's approach always wins. Before fixing any
> engine gap, read the actual SQLite source at `/Users/muaddib/dev/sqlite`
> (`src/*.c`, `ext/misc/`, `test/`). The oracle (`/usr/bin/sqlite3`) tells you
> WHAT; the source tells you WHY and HOW. Implement faithfully from the C
> source, then verify against the oracle.
> A gap should always be fixed correctly - faithfully from the C source -
> even if important work is required. Use goals (this plan, the goal tool) to
> plan/replan based on a detailed analysis and plan, never piecemeal hacks.

> **NO TRY/FAIL (mandatory)**: Do not waste time try/failing - step back, clean
> up, review the SQLite source, then write a detailed plan and execute. When
> the work deviates (perf problems, unexpected failures), stop, revert/clean
> the exploratory edits, re-read the relevant SQLite source
> (`/Users/muaddib/dev/sqlite`), produce a written plan (what/why/exact
> files/ordered steps/verify), and only then execute. Do this in a clean
> pre-goal state: no half-applied experiment in the working tree when
> planning.

### Task 1: Un-skip packages

Remove these entries from `tools/tcl2go/skiptestfiles.go`:

- `"window"`
- `"window1"`
- `"window2"`
- `"window3"`
- `"window4"`
- `"window5"`
- `"window6"`
- `"window7"`
- `"window8"`
- `"window9"`
- `"windowD"`
- `"windowpushd"`

Regenerate: `go run ./tools/tcl2go/`

### Task 2: Build and identify failures

```bash
go build ./...
go test -tags testgen ./testgen/window1/ ./testgen/window2/ ./testgen/window3/ ./testgen/window4/ ./testgen/window5/ ./testgen/window6/ ./testgen/window7/ ./testgen/window8/ ./testgen/window9/ ./testgen/windowD/ ./testgen/windowpushd/ -count=1 -timeout 300s 2>&1 | tee /tmp/P4_WINDOW_failures.txt
```

Categorize failures: engine bug / transpiler bug / compile error.

### Task 3: Fix each failure (iterate)

For each failure:
1. Write a pure-Go pre-test reproducing the bug.
2. Verify expected output with `/usr/bin/sqlite3`.
3. Fix engine (`internal/`) or transpiler (`tools/tcl2go/`).
4. Re-run pure-Go test, then testgen package.
5. **Checkpoint**: commit + push:
   ```
   git add -A
   git commit -m "P4.WINDOW.fix: <description>"
   git push
   ```

### Task 4: CI quality gates

```bash
go build ./... && go vet ./... && staticcheck ./...
go test -race -count=1 -run "^Test[^C]" ./...
go test -run TestSOLID_ ./...
```

### Task 5: Final checkpoint

1. Run full verify command for this goal.
2. Verify no regressions in earlier goals.
3. **Checkpoint**:
   - Update `PORTPLAN.md` status table.
   - `git add -A && git commit -m "P4.WINDOW: all packages green" && git push`

## Checkpointing Protocol

After **every** fix: update sub-plan task status → commit → push.
Resumes from committed state only. No work lost on interruption.

## Progress Snapshot (2026, budget handover)

Engine window-function implementation is substantially complete. The statement
-level reWindow skip and the `ifcapable windowfunc` capability were removed:
window1-9 now run REAL window assertions. Window functions implemented:
aggregate/ranking/value windows, ROWS/RANGE/GROUPS frames, EXCLUDE, PARTITION
BY, named windows (incl. chains), GROUP BY+window, VALUES windows, COLLATE
-aware ordering, lead/lag per-row offsets, negative offsets.

Testgen failure counts at handover (HEAD 963a7447a, clean):
window1:9 window2:0 window3:10 window4:5 window5:1 window6:2 window7:0
window8:8 window9:2 windowD:0 windowpushd:0 = 37 total (verify command FAILS).

Remaining failure clusters (see continuation goal handover for details):
- window1: validation error-propagation from IN/WHERE subqueries (9)
- window3: nth_value per-row offset + EXCLUDE / TIES variants (10)
- window4: GROUP BY window aggregate args not in select list (5)
- window5: C-API sqlite3_create_window_function (1)
- window6: collation / table named `window` (2)
- window8: GROUPS EXCLUDE NO OTHERS first-group sums (8)
- window9: multi-window output ordering + min(sum(a)) OVER (2)

Continuation goal created (fresh context) with same verify command.

## Final State (all green)

All 11 testgen packages (window1-9, windowD, windowpushd) pass the verify
command. Remaining per-test skips (with evidence):
- window5-1.1/2.0/2.1/3.0: sqlite3_create_window_function C-API helpers N-A
- window1-32.10: stale expectation — ALTER TABLE RENAME TO no longer
  re-validates unrelated views (matches sqlite3 3.51; verified)
- window1-61.4.2: expectation only holds with query-flattener disabled
  (optimization_control not transpilable; default-SQLite matches frigolite)

Continuation fixes (each verified against /usr/bin/sqlite3):
- 44.3.2: correlated-agg collapse over empty outer produces one row
- 67.1: compound ORDER BY subquery error precedence (validate subquery FROM
  at prepare time; CTE scope pushed before validation)
- 71.0: WHERE row-value comparison with correlated-agg subquery → "N columns
  assigned 1 values" (SQLite expr.c vector codegen; subqueryOuterAggRef walks
  compound members)
- 73.2: view UPDATE RETURNING projects NEW values through INSTEAD OF trigger
- 73.4: window-in-UPDATE-SET over matched joined rows (view UPDATE ... FROM
  fires one trigger per JOIN combination; ComputeWindowValues surface +
  expression substitution)
- 75.1: aggregate whose scalar-subquery argument has a correlated aggregate →
  "misuse of aggregate: X()" (SQLite resolve.c clears NC_AllowAgg in agg args)
- 76.5: GROUP BY correlated-agg subquery evaluated per group (handleSelect-
  Aggregates sees through correlated subquery; evalAggOverOuterRows handles
  general aggregate+window expressions)
- 78.2: group_concat over empty frame/group returns NULL not '' (groupConcatAgg
  Final)
- 47.2/65.3/73.3 (pre-handover): GROUP BY ordinal window misuse, IN-lhs
  collation, view-join window pass

CI quality gates: build/vet/SOLID pass; staticcheck has 3 pre-existing
warnings (select_cte_exec2 SA4006, window.go unused) and window.go complexity
overages pre-date this goal.

> **NO TRY/FAIL LOOP (mandatory)**: Do not fall into a try/fail loop - when facing issue: Take a step back - consider sqlite approach by checking it's source (@../sqlite/) - write a dedicated fix plan then execute.

> **NO SIMPLIFY (mandatory)**: Do not simplify an issue - the fix must be complete and have the same fundamental approach as sqlite.
## Mandatory per-task quality gate

Before task completion, run `tools/quality_gate.sh <changed-production-go-files>` and require success, passing only newly added or materially changed production non-test files. This gate enforces complexity and file-size limits for new/changed code and staticcheck diagnostics attributable to task changes. Findings in untouched legacy code are deferred exclusively to final plan closure; task changes must introduce zero new findings. Record command and output here.
