# P5.CALLBACKS broad remediation

## Scope
Restore repository gates after callback regeneration and address baseline findings without changing SQL behavior.

## Ordered plan
1. Inventory staticcheck, complexity, and file-size findings; classify generated `testgen` artifacts versus production code.
2. Remove repeated generated helper source by changing tcl2go generation to reference shared helpers, or establish a documented generated-source gate exclusion if helper extraction is unsafe.
3. Fix production staticcheck findings with focused edits and tests.
4. Run targeted and repository tests; classify unrelated testgen failures and repair only regressions caused by current changes.
5. Run build, vet, staticcheck, complexity, file-size, race, and SOLID gates; update PORTPLAN and evidence.
6. Commit locally; push only if remote publication succeeds.

## Current evidence
- Focused P5 packages pass.
- Historical `go-file-size-check.sh` output reported 1,273 violations because duplicated generated `testgen/*_test.go` fixtures were included.
- Resolution: generated `testgen/` is excluded from the production file-size gate, matching the repository's production-only quality policy; generated fixtures remain covered by the documented testgen baseline.
- Current production file-size check reports no hard violations (soft warnings only).
- `staticcheck ./...` reports no findings; full testgen has unrelated baseline failures.

## Gate blocker and resolution

The callback implementation is green, but the repository-wide strict local complexity gate remains red on legacy production functions (gocognit/gocyclo). This is independent of callback files and cannot be safely solved by changing thresholds or suppressing findings. Correct resolution requires a separate complexity-remediation tranche, with one source-first refactor at a time and focused regression tests. Until that tranche is completed, P5 callback completion should remain blocked rather than treating generated-file policy as sufficient.

`go build ./... && go vet ./... && staticcheck ./... && .agents/skills/golang-check/golang-check.sh && go test -race -count=1 -run '^Test[^C]' ./... && go test -run TestSOLID_ ./...`
## Mandatory per-task quality gate

Before task completion, run `tools/quality_gate.sh <changed-production-go-files>` and require success, passing only newly added or materially changed production non-test files. This gate enforces complexity and file-size limits for new/changed code and staticcheck diagnostics attributable to task changes. Findings in untouched legacy code are deferred exclusively to final plan closure; task changes must introduce zero new findings. Record command and output here.
