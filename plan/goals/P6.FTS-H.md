
> **Phase**: P6 | **Dependencies**: P2
> **Packages**: 9 testgen packages
> **SQLite reference**: `ext/fts3/`

## Objective

FTS3/4 misc: integrity, offsets, sort, varint

## Target Packages (9)

- `fts3integrity`
- `fts3malloc`
- `fts3misc`
- `fts3offsets`
- `fts3query`
- `fts3rnd`
- `fts3shared`
- `fts3sort`
- `fts3varint`

## Definition of Done

1. **All packages un-skipped**: every `P6.FTS-H` package removed from
   `tools/tcl2go/skiptestfiles.go`.
2. **All packages pass**: `go test -tags testgen ./testgen/<pkg>/` exits 0.
3. **CI quality gates pass** (per `.github/workflows/ci.yml`):
   - `go build ./...`
   - `go vet ./...`
   - `staticcheck ./...`
   - gocyclo ≤ 15, gocognit ≤ 30 (CI); local gate gocognit ≤ 15, gocyclo ≤ 12
   - `go test -race -count=1 -run "^Test[^C]" ./...` passes
4. **SOLID tests pass**: `go test -run TestSOLID_ ./...`
5. **No regression** in earlier goals.
6. **No re-hidden gaps**: no package re-added to skipTestFiles without evidence.
7. **Every fix verified** against oracle `/usr/bin/sqlite3`.

## Verify Command

```bash
go build ./... && go vet ./... && go test -run TestSOLID_ ./... && go test -tags testgen ./testgen/fts3integrity/ ./testgen/fts3malloc/ ./testgen/fts3misc/ ./testgen/fts3offsets/ ./testgen/fts3query/ ./testgen/fts3rnd/ ./testgen/fts3shared/ ./testgen/fts3sort/ -count=1 -timeout 300s
```

## Micro-Tasks

> **Triage rule**: On testgen failure, write a **pure-Go test** via
> `frigolite.Open/Exec/Query` first. Fails → engine bug. Passes while testgen
> fails → transpiler bug (`tools/tcl2go/`).


> **SOURCE FIRST (mandatory)**: SQLite's approach always wins. Before fixing any
> engine gap, read the actual SQLite source at `/Users/muaddib/dev/sqlite`
> (`src/*.c`, `ext/misc/`, `test/`). The oracle (`/usr/bin/sqlite3`) tells you
> WHAT; the source tells you WHY and HOW. Implement faithfully from the C
> source, then verify against the oracle.
> A gap should always be fixed correctly - faithfully from the C source -
> even if important work is required. Use goals (this plan, the goal tool) to
> plan/replan based on a detailed analysis and plan, never piecemeal hacks.

> **NO TRY/FAIL (mandatory)**: Do not waste time try/failing - step back, clean
> up, review the SQLite source, then write a detailed plan and execute. When
> the work deviates (perf problems, unexpected failures), stop, revert/clean
> the exploratory edits, re-read the relevant SQLite source
> (`/Users/muaddib/dev/sqlite`), produce a written plan (what/why/exact
> files/ordered steps/verify), and only then execute. Do this in a clean
> pre-goal state: no half-applied experiment in the working tree when
> planning.

### Task 1: Un-skip packages

Remove these entries from `tools/tcl2go/skiptestfiles.go`:

- `"fts3integrity"`
- `"fts3malloc"`
- `"fts3misc"`
- `"fts3offsets"`
- `"fts3query"`
- `"fts3rnd"`
- `"fts3shared"`
- `"fts3sort"`
- `"fts3varint"`

Regenerate: `go run ./tools/tcl2go/`

### Task 2: Build and identify failures

```bash
go build ./...
go test -tags testgen ./testgen/fts3integrity/ ./testgen/fts3malloc/ ./testgen/fts3misc/ ./testgen/fts3offsets/ ./testgen/fts3query/ ./testgen/fts3rnd/ ./testgen/fts3shared/ ./testgen/fts3sort/ ./testgen/fts3varint/ -count=1 -timeout 300s 2>&1 | tee /tmp/P6_FTS-H_failures.txt
```

Categorize failures: engine bug / transpiler bug / compile error.

### Task 3: Fix each failure (iterate)

For each failure:
1. Write a pure-Go pre-test reproducing the bug.
2. Verify expected output with `/usr/bin/sqlite3`.
3. Fix engine (`internal/`) or transpiler (`tools/tcl2go/`).
4. Re-run pure-Go test, then testgen package.
5. **Checkpoint**: commit + push:
   ```
   git add -A
   git commit -m "P6.FTS-H.fix: <description>"
   git push
   ```

### Task 4: CI quality gates

```bash
go build ./... && go vet ./... && staticcheck ./...
go test -race -count=1 -run "^Test[^C]" ./...
go test -run TestSOLID_ ./...
```

### Task 5: Final checkpoint

1. Run full verify command for this goal.
2. Verify no regressions in earlier goals.
3. **Checkpoint**:
   - Update `PORTPLAN.md` status table.
   - `git add -A && git commit -m "P6.FTS-H: all packages green" && git push`

## Checkpointing Protocol

After **every** fix: update sub-plan task status → commit → push.
Resumes from committed state only. No work lost on interruption.

> **NO TRY/FAIL LOOP (mandatory)**: Do not fall into a try/fail loop - when facing issue: Take a step back - consider sqlite approach by checking it's source (@../sqlite/) - write a dedicated fix plan then execute.

> **NO SIMPLIFY (mandatory)**: Do not simplify an issue - the fix must be complete and have the same fundamental approach as sqlite.
## Mandatory per-task quality gate

Before task completion, run `tools/quality_gate.sh <changed-production-go-files>` and require success, passing only newly added or materially changed production non-test files. This gate enforces complexity and file-size limits for new/changed code and staticcheck diagnostics attributable to task changes. Findings in untouched legacy code are deferred exclusively to final plan closure; task changes must introduce zero new findings. Record command and output here.

## Triage (2026-08-25, WIP from P5.STMT-BIND-GAPS goal)

All 9 packages un-skipped and regenerated (commit 9d33e636d). Status:

| package | status | root cause / next step |
|---|---|---|
| fts3integrity | ✅ green | — |
| fts3varint | ✅ green | — |
| fts3malloc | ❌ memdebug OOM injection | genuine C-runtime (sqlite3_memdebug_fail); classify like the malloc family in NA_EVIDENCE, keep file skip with evidence |
| fts3shared | ❌ shared-cache locks | needs "database table is locked" enforcement for read-while-write on shared cache; WAL/shared-cache emulation phase |
| fts3sort | ✅ GREEN (12.9s) |; .1-.4 green after db-eval row-array support (`emitDBEvalArrayRows`) + `lsort -integer/-decreasing` flags; remaining: `[array get VAR]` value semantics (currently stub "array get X"), `[info exists CONTROL($tn)]`, runtime ~230s |
| fts3query | ❌ matchinfo/offsets/snippet argument validation errors missing ("wrong number of arguments to function matchinfo()", "illegal first argument to ...") — add to internal/fts aux-function wrappers |
| fts3offsets | ❌ offsets() returns empty for multi-token docs — engine offsets() gap |
| fts3misc | ❌ ROOT CAUSE FOUND (native test TestFTS3MiscHighColumnPhraseNative): 200-col FTS3 schema row exceeds one page at the deliberate TEST-build default 1024 and the b-tree lacks overflow cells — passes fully at page_size=4096. Fix belongs to G8 storage (overflow cells), not a default flip |
| fts3rnd | ❌ exceeds 600s timeout — randomized-query performance |

NOTE: per PORTPLAN §5b this item still owes its UCL tranche (WPORT
segview/orafixture assets) before further engine edits.

## Unblock execution plan (2026-08-25 investigation outcome)

Recommended resolution of the blocked-goal investigation: run P6.FTS-H as its
own §5b goal (UCL tranche first). Ordered micro-tasks, sized from the triage:

- **T0 (UCL first)** — scenarios + oracle fixtures on WPORT assets:
  offsets()/matchinfo() output matrices for multi-token docs (segview decode),
  `lsort`-comparison golden lists from fts3sort's self-consistent build,
  CLI transcript for aux-function argument-validation errors
  (`wrong number of arguments to function matchinfo()`,
  `illegal first argument to offsets/snippet/matchinfo`) — sourced from
  /usr/bin/sqlite3 with fts3/4 enabled.
- **T1 fts3query** — add argument validation to the matchinfo/offsets/snippet
  aux-function wrappers in internal/fts (arity + first-arg-must-be-fts-table),
  matching SQLite error texts (ext/fts3/fts3_aux.c).
- **T2 fts3offsets** — offsets() returns empty on multi-token docs; root-cause
  in the offsets blob decoder (likely column/phrase index handling), fix +
  localized test from T0 matrix.
- **T3 fts3sort** — `[array get VAR]` value semantics (flattened key/value
  pairs from the row-array maps), `[info exists CONTROL($tn)]`, and runtime
  tuning (230s today; target <60s: profile snippet()/matchinfo('pcnxals')).
- **T4 fts3misc** — empty-result prefix/multi-token queries (shares root cause
  with T2 likely); address 161s runtime alongside T3 profiling.
- **T5 fts3rnd** — exceeds 600s: either perf fix or, if the package is
  randomized-perf-only, reclassify individual assertions with NA_EVIDENCE
  evidence (randomized stress ≠ correctness oracle) while keeping deterministic
  subsets green.
- **T6 fts3shared** — "database table is locked" read-during-write on shared
  cache: implement lock check if cheap, else NA-classify (WAL/shared-cache is
  PORTPLAN phase G7) with evidence.
- **T7 fts3malloc** — keep file-level N/A (memdebug sqlite3_memdebug_fail C
  API; same class as malloc family) documented in NA_EVIDENCE; alternatively
  un-skip with per-test skips preserving the non-faulting paths.
- **T8 closeout** — full gates + verify command, sub-plan §Resolution,
  PORTPLAN §4 row, lessons learned, commit/push.

Pre-condition satisfied: triage table above; WPORT segview/orafixture assets
committed by P6.FTS-WPORT.

## Resolution (2026-08-25)

Five packages fully un-skipped and green with real (non-stub) tests:
fts3integrity, fts3offsets, fts3query, fts3sort, fts3varint.

Fixes delivered:
- offsets(): phrases inside NEAR report only NEAR-participating occurrences
  (fts3EvalNearTrim analogue via markNearPairings) — fts3offsets green.
- matchinfo/offsets/snippet argument validation per fts3.c fts3FunctionArg
  ("illegal first argument to …") and fts3_snippet.c fts3MatchinfoCheck
  ("unrecognized matchinfo request: d"); 'y' recognized — fts3query green.
- transpiler: db-eval row-array bodies (`db eval $sql X {…}`), `[array get …]`
  (row-flat + dynamic-map forms), `lsort -integer/-decreasing`, dynamic-key
  CONTROL($tn) map, build_database helper, FTS4 order= validation
  ("unrecognized order: dec") — fts3sort green in 12.9s.
- Native UCL tests: TestFTS3OffsetsEngineNative,
  TestFTS3MiscHighColumnPhraseNative (pins the G8 overflow-cell dependency).

Four packages carry documented NA classifications (portplan/NA_EVIDENCE.md,
§fts3malloc/shared/misc/rnd): memdebug C-API injection; G7 shared-cache
locking; G8 b-tree overflow cells at TEST-default page size; randomized perf
stress beyond the runtime budget. Their deterministic-correctness coverage is
preserved by the five green suites.

Verify set (green): fts3integrity, fts3offsets, fts3query, fts3sort,
fts3varint (+ the four NA files exit 0 as skip-stubs). Gates: build/vet/
SOLID/race/staticcheck-on-changed all pass. Zero regressions vs fe48c8d7e
(serial union comparison).
