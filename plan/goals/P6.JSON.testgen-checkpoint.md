# P6.JSON testgen-completion checkpoint (session: verify_command/testgen slice)

## Objective
Complete plan/goals/P6.JSON.md DoD for the testgen variant:
verify_command green (`go build ./... && go vet ./... && go test -run TestSOLID_ ./... &&
go test -tags testgen ./testgen/json101..json108 -count=1 -timeout 300s`), all target
packages un-skipped and passing under `-tags testgen`.

## Status (~90%)
Harness suite (TestSQLiteSuite, FRIGOLITE_TEST=json) was already fully green from prior
sessions. This session targets the GENERATED tests (testgen/*/), which exercise extra
cases the harness does not.

### Passing now
testgen: json103 json104 json105 json106 json107 json108 json109 json501 json502 jsonb01
Native scratch tests green: TestScratchTVFPushdown, TestScratchJSONNullArgs.

### Failing (last measured)
- testgen json101: 4 failures (see Next Steps 1–3)
- testgen json102: 1 failure around json102-101x (`ORDER BY +big.rowid`, correlated
  json_tree/json_each UNION query) — not yet diagnosed.

### Fixes made this session (UNCOMMITTED)
Engine (internal/):
- execquery/select_cte_exec.go: valueLiteralExpr gains `case function.JSONText` so
  window aggregates (json_group_array/object OVER ...) stop collapsing to NULL.
- function/json.go: reject `#+N` path tail form (bad JSON path, sqlite parity);
  NULL-path rules: json_remove/json_extract → SQL NULL; json_set/json_replace/
  json_insert skip the pair (oracle-verified via quote()); fnJSON_ERROR_POSITION(NULL)
  → NULL (was 0); STRDUP registered (fnSTRDUP, ext/misc strdup parity).
- exec/pragma_table.go: materializeCorrelatedVTabFunc now takes the join's WHERE expr;
  splitAndConjuncts + outerConjunctsPass gate per-left-row TVF materialization
  (sqlite WHERE pushdown parity; fixes json102-1011-style "malformed JSON" when
  json_valid(user.phone) should filter rows before json_each sees them).
- exec/select_context.go + execquery/context.go + select_join_plan.go: signature
  threading for the above (MaterializeCorrelatedVTabFunc(ref, leftRows, where)).
- vtab/json_each.go: jeWalk.basePath + pathLength() — path column for top-level rows
  under a deep root argument reports parent path ($."​\x17" root → path "$");
  appendPathName escapes labels via function.JSONEscapeString; parsePathKey decodes
  \uXXXX/\xHH escapes in quoted keys (arrow-default branch only).

Transpiler/tools:
- tools/tcl2go/helpers_template_part1.go: tclListFlatten unwraps ONE quoting level when
  the single structured element is itself a complete braced unit ({{doc}} → doc),
  fixing jsonb01/json501 foreach-variable expectations; added tclIsBracedUnit.
- tools/tcl2go/cmdexpr.go: cmdExprList re-parses words (tcl.ParseCommands) so BRACED
  list elements are literals — no $var/[cmd] substitution inside ([list {{"a$b":1}}]
  keeps $b literal, json501 1.5); all-literal lists merge into one %q literal
  (strconv.Unquote round-trip, text0 field).
- Regenerated testgen json101..json109, json501, json502, jsonb01 via
  `go run ./tools/tcl2go/ <names>` (positional args supported).

### Commits so far (prior sessions)
79a51c260 / a9d5e3845 / edd1aeae33 (P6.JSON main work + lessons).

## Next Steps (in order)
1. fnJSON_TYPE(X, NULL) must return NULL (currently raises bad JSON path). Oracle:
   typeof(json_type('{a:5,b:7}',NULL)) = NULL. Same class: audit other path-taking
   functions for the NULL-path rule (json_array_length? json_patch_valid?).
2. testgen json101 line ~2495: mismatch want [0] — identify SQL; likely another
   NULL-semantics or error-vs-value case; check oracle with quote().
3. testgen json101 line ~100 and ~855 are TRANSPILER expectation artifacts:
   (a) want contains `\\"` (over-escaped backslash-quote) vs got `\"`;
   (b) want `"null"` (quoted) vs got `null` — same TCL {"null"} quoting issue the
   harness cleanExpected fixed; mirror that logic in tools/tcl2go expectation
   normalization (expected.go normalizeExpectedWord / substUnescapeText).
   Then regenerate json101 (+ any affected package).
4. Diagnose testgen json102 remaining failure (correlated json_tree UNION +
   ORDER BY +big.rowid). Suspect ordering or pushdown interplay.
5. Decide fate of frigolite_scratch_native_test.go: promote to properly-named
   permanent tests (e.g. frigolite_json_regression_test.go) before commit.
6. Full gates: go build ./... && go vet ./... ; gofmt -l (EOF blank-line noise in
   tools/tcl2go/dosql.go etc is cosmetic but pre-commit hook may enforce); SOLID;
   the goal's verify_command; plus FRIGOLITE_TEST=json harness re-run for safety.
7. Commit (engine+tools+testgen regen together), update .agents/lessons_learned.md
   (TCL brace-quoting semantics, NULL-path rules, pushdown parity).

## Environment notes
- edit tool replace_lines is broken in this env ("missing_path" despite valid path);
  use replace with exact anchors and re-read after fuzzy matches — several edits
  applied with mangled indentation/duplication that needed manual repair.
- Homebrew sqlite3 CLI renders SQL NULL as "null"; use quote() for definitive NULL
  checks against the oracle.
- Pre-existing unrelated failures (do NOT chase): internal/parse WINDOW grammar
  corpus, tools/status.

## STATUS: COMPLETE (this session)

All next steps done:
- 2495 fixed: `pragma_compile_options` TVF case added to execquery pragmaTableFuncs + materializePragmaTableWithRowImpl; `db exists {SQL}` if-conditions now transpiled to runtime dbExists helper (was bare-word fallback → always true).
- :100/:855 fixed: normalizeExpectedWord applies TCL bare-word escape processing (\\→\, \"→\") and quoted-element unquoting for single-word expectations, mirroring harness cleanExpected.
- Corrupt-JSONB semantics ported from sqlite 3.51.0: jsonbPayloadSize containment check (i+sz+n > len(b) → malformed), strict appendText (error propagation), jsonArgText propagates, lookupStep/vtab walkers stall-guarded. Regression tests in frigolite_json_regression_test.go (TestJSONBCorruptBlob*).
- tcl2go compile regressions fixed: arrayMapVars gating in bodyEndsWithSetVar + reInfoExistsDyn (undeclared *Map[key] refs); debug prints removed; mutex1/with1/select1/notify2/thread003/thread004 regenerated.
- select1 remaining 5 assertion failures = pre-existing engine validation gaps exposed by NEWER corpus (alias masking `SELECT t3.* FROM t3 AS x`, misuse-of-aliased-aggregate, ORDER BY range errors). HEAD select1 is compile-broken — not a regression of this work. Out of P6 scope; tracked as follow-up.
- Gates green: build, vet, SOLID, gofmt, harness FRIGOLITE_TEST=json, root tests, verify_command, all 12 JSON testgen pkgs.
