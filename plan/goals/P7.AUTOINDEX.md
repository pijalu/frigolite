
> **Phase**: P7 | **Dependencies**: P5
> **Packages**: 5 testgen packages
> **SQLite reference**: `src/where.c`

## Objective

Automatic index creation

## Target Packages (5)

- `autoindex1`
- `autoindex2`
- `autoindex3`
- `autoindex4`
- `autoindex5`

## Definition of Done

1. **All packages un-skipped**: every `P7.AUTOINDEX` package removed from
   `tools/tcl2go/skiptestfiles.go`.
2. **All packages pass**: `go test -tags testgen ./testgen/<pkg>/` exits 0.
3. **CI quality gates pass** (per `.github/workflows/ci.yml`):
   - `go build ./...`
   - `go vet ./...`
   - `staticcheck ./...`
   - gocyclo ≤ 15, gocognit ≤ 30 (CI); local gate gocognit ≤ 15, gocyclo ≤ 12
   - `go test -race -count=1 -run "^Test[^C]" ./...` passes
4. **SOLID tests pass**: `go test -run TestSOLID_ ./...`
5. **No regression** in earlier goals.
6. **No re-hidden gaps**: no package re-added to skipTestFiles without evidence.
7. **Every fix verified** against oracle `/usr/bin/sqlite3`.

## Verify Command

```bash
go build ./... && go vet ./... && go test -run TestSOLID_ ./... && go test -tags testgen ./testgen/autoindex1/ ./testgen/autoindex2/ ./testgen/autoindex3/ ./testgen/autoindex4/ ./testgen/autoindex5/ -count=1 -timeout 300s
```

## Micro-Tasks

> **Triage rule**: On testgen failure, write a **pure-Go test** via
> `frigolite.Open/Exec/Query` first. Fails → engine bug. Passes while testgen
> fails → transpiler bug (`tools/tcl2go/`).


> **SOURCE FIRST (mandatory)**: SQLite's approach always wins. Before fixing any
> engine gap, read the actual SQLite source at `/Users/muaddib/dev/sqlite`
> (`src/*.c`, `ext/misc/`, `test/`). The oracle (`/usr/bin/sqlite3`) tells you
> WHAT; the source tells you WHY and HOW. Implement faithfully from the C
> source, then verify against the oracle.
> A gap should always be fixed correctly - faithfully from the C source -
> even if important work is required. Use goals (this plan, the goal tool) to
> plan/replan based on a detailed analysis and plan, never piecemeal hacks.

> **NO TRY/FAIL (mandatory)**: Do not waste time try/failing - step back, clean
> up, review the SQLite source, then write a detailed plan and execute. When
> the work deviates (perf problems, unexpected failures), stop, revert/clean
> the exploratory edits, re-read the relevant SQLite source
> (`/Users/muaddib/dev/sqlite`), produce a written plan (what/why/exact
> files/ordered steps/verify), and only then execute. Do this in a clean
> pre-goal state: no half-applied experiment in the working tree when
> planning.

### Task 1: Un-skip packages

Remove these entries from `tools/tcl2go/skiptestfiles.go`:

- `"autoindex1"`
- `"autoindex2"`
- `"autoindex3"`
- `"autoindex4"`
- `"autoindex5"`

Regenerate: `go run ./tools/tcl2go/`

### Task 2: Build and identify failures

```bash
go build ./...
go test -tags testgen ./testgen/autoindex1/ ./testgen/autoindex2/ ./testgen/autoindex3/ ./testgen/autoindex4/ ./testgen/autoindex5/ -count=1 -timeout 300s 2>&1 | tee /tmp/P7_AUTOINDEX_failures.txt
```

Categorize failures: engine bug / transpiler bug / compile error.

### Task 3: Fix each failure (iterate)

For each failure:
1. Write a pure-Go pre-test reproducing the bug.
2. Verify expected output with `/usr/bin/sqlite3`.
3. Fix engine (`internal/`) or transpiler (`tools/tcl2go/`).
4. Re-run pure-Go test, then testgen package.
5. **Checkpoint**: commit + push:
   ```
   git add -A
   git commit -m "P7.AUTOINDEX.fix: <description>"
   git push
   ```

### Task 4: CI quality gates

```bash
go build ./... && go vet ./... && staticcheck ./...
go test -race -count=1 -run "^Test[^C]" ./...
go test -run TestSOLID_ ./...
```

### Task 5: Final checkpoint

1. Run full verify command for this goal.
2. Verify no regressions in earlier goals.
3. **Checkpoint**:
   - Update `PORTPLAN.md` status table.
   - `git add -A && git commit -m "P7.AUTOINDEX: all packages green" && git push`

## Checkpointing Protocol

After **every** fix: update sub-plan task status → commit → push.
Resumes from committed state only. No work lost on interruption.

> **NO TRY/FAIL LOOP (mandatory)**: Do not fall into a try/fail loop - when facing issue: Take a step back - consider sqlite approach by checking it's source (@../sqlite/) - write a dedicated fix plan then execute.

> **NO SIMPLIFY (mandatory)**: Do not simplify an issue - the fix must be complete and have the same fundamental approach as sqlite.
## Mandatory per-task quality gate

Before task completion, run `tools/quality_gate.sh <changed-production-go-files>` and require success, passing only newly added or materially changed production non-test files. This gate enforces complexity and file-size limits for new/changed code and staticcheck diagnostics attributable to task changes. Findings in untouched legacy code are deferred exclusively to final plan closure; task changes must introduce zero new findings. Record command and output here.

## Resolution (2026-08-27)

- Task 1: un-skipped autoindex1..autoindex5 in `tools/tcl2go/skiptestfiles.go`;
  regenerated (`go run ./tools/tcl2go/`: 1219 packages).
- Task 2 triage: autoindex2..5 green out of the box; autoindex1 had exactly
  one failure: `autoindex1-113` (C-runtime `sqlite3_log` callback seam via
  `test_sqlite3_log`, asserting `SQLITE_WARNING_AUTOINDEX` text). Classified
  per-assertion N/A in `skiptests2.go` with evidence in
  `portplan/NA_EVIDENCE.md` (same class as P5.HOOKS C-runtime callbacks).
  Result-side behavior verified vs oracle `/usr/bin/sqlite3`: join output of
  autoindex1-110 identical; oracle EQP shows `SEARCH t2 USING AUTOMATIC
  COVERING INDEX`, engine's in-memory equi-join hash index
  (`select_join_plan.go buildJoinAutoIndex`) is the functional equivalent —
  plan-text EQP assertions already classified N/A (autoindex1-299/800/801/901).
- Task 4 gates: `go build ./... && go vet ./... && go test -run TestSOLID_
  ./...` all exit 0; `tools/quality_gate.sh` on changed files: hard max OK,
  soft-target warn only (pre-existing).
- Task 5: all 5 packages `go test -tags testgen` exit 0 (durations ≤1s each).

## Strict-DoD verification (2026-08-28 continuation)

- `staticcheck ./...`: no findings attributable to this goal; remaining hits are
pre-existing legacy (FTS/rtree shadow-table unused funcs, `execquery` unused
`materializeVTabJoinRows`) untouched by this goal's change (N/A-classify only).
- `go test -race -count=1 -run "^Test[^C]" ./...` (CI gate, DoD #3): **green**
— root `frigolite` pkg 48s ok; all `internal/*` pkgs ok.
- `go test -race -tags testgen ./testgen/autoindex1..5/`: all ok.
- `NA only w/ evidence`: every autoindex N/A skip (autoindex1-113/299/800/801/
901, autoindex-1211, autoindex3-110/120/130/140, autoindex4-1.0) now has an
oracle-verified entry in `portplan/NA_EVIDENCE.md` §P7.AUTOINDEX — each EQP
assertion confirmed against `/usr/bin/sqlite3` 3.51.0 (`USING AUTOMATIC
COVERING INDEX` / `SEARCH …` text frigolite's planner does not emit; the
`/AUTO/` regex matches oracle output), and the ORDER BY tie-order N/A
(autoindex4-1.0) is the non-stable sorter divergence.
- `UCL satisfied`: documented in `portplan/NA_EVIDENCE.md` §P7.AUTOINDEX — UCL
status. No engine edit on the automatic-index seam (feature already ported;
packages green out of the box); e2e testgen coverage exercises the result
seam; planner-EQP prose divergences are oracle-verified N/A.
- `oracle-verified`: all N/A classifications and active-result parity confirmed
via `/usr/bin/sqlite3` 3.51.0.
- `no regression`: verify command + full race gate green; no engine source
changed (only `skiptests2.go` N/A classification + NA_EVIDENCE docs).
- `plan+todos updated`: PORTPLAN.md P7.AUTOINDEX row marked ✅ complete; this
resolution appended.
