
> **Phase**: P7 | **Dependencies**: P5
> **Packages**: 24 testgen packages
> **SQLite reference**: `src/analyze.c, src/where*.c`

## Objective

ANALYZE, stat tables, BestIndex, query optimization

## Target Packages (24)

- `analyze`
- `analyze3`
- `analyze4`
- `analyze5`
- `analyze6`
- `analyze7`
- `analyze8`
- `analyze9`
- `analyzeE`
- `autoanalyze1`
- `bestindex1`
- `bestindex2`
- `bestindex3`
- `bestindex4`
- `bestindex5`
- `bestindex6`
- `bestindex7`
- `bestindex8`
- `bestindex9`
- `bestindexB`
- `bestindexC`
- `bestindexE`
- `bestindexF`
- `bestindexG`

## Definition of Done

1. **All packages un-skipped**: every `P7.PLANNER` package removed from
   `tools/tcl2go/skiptestfiles.go`.
2. **All packages pass**: `go test -tags testgen ./testgen/<pkg>/` exits 0.
3. **CI quality gates pass** (per `.github/workflows/ci.yml`):
   - `go build ./...`
   - `go vet ./...`
   - `staticcheck ./...`
   - gocyclo ≤ 15, gocognit ≤ 30 (CI); local gate gocognit ≤ 15, gocyclo ≤ 12
   - `go test -race -count=1 -run "^Test[^C]" ./...` passes
4. **SOLID tests pass**: `go test -run TestSOLID_ ./...`
5. **No regression** in earlier goals.
6. **No re-hidden gaps**: no package re-added to skipTestFiles without evidence.
7. **Every fix verified** against oracle `/usr/bin/sqlite3`.

## Verify Command

```bash
go build ./... && go vet ./... && go test -run TestSOLID_ ./... && go test -tags testgen ./testgen/analyze/ ./testgen/analyze3/ ./testgen/analyze4/ ./testgen/analyze5/ ./testgen/analyze6/ ./testgen/analyze7/ ./testgen/analyze8/ ./testgen/analyze9/ -count=1 -timeout 300s
```

## Micro-Tasks

> **Triage rule**: On testgen failure, write a **pure-Go test** via
> `frigolite.Open/Exec/Query` first. Fails → engine bug. Passes while testgen
> fails → transpiler bug (`tools/tcl2go/`).


> **SOURCE FIRST (mandatory)**: SQLite's approach always wins. Before fixing any
> engine gap, read the actual SQLite source at `/Users/muaddib/dev/sqlite`
> (`src/*.c`, `ext/misc/`, `test/`). The oracle (`/usr/bin/sqlite3`) tells you
> WHAT; the source tells you WHY and HOW. Implement faithfully from the C
> source, then verify against the oracle.
> A gap should always be fixed correctly - faithfully from the C source -
> even if important work is required. Use goals (this plan, the goal tool) to
> plan/replan based on a detailed analysis and plan, never piecemeal hacks.

> **NO TRY/FAIL (mandatory)**: Do not waste time try/failing - step back, clean
> up, review the SQLite source, then write a detailed plan and execute. When
> the work deviates (perf problems, unexpected failures), stop, revert/clean
> the exploratory edits, re-read the relevant SQLite source
> (`/Users/muaddib/dev/sqlite`), produce a written plan (what/why/exact
> files/ordered steps/verify), and only then execute. Do this in a clean
> pre-goal state: no half-applied experiment in the working tree when
> planning.

### Task 1: Un-skip packages

Remove these entries from `tools/tcl2go/skiptestfiles.go`:

- `"analyze"`
- `"analyze3"`
- `"analyze4"`
- `"analyze5"`
- `"analyze6"`
- `"analyze7"`
- `"analyze8"`
- `"analyze9"`
- `"analyzeE"`
- `"autoanalyze1"`
- `"bestindex1"`
- `"bestindex2"`
- `"bestindex3"`
- `"bestindex4"`
- `"bestindex5"`
- `"bestindex6"`
- `"bestindex7"`
- `"bestindex8"`
- `"bestindex9"`
- `"bestindexB"`
- `"bestindexC"`
- `"bestindexE"`
- `"bestindexF"`
- `"bestindexG"`

Regenerate: `go run ./tools/tcl2go/`

### Task 2: Build and identify failures

```bash
go build ./...
go test -tags testgen ./testgen/analyze/ ./testgen/analyze3/ ./testgen/analyze4/ ./testgen/analyze5/ ./testgen/analyze6/ ./testgen/analyze7/ ./testgen/analyze8/ ./testgen/analyze9/ ./testgen/analyzeE/ ./testgen/autoanalyze1/ ./testgen/bestindex1/ ./testgen/bestindex2/ ./testgen/bestindex3/ ./testgen/bestindex4/ ./testgen/bestindex5/ ./testgen/bestindex6/ ./testgen/bestindex7/ ./testgen/bestindex8/ ./testgen/bestindex9/ ./testgen/bestindexB/ ./testgen/bestindexC/ ./testgen/bestindexE/ ./testgen/bestindexF/ ./testgen/bestindexG/ -count=1 -timeout 300s 2>&1 | tee /tmp/P7_PLANNER_failures.txt
```

Categorize failures: engine bug / transpiler bug / compile error.

### Task 3: Fix each failure (iterate)

For each failure:
1. Write a pure-Go pre-test reproducing the bug.
2. Verify expected output with `/usr/bin/sqlite3`.
3. Fix engine (`internal/`) or transpiler (`tools/tcl2go/`).
4. Re-run pure-Go test, then testgen package.
5. **Checkpoint**: commit + push:
   ```
   git add -A
   git commit -m "P7.PLANNER.fix: <description>"
   git push
   ```

### Task 4: CI quality gates

```bash
go build ./... && go vet ./... && staticcheck ./...
go test -race -count=1 -run "^Test[^C]" ./...
go test -run TestSOLID_ ./...
```

### Task 5: Final checkpoint

1. Run full verify command for this goal.
2. Verify no regressions in earlier goals.
3. **Checkpoint**:
   - Update `PORTPLAN.md` status table.
   - `git add -A && git commit -m "P7.PLANNER: all packages green" && git push`

## Checkpointing Protocol

After **every** fix: update sub-plan task status → commit → push.
Resumes from committed state only. No work lost on interruption.

> **NO TRY/FAIL LOOP (mandatory)**: Do not fall into a try/fail loop - when facing issue: Take a step back - consider sqlite approach by checking it's source (@../sqlite/) - write a dedicated fix plan then execute.

> **NO SIMPLIFY (mandatory)**: Do not simplify an issue - the fix must be complete and have the same fundamental approach as sqlite.
## Mandatory per-task quality gate

Before task completion, run `tools/quality_gate.sh <changed-production-go-files>` and require success, passing only newly added or materially changed production non-test files. This gate enforces complexity and file-size limits for new/changed code and staticcheck diagnostics attributable to task changes. Findings in untouched legacy code are deferred exclusively to final plan closure; task changes must introduce zero new findings. Record command and output here.

---

### T27 P7.PLANNER.bestindex (2026-09-11) — vtab xBestIndex contract

TARGET: bestindex1-9, bestindexA-G, autoanalyze1 (17 testgen packages).

#### BASELINE (§5g item 2, before any edit)

`tools/status/last_run.json` (generated 2026-09-11T00:09:07Z):

| package | state |
|---|---|
| bestindex1-9, bestindexB, bestindexC, bestindexE, bestindexF, bestindexG, autoanalyze1 | skipped (skiptestfiles DEFERRED entries, tools/tcl2go/skiptestfiles.go:351-371) |
| bestindexA | fail 508ms: `declare_vtab: syntax error` on `CREATE VIRTUAL TABLE t1 USING tcl(vtab_command)` |
| bestindexD | fail 552ms: same error on `CREATE VIRTUAL TABLE x1 USING tcl(vtab_command)` |

Serial confirm (this session): `go test -tags testgen ./testgen/bestindexA/ ./testgen/bestindexD/ -count=1`
→ both FAIL with `declare_vtab: syntax error` (the `tcl` module resolves its
schema through vtab.TclVarGet("vtab_command"), but `vtab_command` is a TCL
proc the transpiler did not transpile → empty schema).

#### DIAGNOSIS (why un-skip alone cannot green this family)

Every bestindex* source (test/bestindex{1-9,A-E}.test) drives a FIXTURE MODULE
whose methods are TCL procs registered by `register_tcl_module db`
(tclsqlite.c). The generated tests contain
`// register_tcl_module db (unsupported command, not transpiled)` and
`// do_xbestindex_test ... (unsupported command, not transpiled)` — there is
no transpiled assertion to satisfy: the module implementation itself
(xConnect/xBestIndex/xFilter callbacks, `::vtab_constraints` variable capture,
`$hdl constraints`/`orderby`/`mask`/`distinct` subcommands) is TCL-proc
infrastructure that tcl2go cannot express. Per the coordinator's directive
these are the genuinely untranspilable parts; the ENGINE side of the contract
they exercise must be implemented and pinned by native tests (supplement, not
replacement, per .agents/lessons_learned.md MANDATORY RULES).

Engine gaps identified (from src/where.c allocateIndexInfo /
whereLoopAddVirtualOne / sqlite3WhereAddLimit, src/whereexpr.c
isAuxiliaryVtabOperator, src/vtab.h sqlite3_index_info, src/wherecode.c EQP):

1. No structured xBestIndex contract: `vtab.VirtualTable.BestIndex([]byte)`
   receives nothing and returns nothing (all 30+ modules return `nil, nil`).
   No IndexInfo (constraints/op/usable, orderBy, usage argvIndex+omit,
   idxNum/idxStr, orderByConsumed, estimatedCost/Rows, idxFlags, colUsed,
   distinct flag), no xFilter argv handoff, no xFindFunction overloads.
2. No EQP vtab string: SQLite renders `SCAN <name> VIRTUAL TABLE INDEX
   <idxNum>:<idxStr>` (bestindex1 do_eqp_test); frigolite renders plain
   `SCAN <name>` (execquery/explain_plan.go:66).
3. LIMIT/OFFSET constraints never offered (sqlite3WhereAddLimit).
4. autoanalyze1 needs debug-build-only `PRAGMA stats` (ifcapable !debug
   finish_test — oracle /usr/bin/sqlite3 has DEBUG off, file tests nothing
   there): N-A candidate, not an engine gap.

#### PLAN (ordered steps)

1. internal/vtab/indexinfo.go (NEW): port sqlite3_index_info —
   IndexConstraintOp constants (sqlite3.h:7799-7815), IndexConstraint
   {Column,Op,Usable,IsIn}, IndexOrderBy, ConstraintUsage, IndexInfo
   {Constraints, OrderBy, Distinct, Usage, IdxNum, IdxStr, OrderByConsumed,
   EstimatedCost (default SQLITE_BIG_DBL/2, where.c:4300), EstimatedRows
   (default 25), IdxFlags, ColsUsed}, ErrVtabConstraint (xBestIndex
   SQLITE_CONSTRAINT → plan rejected, vtab.c vtabBestIndex). Optional
   interfaces (Open/Closed — existing 30+ modules untouched):
   PlanBestIndexer{BestIndexPlan(*IndexInfo) error} (xBestIndex),
   PlanFilterer{FilterPlan(idxNum, idxStr, argv)} on Cursor (xFilter),
   FunctionOverloader{FindFunction(name, nArg) int} (xFindFunction).
2. execquery.VtabScanOptions: add OrderBy, Distinct, ColsUsed, Limit/Offset
   exprs; vtabScanOptions populates them (single-table vtab path).
3. execquery BuildVtabIndexInfo (NEW file vtab_bestindex.go): port
   allocateIndexInfo (conjunct enumeration + op mapping, IN→EQ, usable =
   prereqRight==0), isAuxiliaryVtabOperator (MATCH/LIKE/GLOB/REGEXP aux ops
   incl. function-arg form; xFindFunction overloads >=150; NE/ISNOT/ISNOTNULL
   aux), sqlite3WhereAddLimit (conditions 1-5: single vtab FROM, no
   aggregate/DISTINCT/GROUP BY, all terms passable, plain-column ORDER BY;
   OFFSET term before LIMIT), sqlite3_vtab_distinct flags (1 GROUP BY,
   2 DISTINCT, 3 DISTINCT+ORDER BY) with synthesized distinct aOrderBy.
4. exec materializer glue (NEW internal/exec/vtab_bestindex.go):
   materializeVtabModule calls BuildVtabIndexInfo + BestIndexPlan when the
   instance implements PlanBestIndexer; validates contiguous argvIndex
   ("xBestIndex malfunction", where.c:4366); binds argv values in
   argvIndex order and calls Cursor FilterPlan before first Next; residual =
   WHERE minus consumed (omit + argvIndex>0) conjuncts; LIMIT/OFFSET applied
   by the engine after materialization as before (module claiming them does
   not change observable rows — engine re-application is idempotent).
5. EQP: explain_plan.go renders `SCAN <name> VIRTUAL TABLE INDEX <n>:<s>`
   for created-vtab sources, calling the module's BestIndexPlan at
   explain time (prepare-time xBestIndex parity, wherecode.c:sqlite3WhereExplain).
6. Public fixture-module API: DB.RegisterVtabModule (sqlite3_create_module
   parity) so native tests can register Go fixture modules.
7. Native supplement tests frigolite_bestindex_test.go: fixture module
   recording IndexInfo; essential assertions ported: bestindexA (constraint
   enumeration eq/ne/overload/limit, `?=a` normalization, `(b+1)=?` excluded),
   bestindex1 (usable flag, idxNum/idxStr round-trip, EQP string), bestindexD
   (colUsed mask), bestindex9 (orderby + distinct flags), bestindexE 1.x
   (enumeration a=?/b=?), bestindexB (default plan + xFilter rows),
   bestindex2/3 (omit/use semantics on rows), bestindex4 (cost/rows
   observation), bestindex5 (ORDER BY consumption observation).
8. skiptestfiles: bestindex1-9/A-G → N-A with evidence pointer to the native
   test (untranspilable register_tcl_module harness; engine contract
   implemented + pinned); autoanalyze1 → N-A (debug-only PRAGMA stats,
   oracle-skipped). NA_EVIDENCE.md entries. Regen via tcl2go.
9. Gates: build/vet/SOLID/race TestNative/quality_gate.sh on changed files;
   vtab-family regression (vtab1-9, vtabA-F/H-K, tabfunc01, csv01, rtree2);
   40-package sweep after regen.

VERIFY: per step 9 + `go test -tags testgen ./testgen/bestindexA/ ./testgen/bestindexD/` post-regen (stubs).
