
> **Phase**: P8 | **Dependencies**: P1, P2
> **Packages**: 24 testgen packages
> **SQLite reference**: `src/pager.c, src/btree.c`

## Objective

Pager/page-format/btree validation (SQL surface)

> **TAKEOVER NOTE (2026-09-06)**: the `.goa` goal tool was REMOVED by the user.
> Goal tracking for this goal = THIS file (status + close status), `PORTPLAN.md`
> §2/§4 live markers, and the agent todo list. Commits are the only durable
> checkpoint record — commit + push after every task (protocol below).

## Baseline & Progress (2026-09-06 — ACTIVE)

- **Pre-goal state**: 24 packages in `tools/tcl2go/skiptestfiles.go`
  (bigfile..stmtrand), all skipped no-op stubs; `pendingrace` was un-skipped
  as P8.PRAGMA collateral and FAILED (`pendingrace_test.go:140`:
  `PRAGMA integrity_check` on a locked db must error "database is locked",
  got nil). 5 INCRVACUUM-window regressions (lock, pcache, corruptB,
  fts3corrupt4, tkt_fc62af4523) are owned by FULL-SUITE-DRIFT, NOT this goal
  (ledger evidence amended 2026-09-06).
- **T0 pre-regen baseline** (HEAD c532117c): goal verify command exited 0 —
  but only because 23 of 24 packages were still trivial no-op stubs. The real
  baseline is the post-regen census below.
- **T1 done**: removed the 23 remaining skip entries from
  `tools/tcl2go/skiptestfiles.go`; regenerated testgen (1,219 files). Regen
  delta = removal of a stray `QLDBG` debug Println from the shared helpers
  template (fixed in 9c8a3907 but suite not re-regenerated) + the 23 package
  bodies becoming real.
- **T2 census**: complete. Batches 1-2 landed; see fix order + batch plans.

## Current Status (2026-09-06 — ACTIVE, batches 1-2 landed)

**Committed/pushed**: 2ff1c238 (T1 un-skip+regen), 7104478c (batch 1),
f19e260a (batch 2). Score after batch 2: **11/24 pass**
(bigfile, bigfile2, btree01, btree02, pager1, pager2, pager3, pager4,
quickcheck, readonly, stmtrand), 13 red. Batch 1+2 were verified with
spot-checks only (lock6/tkt1667/tkt4018/corrupt2/autovacuum/journal1/
journal2/jrnlmode2/quota*/backup); the full-suite no-flip gate is T4 at
close. `go build ./...` + SOLID green at f19e260a.

**Batch 1 (7104478c)** — stmtrand() engine fn (ext/misc/stmtrand.c port,
statement-scoped LCG in execexpr auxdata; sequences validated against TCL
literals); cteMainVerbIsQuery rewrite (CTE column list dropped result
comparisons suite-wide); read-count [expr] reconstruction; pending-byte
engine movement + previous-value capture (pager1 green). memdb1 deferred
(serialize tranche).

**Batch 2 (f19e260a)** — SQLITE_READONLY write path: pager.Open permission
fallback (O_RDWR EACCES → O_RDONLY + readOnly flag; the flag existed but was
never set); journal-open databaseFileMoved() identity check (pager.c
databaseIsUnmoved) → OFF/MEMORY writable, DELETE/TRUNCATE/PERSIST give
"attempt to write a readonly database" on a moved db; engine gates
(execDMLWritable/execOtherDDL) reject on ReadOnly pager; transpiler
tclFileChmod (octal + TCL symbolic perms), file delete -force consumes the
flag, file rename emits os.Rename. pager4 + readonly green.

### NEXT STEP (T3.3) — filefmt + pagesize (header/page-size validation)

- filefmt: "file is not a database" on garbage headers (filefmt_test.go:111);
  invalid header page sizes must error mentioning the size (:145 ×8:
  512..32768); :286 mismatch. Source of truth: btree.c lockBtree SQLITE_NOTADB
  checks (header page-size field 16-17 must be a power of 2 in [512,65536],
  value 1 means 65536). NOTE: frigolite's Pager.ValidateHeader was
  deliberately narrowed in P8.PRAGMA (corruption must surface as ROWS for
  integrity_check, NOTADB at schema load) — re-read that history before
  touching it.
- pagesize: PRAGMA page_size change on a populated db → "database disk image
  is malformed" (:92/:111/:117 — engine bug in the SetPageSize/
  updateDBHeaderField path); :323/:341 mismatches. Oracle:
  /usr/bin/sqlite3 page_size change sequences.
- Pure-Go discriminators for both, oracle-verified, one fix commit.

### THEN (T3.4) — pendingrace (hot-journal contract)

- Discriminator (ran 2026-09-06, /tmp/pagerdisc/discrim_test.go): hot journal
  restored over the original db + idle second connection →
  `PRAGMA integrity_check` returns "ok" (nil error) — testgen expects
  "database is locked". The TCL expectation is produced by tvfs2 xUnlock
  fault-injection (stripped by the transpiler), so the ENGINE-VISIBLE
  contract is: a hot journal left by a "crash" must be detected and played
  back before any read succeeds (never read the crashed state).
- FIRST ACTION: in the discriminator, after integrity_check, check whether
  test.db-journal still exists and whether the 10 t1 rows survive (SQLite
  deletes the journal after playback). If frigolite IGNORES hot journals →
  implement playback (pager.c pagerPlayBack + hot-journal detection at read
  start) — real engine work, NO_SIMPLIFY applies.
- Separate observation: discriminator page_count=13 vs oracle 20/21 for the
  cache_size=5 workload (btree fill divergence) — investigate with oracle.
- The transpiled :140 expectation may be unsatisfiable without fault
  injection; only if the honest contract (playback) lands and the assertion
  still cannot pass, document the harness limitation in NA_EVIDENCE.

### THEN (T3.5) — remaining reds, in this order

1. memdb1 serialize/deserialize tranche: DB.Serialize() ([]byte image,
   len == page_size×page_count) + DB.Deserialize(img, -readonly/-maxsize/
   aux-schema flags) with error contracts (not-a-database etc.); transpiler:
   `set ::db1 [db serialize]` collides with the pre-declared
   `var db1 *frigolite.DB` (string shadow or placeholder retype needed) and
   `db deserialize $db1` (only the decode_hexdb form is handled today).
   Mirror src/memdb.c + sqlite3_deserialize (test/memdb1.test 8.x-12.x).
2. memdb2: generated code opens `/test.db` (root path leak — inspect the
   `sqlite3` command transpilation; likely a missing filename argument).
3. memdb: :149 mismatches ×8 + :159 "signature one" (memdb.test signature
   checks); memsubsys1/2: sqlite3_status/memctrl harness seams; cache:
   pager cache stats (panic at :101).
4. bitvec: 300s HANG at :180 — instrument the loop first (engine bitvec vs
   transpiler loop before touching anything).
5. sqllimits1: limit mismatches (:164-218) + panic :222 — SQLITE_MAX_*
   enforcement (engine.SetLimit seams exist).
6. oserror: VFS fault injection stripped (panic :134) — likely harness NA
   with a native port of the engine-visible I/O-error contracts.
7. sqldiff1: sqldiff tool output (:101/:140) — check how the harness
   computes the diff (native seam) before classifying.

### Close sequence (unchanged)

T4 full-suite run + ledger re-seed + `tools/status --check` zero-flip gate
(regen = suite-wide event §5g item 4) → T5 gates (build/vet/staticcheck/
quality_gate(changed)/-race/SOLID) + sub-plan close + PORTPLAN updates →
T6 commit+push + lessons_learned session entry.

### Census detail (2026-09-06, /tmp/p8pager_census.log)

7 PASS pre-fix: bigfile, bigfile2, btree01, btree02, pager2, pager3,
quickcheck. Batch 1+2 fixed: stmtrand, pager1, pager4, readonly. Remaining
13 red with first failures:

| package | state | class / first failures |
|---------|-------|------------------------|
| bitvec | HANG (300s timeout) | loop at bitvec_test.go:180; stack frames helpers_test.go:1599 |
| cache | FAIL | panic cache_test.go:101 (cache-status harness seam) |
| filefmt | FAIL | header validation: "file is not a database" missing; invalid-page-size errors (512..32768) missing; mismatch :286 |
| memdb | FAIL | :149 result mismatch ×8; :159 expected err containing "signature one" got nil |
| memdb1 | BUILD FAIL | db1 var collision (serialize tranche, T3.5 item 1); read-expr fixed in batch 1 |
| memdb2 | FAIL | opens `/test.db` (root path leak) → read-only fs error |
| memsubsys1 | FAIL | :151/:224/:233 result mismatch (memory-status counters) |
| memsubsys2 | FAIL | panic memsubsys2_test.go:84 |
| oserror | FAIL | panic oserror_test.go:134 (VFS error injection stripped) |
| pagesize | FAIL | page_size change → "database disk image is malformed" :92/:111/:117; mismatch :323/:341 |
| pendingrace | FAIL | :140 integrity_check on hot-journal db expected "database is locked" got nil (see T3.4 plan) |
| sqldiff1 | FAIL | :101/:140 result mismatch (sqldiff tool seam) |
| sqllimits1 | FAIL | limit mismatch ×7 (:164..:218) + panic :222 |

### Fix order (commit per class)

1. ✅ Transpiler batch (7104478c): memdb1 read parse error, pager1
   sqlite_pending_byte, stmtrand UDF (+engine fn).
2. ✅ pager4 + readonly SQLITE_READONLY write path (f19e260a).
3. ⏭ NEXT: filefmt + pagesize (header/page-size validation) — see T3.3.
4. pendingrace (hot-journal playback contract; see T3.4).
5. cache, memsubsys1/2, memdb, memdb2, bitvec, sqllimits1, oserror, sqldiff1
   (see T3.5 order).

### Batch 1 plan (root causes + exact changes) — EXECUTED in 7104478c

1. **stmtrand() engine function** (ext/misc/stmtrand.c port):
   - Discriminator: LCG validated against the TCL literal sequences
     (seed 0 → `4 1 8 3 6 1 4 9 2 1 2 1 0 1 8 1 4 7 6 1 8 7 0 3 0 9 4 5 9 9`,
     seed 1 → `3 8 9 4 7 2 3 8 5 2 7 0 3 0 7 2 5 6 1 8 5 6 7 8 7 2 9 6 8 0`)
     — Go uint32 arithmetic matches exactly (x=(x>>1)^((1+~(x&1))&0xd0000001);
     y=y*1103515245+12345; result=(x^y)&0x7fffffff).
   - Auxdata semantics are observable: 1.4 `stmtrand(x)` (seed=first row's x=1)
     produces the SAME sequence as 1.3 `stmtrand(1)` → one LCG per STATEMENT,
     seeded by the FIRST call's argument; later args ignored; reset per
     statement.
   - Changes: `internal/execexpr/types.go` (Evaluator.stmtAux +
     AuxData/SetAuxData/ResetStatementAux), `expression_eval.go`
     evalEngineFunc `case "STMTRAND"`, `internal/exec/engine_core.go` Exec
     reset next to counterVal (outermost execDepth only — nested trigger
     statements share the outer aux, a documented approximation),
     `internal/function/function.go` registration (0-1 args, fresh-state
     fallback ScalarFn for contexts without statement scope).
2. **cteMainVerbIsQuery** (tools/tcl2go/sqlhelpers.go): any CTE with a column
   list `WITH name(cols) AS (...) SELECT` mis-classifies as non-query
   (the name's `()` closes depth→0 and the tail starts with "AS" → false),
   so do_execsql_test drops the result comparison (stmtrand 1.1-1.4). Rewrite
   as a structured scan: name → optional balanced column-list → AS → balanced
   body → [`,` more CTEs] → main verb SELECT/VALUES ⇒ query.
3. **`read $fd <expr>` count** (processset.go ~714 + processset_part2.go ~209):
   `set data [read $fd [expr 20*1024]]` split the expr across whitespace
   Fields and emitted `tclReadFileWithLen(fd, [expr)` → Go syntax error
   (memdb1 436). Reconstruct the bracket-balanced count text; transpile-fold
   plain literal exprs, else emit `toInt(<runtimeExprValue>)`.
4. **pending byte** (pager1 build + faithful movement):
   - gen.go: extend the shadow-var pre-declaration to sources containing
     `sqlite3_test_control_pending_byte` (pager1 assigns the shadow via that
     command but never mentions the global).
   - processSqlite3TestControlPendingByte: emit `db.SetPendingByteMain(n)`
     (the engine seam exists, engine.go:408) alongside the shadow var;
     `$var` args emit runtime dispatch (current code errors on ParseInt).
   - `set pending_prev [sqlite3_test_control_pending_byte 0x1000000]` must
     capture the PREVIOUS byte (test/42.x restores it) — make
     Engine.SetPendingByteMain return the previous offset (additive), DB
     wrapper likewise, transpile the set-capture form.
   - Engine read-side behavior for a pending-byte page inside an existing db
     (pager1 42.1 expects "database disk image is malformed") — RESOLVED FOR
     NOW: the generated 42.1 catchsql carries NO assertion (`_ = _res //
     catchsql`), so the read-side contract is not observed by testgen and
     pager1 passes without it. C-source investigation so far: btree.c has
     only an overflow-page pending check (accessPayload, ~:7169); no general
     read-side refusal found in pager.c/btree.c. If a native test ever needs
     the malformed behavior, settle the mechanism by running the real pager1
     42.1 against a scratch C build of sqlite3.c (test_control) first.

### UCL tranche plan (§5b: fixtures/discriminators BEFORE engine edits)

1. **pendingrace** first (already un-skipped, known FAIL) — locked-db
   integrity_check must return "database is locked" (src/btree.c
   querySharedCacheTableLock / pager.c SQLITE_BUSY paths).
2. Per-package census → group failures by engine class (pager error paths,
   page-format validation, cache behavior, memdb/vfs, limits) — one fix
   commit per class, pure-Go discriminator + oracle check before each engine
   edit, native regression test per fixed seam.
3. `bigfile`/`bigfile2` (>4GB) may expose 32-bit page-count math; check
   header field 28 + `PRAGMA max_page_count` against oracle. If the >4GB
   harness itself is transpiler-N-A, record NA_EVIDENCE (harness limitation,
   not engine gap) — no engine skip.
4. Full-suite run + ledger re-seed + `--check` zero-flip gate at close
   (regen = suite-wide event, §5g item 4).

## Target Packages (24)

- `bigfile`
- `bigfile2`
- `bitvec`
- `btree01`
- `btree02`
- `cache`
- `filefmt`
- `memdb`
- `memdb1`
- `memdb2`
- `memsubsys1`
- `memsubsys2`
- `oserror`
- `pager1`
- `pager2`
- `pager3`
- `pager4`
- `pagesize`
- `pendingrace`
- `quickcheck`
- `readonly`
- `sqldiff1`
- `sqllimits1`
- `stmtrand`

## Definition of Done

1. **All packages un-skipped**: every `P8.PAGER` package removed from
   `tools/tcl2go/skiptestfiles.go`.
2. **All packages pass**: `go test -tags testgen ./testgen/<pkg>/` exits 0.
3. **CI quality gates pass** (per `.github/workflows/ci.yml`):
   - `go build ./...`
   - `go vet ./...`
   - `staticcheck ./...`
   - gocyclo ≤ 15, gocognit ≤ 30 (CI); local gate gocognit ≤ 15, gocyclo ≤ 12
   - `go test -race -count=1 -run "^Test[^C]" ./...` passes
4. **SOLID tests pass**: `go test -run TestSOLID_ ./...`
5. **No regression** in earlier goals.
6. **No re-hidden gaps**: no package re-added to skipTestFiles without evidence.
7. **Every fix verified** against oracle `/usr/bin/sqlite3`.

## Verify Command

```bash
go build ./... && go vet ./... && go test -run TestSOLID_ ./... && go test -tags testgen ./testgen/bigfile/ ./testgen/bigfile2/ ./testgen/bitvec/ ./testgen/btree01/ ./testgen/btree02/ ./testgen/cache/ ./testgen/filefmt/ ./testgen/memdb/ ./testgen/memdb1/ ./testgen/memdb2/ ./testgen/memsubsys1/ ./testgen/memsubsys2/ ./testgen/oserror/ ./testgen/pager1/ ./testgen/pager2/ ./testgen/pager3/ ./testgen/pager4/ ./testgen/pagesize/ ./testgen/pendingrace/ ./testgen/quickcheck/ ./testgen/readonly/ ./testgen/sqldiff1/ ./testgen/sqllimits1/ ./testgen/stmtrand/ -count=1 -timeout 600s
```

## Micro-Tasks

> **Triage rule**: On testgen failure, write a **pure-Go test** via
> `frigolite.Open/Exec/Query` first. Fails → engine bug. Passes while testgen
> fails → transpiler bug (`tools/tcl2go/`).


> **SOURCE FIRST (mandatory)**: SQLite's approach always wins. Before fixing any
> engine gap, read the actual SQLite source at `/Users/muaddib/dev/sqlite`
> (`src/*.c`, `ext/misc/`, `test/`). The oracle (`/usr/bin/sqlite3`) tells you
> WHAT; the source tells you WHY and HOW. Implement faithfully from the C
> source, then verify against the oracle.
> A gap should always be fixed correctly - faithfully from the C source -
> even if important work is required. Use goals (this plan, the goal tool) to
> plan/replan based on a detailed analysis and plan, never piecemeal hacks.

> **NO TRY/FAIL (mandatory)**: Do not waste time try/failing - step back, clean
> up, review the SQLite source, then write a detailed plan and execute. When
> the work deviates (perf problems, unexpected failures), stop, revert/clean
> the exploratory edits, re-read the relevant SQLite source
> (`/Users/muaddib/dev/sqlite`), produce a written plan (what/why/exact
> files/ordered steps/verify), and only then execute. Do this in a clean
> pre-goal state: no half-applied experiment in the working tree when
> planning.

### Task 1: Un-skip packages

Remove these entries from `tools/tcl2go/skiptestfiles.go`:

- `"bigfile"`
- `"bigfile2"`
- `"bitvec"`
- `"btree01"`
- `"btree02"`
- `"cache"`
- `"filefmt"`
- `"memdb"`
- `"memdb1"`
- `"memdb2"`
- `"memsubsys1"`
- `"memsubsys2"`
- `"oserror"`
- `"pager1"`
- `"pager2"`
- `"pager3"`
- `"pager4"`
- `"pagesize"`
- `"pendingrace"`
- `"quickcheck"`
- `"readonly"`
- `"sqldiff1"`
- `"sqllimits1"`
- `"stmtrand"`

Regenerate: `go run ./tools/tcl2go/`

### Task 2: Build and identify failures

```bash
go build ./...
go test -tags testgen ./testgen/bigfile/ ./testgen/bigfile2/ ./testgen/bitvec/ ./testgen/btree01/ ./testgen/btree02/ ./testgen/cache/ ./testgen/filefmt/ ./testgen/memdb/ ./testgen/memdb1/ ./testgen/memdb2/ ./testgen/memsubsys1/ ./testgen/memsubsys2/ ./testgen/oserror/ ./testgen/pager1/ ./testgen/pager2/ ./testgen/pager3/ ./testgen/pager4/ ./testgen/pagesize/ ./testgen/pendingrace/ ./testgen/quickcheck/ ./testgen/readonly/ ./testgen/sqldiff1/ ./testgen/sqllimits1/ ./testgen/stmtrand/ -count=1 -timeout 300s 2>&1 | tee /tmp/P8_PAGER_failures.txt
```

Categorize failures: engine bug / transpiler bug / compile error.

### Task 3: Fix each failure (iterate)

For each failure:
1. Write a pure-Go pre-test reproducing the bug.
2. Verify expected output with `/usr/bin/sqlite3`.
3. Fix engine (`internal/`) or transpiler (`tools/tcl2go/`).
4. Re-run pure-Go test, then testgen package.
5. **Checkpoint**: commit + push:
   ```
   git add -A
   git commit -m "P8.PAGER.fix: <description>"
   git push
   ```

### Task 4: CI quality gates

```bash
go build ./... && go vet ./... && staticcheck ./...
go test -race -count=1 -run "^Test[^C]" ./...
go test -run TestSOLID_ ./...
```

### Task 5: Final checkpoint

1. Run full verify command for this goal.
2. Verify no regressions in earlier goals.
3. **Checkpoint**:
   - Update `PORTPLAN.md` status table.
   - `git add -A && git commit -m "P8.PAGER: all packages green" && git push`

## Checkpointing Protocol

After **every** fix: update sub-plan task status → commit → push.
Resumes from committed state only. No work lost on interruption.

> **NO TRY/FAIL LOOP (mandatory)**: Do not fall into a try/fail loop - when facing issue: Take a step back - consider sqlite approach by checking it's source (@../sqlite/) - write a dedicated fix plan then execute.

> **NO SIMPLIFY (mandatory)**: Do not simplify an issue - the fix must be complete and have the same fundamental approach as sqlite.
## Mandatory per-task quality gate

Before task completion, run `tools/quality_gate.sh <changed-production-go-files>` and require success, passing only newly added or materially changed production non-test files. This gate enforces complexity and file-size limits for new/changed code and staticcheck diagnostics attributable to task changes. Findings in untouched legacy code are deferred exclusively to final plan closure; task changes must introduce zero new findings. Record command and output here.
