
> **Phase**: P8 | **Dependencies**: P1, P2
> **Packages**: 1 testgen packages
> **SQLite reference**: `ext/misc/recover.c`

## Objective

.recover (corrupt-db recovery)

## Target Packages (1)

- `recover_pkg` *(generated from `recover.test`; dir name differs from TCL file name)*

## Definition of Done

1. **All packages un-skipped**: every `P8.RECOVER` package removed from
   `tools/tcl2go/skiptestfiles.go`.
2. **All packages pass**: `go test -tags testgen ./testgen/<pkg>/` exits 0.
3. **CI quality gates pass** (per `.github/workflows/ci.yml`):
   - `go build ./...`
   - `go vet ./...`
   - `staticcheck ./...`
   - gocyclo ≤ 15, gocognit ≤ 30 (CI); local gate gocognit ≤ 15, gocyclo ≤ 12
   - `go test -race -count=1 -run "^Test[^C]" ./...` passes
4. **SOLID tests pass**: `go test -run TestSOLID_ ./...`
5. **No regression** in earlier goals.
6. **No re-hidden gaps**: no package re-added to skipTestFiles without evidence.
7. **Every fix verified** against oracle `/usr/bin/sqlite3`.

## Verify Command

```bash
go build ./... && go vet ./... && go test -run TestSOLID_ ./... && go test -tags testgen ./testgen/recover_pkg/ -count=1 -timeout 300s
```

## Micro-Tasks

> **Triage rule**: On testgen failure, write a **pure-Go test** via
> `frigolite.Open/Exec/Query` first. Fails → engine bug. Passes while testgen
> fails → transpiler bug (`tools/tcl2go/`).


> **SOURCE FIRST (mandatory)**: SQLite's approach always wins. Before fixing any
> engine gap, read the actual SQLite source at `/Users/muaddib/dev/sqlite`
> (`src/*.c`, `ext/misc/`, `test/`). The oracle (`/usr/bin/sqlite3`) tells you
> WHAT; the source tells you WHY and HOW. Implement faithfully from the C
> source, then verify against the oracle.
> A gap should always be fixed correctly - faithfully from the C source -
> even if important work is required. Use goals (this plan, the goal tool) to
> plan/replan based on a detailed analysis and plan, never piecemeal hacks.

> **NO TRY/FAIL (mandatory)**: Do not waste time try/failing - step back, clean
> up, review the SQLite source, then write a detailed plan and execute. When
> the work deviates (perf problems, unexpected failures), stop, revert/clean
> the exploratory edits, re-read the relevant SQLite source
> (`/Users/muaddib/dev/sqlite`), produce a written plan (what/why/exact
> files/ordered steps/verify), and only then execute. Do this in a clean
> pre-goal state: no half-applied experiment in the working tree when
> planning.

### Task 1: Un-skip packages

Remove these entries from `tools/tcl2go/skiptestfiles.go`:

- `"recover"`

Regenerate: `go run ./tools/tcl2go/`

### Task 2: Build and identify failures

```bash
go build ./...
go test -tags testgen ./testgen/recover_pkg/ -count=1 -timeout 300s 2>&1 | tee /tmp/P8_RECOVER_failures.txt
```

Categorize failures: engine bug / transpiler bug / compile error.

### Task 3: Fix each failure (iterate)

For each failure:
1. Write a pure-Go pre-test reproducing the bug.
2. Verify expected output with `/usr/bin/sqlite3`.
3. Fix engine (`internal/`) or transpiler (`tools/tcl2go/`).
4. Re-run pure-Go test, then testgen package.
5. **Checkpoint**: commit + push:
   ```
   git add -A
   git commit -m "P8.RECOVER.fix: <description>"
   git push
   ```

### Task 4: CI quality gates

```bash
go build ./... && go vet ./... && staticcheck ./...
go test -race -count=1 -run "^Test[^C]" ./...
go test -run TestSOLID_ ./...
```

### Task 5: Final checkpoint

1. Run full verify command for this goal.
2. Verify no regressions in earlier goals.
3. **Checkpoint**:
   - Update `PORTPLAN.md` status table.
   - `git add -A && git commit -m "P8.RECOVER: all packages green" && git push`

## Checkpointing Protocol

After **every** fix: update sub-plan task status → commit → push.
Resumes from committed state only. No work lost on interruption.

> **NO TRY/FAIL LOOP (mandatory)**: Do not fall into a try/fail loop - when facing issue: Take a step back - consider sqlite approach by checking it's source (@../sqlite/) - write a dedicated fix plan then execute.

> **NO SIMPLIFY (mandatory)**: Do not simplify an issue - the fix must be complete and have the same fundamental approach as sqlite.
## Mandatory per-task quality gate

Before task completion, run `tools/quality_gate.sh <changed-production-go-files>` and require success, passing only newly added or materially changed production non-test files. This gate enforces complexity and file-size limits for new/changed code and staticcheck diagnostics attributable to task changes. Findings in untouched legacy code are deferred exclusively to final plan closure; task changes must introduce zero new findings. Record command and output here.

## T0/T1 BASELINE + PORT PLAN (2026-09-07)

**T0/T1 census**: `recover_pkg` was a vacuous no-op stub
(`func Test_recover(t *testing.T) {}`, "deep-engine applicable gap
DEFERRED") — it "passes" without exercising anything. It is un-skipped and
runs clean (0.37s): the anti-drift baseline is green-with-stub. No other
package is affected by this goal.

**What recover.test actually drives** (test/recover.test, 182 lines):
`recover_with_opts OPTS` locates the external sqlite3 CLI
(`test_find_cli`), runs `.recover OPTS` as a subprocess over test.db,
strips the ".dbconfig defensive off" line, writes the emitted SQL into
test.db2, then compares db vs db2 (schema + per-table contents; the 2.x
block expects orphaned WITHOUT ROWID rows in lost_and_found; 4.x expects
dropped-table rows in lost_and_found and honors -ignore-freelist).

**Reference port scope** (ext/recover/):
- sqlite3recover.c — 2,901 lines: recovery engine (schema walk, row
  recovery, lost_and_found, freelist handling, SQL generation).
- dbdata.c — 1,023 lines: sqlite_dbdata page reader (cell enumeration +
  overflow reassembly) — also the P6.DBDATA seam.
- test_recover.c — 309 lines: TCL bindings (.recover options).

**UCL tranche (fixtures captured, committed)**:
testdata/recoverconformance/ — s1_1 (rowid table + blob + embedded
newline), s1_2 (CR/LF text), s1_3 (AUTOINCREMENT + empty t2), s2_1
(WITHOUT ROWID), s2_2 (schema-deleted orphans -> lost_and_found), s3_0
(minimal db), s4_1 (dropped tables -> lost_and_found, freelist rows).
Each scenario: `<name>.input.db` + `<name>.recover.sql` (reference 3.51.0
build output). The recovered-SQL format to reproduce: .dbconfig line
(stripped by the harness), BEGIN, PRAGMA preamble (writable_schema/
foreign_keys/encoding/page_size/auto_vacuum/user_version/application_id),
CREATE TABLE/INDEX statements, INSERT OR IGNORE INTO 'tbl'('c',...)
VALUES (...), lost_and_found(rootpgno, pgno, nfield, id, c0..cN), COMMIT.

**Port plan (tranches, commit per tranche)**:
- T1 internal/recover: page reader (dbdata port) — btree page/cell
  enumeration with overflow reassembly, freelist walk.
- T2 schema + rowid-table recovery: sqlite_master walk, CREATE emission,
  row reassembly with column mapping, INSERT OR IGNORE generation, PRAGMA
  preamble — target: s1_1/s1_2/s3_0 fixtures byte-comparable.
- T3 WITHOUT ROWID (index-btree scan, PK reordering) — s2_1.
- T4 lost_and_found: orphan-page detection (pages unreachable from any
  root and not freelist), -ignore-freelist — s2_2/s4_1.
- T5 AUTOINCREMENT sequence recovery (sqlite_sequence) — s1_3.
- T6 transpiler: recover_with_opts/do_recover_test harness -> in-process
  DB.RecoverRecoverSQL API writing test.db2 (replaces the external-CLI
  subprocess); un-stub recover_pkg.
- T7 close: verify command, full suite, ledger, docs.

**API sketch** (mirrors sqlite3recover.h): Recover{Source, Output
*sql.Builder, IgnoreFreelist, LostAndFound string} with
Run() error emitting the recovery SQL in reference order.

## T-LOG (2026-09-07, continued — page-type-aware WR mapping)

**Status**: 2.1.1 FIXED + committed (16524863); 2.4.1 BLOCKED on engine WR
write-path gap (separate goal required).

**Fix committed**: `recoverTableRows` probes the WR table root page type
once via `isDeclaredOrderWR` helper (internal/recover/recover.go):
- index pages (0x0a/0x02, oracle layout) → PK-first `iField` remap
  (sqlite3recover.c `RecoverColumn.iField` from `PRAGMA index_xinfo`).
- table-leaf pages (0x0d/0x05, frigolite writer layout) → identity mapping.
- Oracle conformance fixtures (s2_1/s2_2) stay green; recover_pkg 2.1.1
  `compare_dbs` now passes (values emitted in declared order match what
  the engine replay expects).

**Validator note**: the objective's "confirm WITHOUT ROWID identity
mapping" is INVALIDATED as a general statement — identity is correct only
for engine-written files; oracle files require PK-first `iField`
(oracle page-2 bytes `06040101090203` = record `[2 3 1]` for row
`(1,2,3)` PK(b,c); `PRAGMA index_xinfo(t1)` = b:0, c:1, a:2).

**Remaining (2.4.1)**: WR orphan rows differ in id (got rowid, want NULL)
and field order (got declared, want storage). Orphan pages carry no schema,
so the PK is unknowable in recover — this needs the engine WR write-path
port (DDL root as index-leaf 0x0a + writeTableRow PK-first reorder +
positional-decode inverse mapping in ~20 files + rowid-less
UPDATE/DELETE addressing; DELETE currently sorts by PK post-scan and
addresses cells by synthetic rowid). Filed as follow-up goal.
