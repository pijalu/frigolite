{
  "file": "analyzeC.test",
  "name": "analyzeC",
  "tests": [
    {
      "name": "1.1",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT *, '#' FROM t1 WHERE a BETWEEN 3 AND 8 ORDER BY c;",
          "expect": "{/.* USING INDEX t1a .a>. AND a<...*/}"
        }
      ]
    },
    {
      "name": "1.2",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT c FROM t1 ORDER BY a;",
          "expect": "{3 111 6 12 9 12}"
        }
      ]
    },
    {
      "name": "1.3",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT c FROM t1 ORDER BY a;",
          "expect": "{/.*SCAN t1 USING INDEX t1a.*/}"
        }
      ]
    },
    {
      "name": "2.1",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT *, '#' FROM t1 WHERE a BETWEEN 3 AND 8 ORDER BY c;",
          "expect": "{~/.*USING INDEX.*/}"
        }
      ]
    },
    {
      "name": "2.2",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT c FROM t1 ORDER BY a;",
          "expect": "{3 111 6 12 9 12}"
        }
      ]
    },
    {
      "name": "2.3",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT c FROM t1 ORDER BY a;",
          "expect": "{~/.*USING INDEX.*/}"
        }
      ]
    },
    {
      "name": "3.1",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT *, '#' FROM t1 WHERE a BETWEEN 3 AND 8 ORDER BY c;",
          "expect": "{~/.*USING INDEX.*/}"
        }
      ]
    },
    {
      "name": "3.2",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT c FROM t1 ORDER BY a;",
          "expect": "{3 111 6 12 9 12}"
        }
      ]
    },
    {
      "name": "3.3",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT c FROM t1 ORDER BY a;",
          "expect": "{~/.*USING INDEX.*/}"
        }
      ]
    },
    {
      "name": "4.1",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT count(a) FROM t1;",
          "expect": "{/.*INDEX t1ab.*/}"
        }
      ]
    },
    {
      "name": "5.1",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT count(a) FROM t1;",
          "expect": "{/.*INDEX t1ab.*/}"
        }
      ]
    }
  ]
}