{
  "file": "autoindex4.test",
  "name": "autoindex4",
  "tests": [
    {
      "name": "autoindex4-1.0",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE TABLE t1(a,b);\n  INSERT INTO t1 VALUES(123,'abc'),(234,'def'),(234,'ghi'),(345,'jkl');\n  CREATE TABLE t2(x,y);\n  INSERT INTO t2 VALUES(987,'zyx'),(654,'wvu'),(987,'rqp');\n\n  SELECT *, '|' FROM t1, t2 WHERE a=234 AND x=987 ORDER BY +b;",
          "expect": "{234 def 987 rqp | 234 def 987 zyx | 234 ghi 987 rqp | 234 ghi 987 zyx |}"
        }
      ]
    },
    {
      "name": "autoindex4-1.2",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT *, '|' FROM t1 LEFT JOIN t2 ON a=234 AND x=555;",
          "expect": "{123 abc {} {} | 234 def {} {} | 234 ghi {} {} | 345 jkl {} {} |}"
        }
      ]
    },
    {
      "name": "autoindex4-1.2-rj",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT t1.*, t2.*, '|' FROM t2 RIGHT JOIN t1 ON a=234 AND x=555;",
          "expect": "{123 abc {} {} | 234 def {} {} | 234 ghi {} {} | 345 jkl {} {} |}"
        }
      ]
    },
    {
      "name": "autoindex4-1.3",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT *, '|' FROM t1 LEFT JOIN t2 ON x=555 WHERE a=234;",
          "expect": "{234 def {} {} | 234 ghi {} {} |}"
        }
      ]
    },
    {
      "name": "autoindex4-1.3-rj",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT t1.*, t2.*, '|' FROM t2 RIGHT JOIN t1 ON x=555 WHERE a=234;",
          "expect": "{234 def {} {} | 234 ghi {} {} |}"
        }
      ]
    },
    {
      "name": "autoindex4-1.4",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT *, '|' FROM t1 LEFT JOIN t2 WHERE a=234 AND x=555;",
          "expect": "{}"
        }
      ]
    },
    {
      "name": "autoindex4-3.0",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE TABLE A(Name text);\n  CREATE TABLE Items(ItemName text , Name text);\n  INSERT INTO Items VALUES('Item1','Parent');\n  INSERT INTO Items VALUES('Item2','Parent');\n  CREATE TABLE B(Name text);\n  \n  SELECT Items.ItemName\n    FROM Items\n      LEFT JOIN A ON (A.Name = Items.ItemName and Items.ItemName = 'dummy')\n      LEFT JOIN B ON (B.Name = Items.ItemName)\n    WHERE Items.Name = 'Parent'\n    ORDER BY Items.ItemName;",
          "expect": "{Item1 Item2}"
        }
      ]
    },
    {
      "name": "autoindex4-3.1",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT Items.ItemName\n    FROM A\n      RIGHT JOIN Items ON (A.Name = Items.ItemName and Items.ItemName = 'dummy')\n      LEFT JOIN B ON (B.Name = Items.ItemName)\n    WHERE Items.Name = 'Parent'\n    ORDER BY Items.ItemName;",
          "expect": "{Item1 Item2}"
        }
      ]
    },
    {
      "name": "autoindex4-3.10",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE INDEX Items_x1 ON Items(ItemName,Name) WHERE ItemName = 'dummy';\n  \n  SELECT Items.ItemName\n    FROM Items\n      LEFT JOIN A ON (A.Name = Items.ItemName and Items.ItemName = 'dummy')\n      LEFT JOIN B ON (B.Name = Items.ItemName)\n    WHERE Items.Name = 'Parent'\n    ORDER BY Items.ItemName;",
          "expect": "{Item1 Item2}"
        },
        {
          "type": "exec",
          "sql": "DROP TABLE IF EXISTS t1;\n       CREATE TABLE t1(a INT, b INT);\n       DROP TABLE IF EXISTS t2;\n       CREATE TABLE t2(x INT, y INT);"
        },
        {
          "type": "exec",
          "sql": "PRAGMA automatic_index=ON;"
        },
        {
          "type": "exec",
          "sql": "PRAGMA automatic_index=OFF;"
        },
        {
          "type": "exec",
          "sql": "PRAGMA automatic_index=ON;"
        }
      ]
    }
  ]
}