{
  "file": "bestindex7.test",
  "name": "bestindex7",
  "tests": [
    {
      "name": "1.2",
      "steps": [
        {
          "type": "query",
          "sql": "select * from vt1 WHERE a=0",
          "expect": "{0}"
        }
      ]
    },
    {
      "name": "1.3",
      "steps": [
        {
          "type": "query",
          "sql": "select * from vt1 WHERE a=1",
          "expect": "{}"
        }
      ]
    },
    {
      "name": "1.5",
      "steps": [
        {
          "type": "exec",
          "sql": "UPDATE t1 SET x=NULL WHERE x=2;",
          "expect": "do_execsql_test 1.6 { select * from vt1 } {0 {}}"
        }
      ]
    },
    {
      "name": "1.7",
      "steps": [
        {
          "type": "query",
          "sql": "select * from vt1 WHERE a=0",
          "expect": "{0}"
        }
      ]
    },
    {
      "name": "1.8",
      "steps": [
        {
          "type": "query",
          "sql": "select * from vt1 WHERE a=1",
          "expect": "{}"
        }
      ]
    },
    {
      "name": "1.9",
      "steps": [
        {
          "type": "query",
          "sql": "select * from vt1 WHERE a=1 OR a=0",
          "expect": "{0}"
        }
      ]
    },
    {
      "name": "1.10",
      "steps": [
        {
          "type": "query",
          "sql": "select * from vt1 WHERE a IN (2)",
          "expect": "{}"
        }
      ]
    },
    {
      "name": "1.10",
      "steps": [
        {
          "type": "query",
          "sql": "select * from vt1 WHERE a IN (0,1,2,3)",
          "expect": "{0}"
        }
      ]
    },
    {
      "name": "1.11",
      "steps": [
        {
          "type": "query",
          "sql": "select * from vt1 WHERE a IN (0, NULL)",
          "expect": "{0}"
        }
      ]
    }
  ]
}