{
  "file": "corruptB.test",
  "name": "corruptB",
  "tests": [
    {
      "name": "corruptB-1.3.1",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT rootpage FROM sqlite_master",
          "expect": "4"
        },
        {
          "type": "exec",
          "sql": "SELECT rootpage FROM sqlite_master"
        }
      ]
    },
    {
      "name": "corruptB-1.3.2",
      "steps": [
        {
          "type": "exec",
          "sql": "SELECT * FROM t1",
          "expect": "1 {database disk image is malformed}"
        }
      ]
    },
    {
      "name": "corruptB-1.4.2",
      "steps": [
        {
          "type": "exec",
          "sql": "SELECT * FROM t1",
          "expect": "1 {database disk image is malformed}"
        }
      ]
    },
    {
      "name": "corruptB-1.6.2",
      "steps": [
        {
          "type": "exec",
          "sql": "SELECT * FROM t1",
          "expect": "1 {database disk image is malformed}"
        }
      ]
    },
    {
      "name": "corruptB-1.7.2",
      "steps": [
        {
          "type": "exec",
          "sql": "SELECT * FROM t1",
          "expect": "1 {database disk image is malformed}"
        }
      ]
    },
    {
      "name": "corruptB-1.8.2",
      "steps": [
        {
          "type": "exec",
          "sql": "SELECT * FROM t1",
          "expect": "1 {database disk image is malformed}"
        }
      ]
    },
    {
      "name": "corruptB-1.9.2",
      "steps": [
        {
          "type": "exec",
          "sql": "SELECT * FROM t1",
          "expect": "1 {database disk image is malformed}"
        }
      ]
    },
    {
      "name": "corruptB-2.1.2",
      "steps": [
        {
          "type": "exec",
          "sql": "SELECT * FROM t1",
          "expect": "1 {database disk image is malformed}"
        },
        {
          "type": "exec",
          "sql": "SELECT rootpage FROM sqlite_master WHERE name = 't2'"
        }
      ]
    },
    {
      "name": "corruptB-3.1.2",
      "steps": [
        {
          "type": "exec",
          "sql": "SELECT * FROM t2",
          "expect": "1 {database disk image is malformed}"
        }
      ]
    }
  ]
}