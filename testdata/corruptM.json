{
  "file": "corruptM.test",
  "name": "corruptM",
  "tests": [
    {
      "name": "corruptM-100",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE TABLE t1(a,b,c);\n  INSERT INTO t1 VALUES(111,222,333);\n  CREATE INDEX i1 ON t1(b);\n  CREATE VIEW v2 AS SELECT 15,22;\n  CREATE TRIGGER r1 AFTER INSERT ON t1 BEGIN SELECT 5; END;\n  SELECT type, name, tbl_name, '|' FROM sqlite_master;",
          "expect": "{table t1 t1 | index i1 t1 | view v2 v2 | trigger r1 t1 |}"
        }
      ]
    },
    {
      "name": "corruptM-101",
      "steps": [
        {
          "type": "query",
          "sql": "PRAGMA writable_schema=on;\n  UPDATE sqlite_master SET tbl_name=NULL WHERE name='t1';\n  SELECT type, name, tbl_name, '|' FROM sqlite_master;",
          "expect": "{table t1 {} | index i1 t1 | view v2 v2 | trigger r1 t1 |}"
        }
      ]
    },
    {
      "name": "corruptM-110",
      "steps": [
        {
          "type": "exec",
          "sql": "UPDATE sqlite_master SET tbl_name='tx' WHERE name='t1';\n  SELECT type, name, tbl_name, '|' FROM sqlite_master;",
          "expect": "{table t1 tx | index i1 t1 | view v2 v2 | trigger r1 t1 |}"
        }
      ]
    },
    {
      "name": "corruptM-112",
      "steps": [
        {
          "type": "exec",
          "sql": "UPDATE sqlite_master SET tbl_name='t1', type='tabl' WHERE name='t1';\n  SELECT type, name, tbl_name, '|' FROM sqlite_master;",
          "expect": "{tabl t1 t1 | index i1 t1 | view v2 v2 | trigger r1 t1 |}"
        }
      ]
    },
    {
      "name": "corruptM-114",
      "steps": [
        {
          "type": "exec",
          "sql": "UPDATE sqlite_master SET tbl_name='t9',type='table',name='t9'WHERE name='t1';\n  SELECT type, name, tbl_name, '|' FROM sqlite_master;",
          "expect": "{table t9 t9 | index i1 t1 | view v2 v2 | trigger r1 t1 |}"
        }
      ]
    },
    {
      "name": "corruptM-120",
      "steps": [
        {
          "type": "exec",
          "sql": "UPDATE sqlite_master SET name='t1',tbl_name='T1' WHERE name='t9';\n  SELECT type, name, tbl_name, '|' FROM sqlite_master;",
          "expect": "{table t1 T1 | index i1 t1 | view v2 v2 | trigger r1 t1 |}"
        }
      ]
    },
    {
      "name": "corruptM-130",
      "steps": [
        {
          "type": "exec",
          "sql": "UPDATE sqlite_master SET type='view' WHERE name='t1';\n  SELECT type, name, tbl_name, '|' FROM sqlite_master;",
          "expect": "{view t1 T1 | index i1 t1 | view v2 v2 | trigger r1 t1 |}"
        }
      ]
    },
    {
      "name": "corruptM-140",
      "steps": [
        {
          "type": "exec",
          "sql": "UPDATE sqlite_master SET type='table', tbl_name='t1' WHERE name='t1';\n  UPDATE sqlite_master SET tbl_name='tx' WHERE name='i1';\n  SELECT type, name, tbl_name, '|' FROM sqlite_master;",
          "expect": "{table t1 t1 | index i1 tx | view v2 v2 | trigger r1 t1 |}"
        }
      ]
    },
    {
      "name": "corruptM-150",
      "steps": [
        {
          "type": "exec",
          "sql": "UPDATE sqlite_master SET type='table', tbl_name='t1' WHERE name='i1';\n  SELECT type, name, tbl_name, '|' FROM sqlite_master;",
          "expect": "{table t1 t1 | table i1 t1 | view v2 v2 | trigger r1 t1 |}"
        }
      ]
    },
    {
      "name": "corruptM-160",
      "steps": [
        {
          "type": "exec",
          "sql": "UPDATE sqlite_master SET type='view', tbl_name='t1' WHERE name='i1';\n  SELECT type, name, tbl_name, '|' FROM sqlite_master;",
          "expect": "{table t1 t1 | view i1 t1 | view v2 v2 | trigger r1 t1 |}"
        }
      ]
    },
    {
      "name": "corruptM-170",
      "steps": [
        {
          "type": "exec",
          "sql": "UPDATE sqlite_master SET type='index', tbl_name='t1' WHERE name='i1';\n  UPDATE sqlite_master SET type='table', tbl_name='v2' WHERE name='v2';\n  SELECT type, name, tbl_name, '|' FROM sqlite_master;",
          "expect": "{table t1 t1 | index i1 t1 | table v2 v2 | trigger r1 t1 |}"
        }
      ]
    },
    {
      "name": "corruptM-180",
      "steps": [
        {
          "type": "exec",
          "sql": "UPDATE sqlite_master SET type='view',name='v3',tbl_name='v3' WHERE name='v2';\n  SELECT type, name, tbl_name, '|' FROM sqlite_master;",
          "expect": "{table t1 t1 | index i1 t1 | view v3 v3 | trigger r1 t1 |}"
        }
      ]
    },
    {
      "name": "corruptM-190",
      "steps": [
        {
          "type": "exec",
          "sql": "UPDATE sqlite_master SET type='view',name='v2',tbl_name='v2' WHERE name='v3';\n  UPDATE sqlite_master SET type='view' WHERE name='r1';\n  SELECT type, name, tbl_name, '|' FROM sqlite_master;",
          "expect": "{table t1 t1 | index i1 t1 | view v2 v2 | view r1 t1 |}"
        }
      ]
    },
    {
      "name": "corruptM-192",
      "steps": [
        {
          "type": "exec",
          "sql": "UPDATE sqlite_master SET type='trigger',tbl_name='v2' WHERE name='r1';\n  SELECT type, name, tbl_name, '|' FROM sqlite_master;",
          "expect": "{table t1 t1 | index i1 t1 | view v2 v2 | trigger r1 v2 |}"
        }
      ]
    }
  ]
}