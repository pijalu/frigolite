{
  "file": "exec.test",
  "name": "exec",
  "tests": [
    {
      "name": "exec-1.1",
      "steps": [
        {
          "type": "query",
          "sql": "CREATE TABLE t1(a,b);\n    INSERT INTO t1 VALUES(1,2);\n    SELECT * FROM t1;",
          "expect": "1 2"
        },
        {
          "type": "exec",
          "sql": "CREATE TABLE t1(a,b);\n    INSERT INTO t1 VALUES(1,2);\n    SELECT * FROM t1;"
        }
      ]
    }
  ],
  "source": "capi"
}