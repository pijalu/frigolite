{
  "file": "fts3al.test",
  "name": "fts3al",
  "tests": [
    {
      "name": "fts3al-1.4",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT rowid, length(snippet(t4)) FROM t4 WHERE t4 MATCH 'target'",
          "expect": "1 241 2 247"
        },
        {
          "type": "exec",
          "sql": "SELECT rowid, length(snippet(t4)) FROM t4 WHERE t4 MATCH 'target'"
        },
        {
          "type": "exec",
          "sql": "CREATE VIRTUAL TABLE t4 USING fts3(content)"
        }
      ]
    }
  ]
}