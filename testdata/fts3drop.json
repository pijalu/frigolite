{
  "file": "fts3drop.test",
  "name": "fts3drop",
  "tests": [
    {
      "name": "1.3",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT * FROM f1;",
          "expect": "{a b c} {d e f}"
        }
      ]
    },
    {
      "name": "1.5",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT * FROM f1;",
          "expect": "{a b c} {d e f}"
        }
      ]
    }
  ]
}