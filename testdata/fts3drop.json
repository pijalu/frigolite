{
  "file": "fts3drop.test",
  "name": "fts3drop",
  "tests": [
    {
      "name": "1.1",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE VIRTUAL TABLE f1 USING fts3;\n  INSERT INTO f1 VALUES('a b c');"
        }
      ]
    },
    {
      "name": "1.2",
      "steps": [
        {
          "type": "exec",
          "sql": "BEGIN;\n    INSERT INTO f1 VALUES('d e f');\n    SAVEPOINT one;\n      INSERT INTO f1 VALUES('g h i');\n      DROP TABLE f1;\n    ROLLBACK TO one;\n  COMMIT;"
        }
      ]
    },
    {
      "name": "1.3",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT * FROM f1;",
          "expect": "{a b c} {d e f}"
        }
      ]
    },
    {
      "name": "1.4",
      "steps": [
        {
          "type": "exec",
          "sql": "BEGIN;\n    INSERT INTO f1 VALUES('g h i');\n    SAVEPOINT one;\n      INSERT INTO f1 VALUES('j k l');\n      DROP TABLE f1;\n    RELEASE one;\n  ROLLBACK;"
        }
      ]
    },
    {
      "name": "1.5",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT * FROM f1;",
          "expect": "{a b c} {d e f}"
        }
      ]
    }
  ]
}