{
  "file": "fts3e.test",
  "name": "fts3e",
  "tests": [
    {
      "name": "fts3e-1.1",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT docid FROM t1 ORDER BY docid;",
          "expect": "1 2 3"
        },
        {
          "type": "exec",
          "sql": "SELECT docid FROM t1 ORDER BY docid;"
        }
      ]
    },
    {
      "name": "fts3e-1.2",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT docid FROM t1 WHERE c LIKE '%test' ORDER BY docid;",
          "expect": "1 2 3"
        },
        {
          "type": "exec",
          "sql": "SELECT docid FROM t1 WHERE c LIKE '%test' ORDER BY docid;"
        }
      ]
    },
    {
      "name": "fts3e-1.3",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT docid FROM t1 WHERE c LIKE 'That%' ORDER BY docid;",
          "expect": "2"
        },
        {
          "type": "exec",
          "sql": "SELECT docid FROM t1 WHERE c LIKE 'That%' ORDER BY docid;"
        }
      ]
    },
    {
      "name": "fts3e-2.1",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT docid FROM t1 WHERE docid in (1, 2, 10);\n    SELECT rowid FROM t1 WHERE rowid in (1, 2, 10);",
          "expect": "1 2 1 2"
        },
        {
          "type": "exec",
          "sql": "SELECT docid FROM t1 WHERE docid in (1, 2, 10);\n    SELECT rowid FROM t1 WHERE rowid in (1, 2, 10);"
        }
      ]
    },
    {
      "name": "fts3e-2.2",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT docid, weight FROM t1, t2 WHERE t2.id = t1.docid ORDER BY weight;\n    SELECT t1.rowid, weight FROM t1, t2 WHERE t2.id = t1.rowid ORDER BY weight;",
          "expect": "2 5 1 10 3 20 2 5 1 10 3 20"
        },
        {
          "type": "exec",
          "sql": "SELECT docid, weight FROM t1, t2 WHERE t2.id = t1.docid ORDER BY weight;\n    SELECT t1.rowid, weight FROM t1, t2 WHERE t2.id = t1.rowid ORDER BY weight;"
        }
      ]
    },
    {
      "name": "fts3e-2.3",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT docid, weight FROM t1, t2\n           WHERE t2.weight>5 AND t2.id = t1.docid ORDER BY weight;\n    SELECT t1.rowid, weight FROM t1, t2\n           WHERE t2.weight>5 AND t2.id = t1.rowid ORDER BY weight;",
          "expect": "1 10 3 20 1 10 3 20"
        },
        {
          "type": "exec",
          "sql": "SELECT docid, weight FROM t1, t2\n           WHERE t2.weight>5 AND t2.id = t1.docid ORDER BY weight;\n    SELECT t1.rowid, weight FROM t1, t2\n           WHERE t2.weight>5 AND t2.id = t1.rowid ORDER BY weight;"
        }
      ]
    }
  ]
}