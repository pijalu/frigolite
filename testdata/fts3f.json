{
  "file": "fts3f.test",
  "name": "fts3f",
  "tests": [
    {
      "name": "1.0",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE VIRTUAL TABLE ft USING fts3(x);\n  BEGIN;\n    INSERT INTO ft VALUES('a one'), ('b one'), ('c one');"
        }
      ]
    },
    {
      "name": "1.2",
      "steps": [
        {
          "type": "exec",
          "sql": "BEGIN;\n    INSERT INTO ft VALUES('a one'), ('b one'), ('c one');"
        }
      ]
    },
    {
      "name": "1.3",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT docid, optimize(ft) FROM ft WHERE ft MATCH 'one'",
          "expect": "1 {Index optimized} 2 {Index already optimal} 3 {Index already optimal}\n  4 {Index already optimal}\n  5 {Index already optimal} 6 {Index already optimal}"
        },
        {
          "type": "exec",
          "sql": "SELECT docid FROM ft WHERE ft MATCH 'one'"
        }
      ]
    }
  ]
}