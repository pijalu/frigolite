{
  "file": "fts3rank.test",
  "name": "fts3rank",
  "tests": [
    {
      "name": "1.0",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE VIRTUAL TABLE t1 USING fts3(a, b);\n  INSERT INTO t1 VALUES('one two', 'one');\n  INSERT INTO t1 VALUES('one two', 'three');\n  INSERT INTO t1 VALUES('one two', 'two');"
        }
      ]
    },
    {
      "name": "1.1",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT * FROM t1 WHERE t1 MATCH 'one' \n  ORDER BY rank(matchinfo(t1), 1.0, 1.0) DESC, rowid",
          "expect": "{one two} one\n  {one two} three\n  {one two} two"
        }
      ]
    },
    {
      "name": "1.2",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT * FROM t1 WHERE t1 MATCH 'two' \n  ORDER BY rank(matchinfo(t1), 1.0, 1.0) DESC, rowid",
          "expect": "{one two} two\n  {one two} one\n  {one two} three"
        }
      ]
    },
    {
      "name": "1.3",
      "steps": [
        {
          "type": "exec",
          "sql": "SELECT * FROM t1 ORDER BY rank(matchinfo(t1), 1.0, 1.0) DESC, rowid",
          "expect": "1 {invalid matchinfo blob passed to function rank()}"
        }
      ]
    },
    {
      "name": "1.4",
      "steps": [
        {
          "type": "exec",
          "sql": "SELECT * FROM t1 ORDER BY rank(x'0000000000000000') DESC, rowid",
          "expect": "0 {{one two} one {one two} three {one two} two}"
        }
      ]
    },
    {
      "name": "1.5le",
      "steps": [
        {
          "type": "exec",
          "sql": "SELECT * FROM t1 ORDER BY rank(x'0100000001000000') DESC, rowid",
          "expect": "1 {invalid matchinfo blob passed to function rank()}"
        }
      ]
    },
    {
      "name": "1.5be",
      "steps": [
        {
          "type": "exec",
          "sql": "SELECT * FROM t1 ORDER BY rank(x'0000000100000001') DESC, rowid",
          "expect": "1 {invalid matchinfo blob passed to function rank()}"
        }
      ]
    }
  ]
}