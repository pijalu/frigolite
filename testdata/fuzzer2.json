{
  "file": "fuzzer2.test",
  "name": "fuzzer2",
  "tests": [
    {
      "name": "1.1.1",
      "steps": [
        {
          "type": "exec",
          "sql": "DROP TABLE IF EXISTS x1;\n  DROP TABLE IF EXISTS x1_rules;\n  CREATE TABLE x1_rules(ruleset, cFrom, cTo, cost);"
        },
        {
          "type": "exec",
          "sql": "UPDATE x1_rules SET cost = 20 WHERE cost<20 AND cFrom!='xx'"
        }
      ]
    },
    {
      "name": "1.2",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT count(*) FROM x1_rules WHERE cTo!=cFrom;"
        }
      ]
    },
    {
      "name": "1.2.2",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT cTo FROM x1_rules WHERE cFrom='xx' \n  ORDER BY cost asc, rowid asc LIMIT 9;",
          "expect": "hw hx hy hz ia ib ic id ie"
        }
      ]
    }
  ]
}