{
  "file": "index2.test",
  "name": "index2",
  "tests": [
    {
      "name": "index2-1.3",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT c123 FROM t1"
        },
        {
          "type": "exec",
          "sql": "SELECT c123 FROM t1"
        }
      ]
    },
    {
      "name": "index2-1.4",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT count(*) FROM t1"
        },
        {
          "type": "exec",
          "sql": "SELECT count(*) FROM t1"
        }
      ]
    },
    {
      "name": "index2-1.5",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT round(sum(c1000)) FROM t1",
          "expect": "50601000.0"
        },
        {
          "type": "exec",
          "sql": "SELECT round(sum(c1000)) FROM t1"
        }
      ]
    },
    {
      "name": "index2-2.2",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN SELECT c9 FROM t1 ORDER BY c1, c2, c3, c4, c5",
          "expect": "9 10009 20009 30009 40009"
        },
        {
          "type": "exec",
          "sql": "EXPLAIN SELECT c9 FROM t1 ORDER BY c1, c2, c3, c4, c5"
        },
        {
          "type": "exec",
          "sql": "SELECT c9 FROM t1 ORDER BY c1, c2, c3, c4, c5, c6 LIMIT 5"
        }
      ]
    }
  ]
}