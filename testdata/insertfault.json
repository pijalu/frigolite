{
  "file": "insertfault.test",
  "name": "insertfault",
  "tests": [
    {
      "name": "1.0",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE TABLE t1(a INTEGER PRIMARY KEY, b, c, d DEFAULT true);\n  INSERT INTO t1 DEFAULT VALUES;\n  SELECT * FROM t1;",
          "expect": "{1 {} {} 1}"
        },
        {
          "type": "exec",
          "sql": "SELECT * FROM sqlite_master"
        },
        {
          "type": "exec",
          "sql": "SELECT * FROM t1"
        }
      ]
    }
  ]
}