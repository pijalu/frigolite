{
  "file": "interrupt2.test",
  "name": "interrupt2",
  "tests": [
    {
      "name": "1.$idelay.4",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT count(*) FROM t1",
          "expect": "1000"
        }
      ]
    },
    {
      "name": "2.0",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE TEMP TABLE z1(a, b);\n  INSERT INTO z1 SELECT * FROM t1;"
        },
        {
          "type": "exec",
          "sql": "SELECT * FROM z1"
        },
        {
          "type": "exec",
          "sql": "SELECT * FROM sqlite_master"
        },
        {
          "type": "exec",
          "sql": "SELECT * FROM sqlite_master"
        },
        {
          "type": "exec",
          "sql": "CREATE TABLE t2(x, y)"
        },
        {
          "type": "exec",
          "sql": "CREATE TABLE t3(x, y)"
        },
        {
          "type": "exec",
          "sql": "CREATE TABLE t4(x, y)"
        }
      ]
    }
  ]
}