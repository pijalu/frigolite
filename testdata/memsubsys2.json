{
  "file": "memsubsys2.test",
  "name": "memsubsys2",
  "tests": [
    {
      "name": "setup_0",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE TABLE t1(x, y);\n    CREATE TABLE t2(a, b);\n    CREATE INDEX i1 ON t1(x,y);\n    INSERT INTO t1 VALUES(1, 100);\n    INSERT INTO t1 VALUES(2, 200);"
        },
        {
          "type": "exec",
          "sql": "INSERT INTO t2 SELECT * FROM t1"
        },
        {
          "type": "exec",
          "sql": "DELETE FROM t2"
        },
        {
          "type": "exec",
          "sql": "SELECT count(*) FROM t1"
        }
      ]
    }
  ],
  "source": "capi"
}