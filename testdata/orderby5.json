{
  "file": "orderby5.test",
  "name": "orderby5",
  "tests": [
    {
      "name": "1.1",
      "steps": [
        {
          "type": "query",
          "sql": "CREATE TABLE t1(a,b,c);\n  CREATE INDEX t1bc ON t1(b,c);\n\n  EXPLAIN QUERY PLAN\n  SELECT DISTINCT a, b, c FROM t1 WHERE a=0;",
          "expect": "~/B-TREE/"
        }
      ]
    },
    {
      "name": "1.2.1",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT DISTINCT a, c, b FROM t1 WHERE a=0;",
          "expect": "~/B-TREE/"
        }
      ]
    },
    {
      "name": "1.2.2",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT DISTINCT a, c, b FROM t1 WHERE a='xyz' COLLATE nocase;",
          "expect": "/B-TREE/"
        }
      ]
    },
    {
      "name": "1.2.3",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT DISTINCT a COLLATE nocase, c, b FROM t1 WHERE a='xyz';",
          "expect": "/B-TREE/"
        }
      ]
    },
    {
      "name": "1.2.4",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT DISTINCT a COLLATE nocase, c, b FROM t1 WHERE a='xyz' COLLATE nocase;",
          "expect": "~/B-TREE/"
        }
      ]
    },
    {
      "name": "1.3",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT DISTINCT b, a, c FROM t1 WHERE a=0;",
          "expect": "~/B-TREE/"
        }
      ]
    },
    {
      "name": "1.4",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT DISTINCT b, c, a FROM t1 WHERE a=0;",
          "expect": "~/B-TREE/"
        }
      ]
    },
    {
      "name": "1.5",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT DISTINCT c, a, b FROM t1 WHERE a=0;",
          "expect": "~/B-TREE/"
        }
      ]
    },
    {
      "name": "1.6",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT DISTINCT c, b, a FROM t1 WHERE a=0;",
          "expect": "~/B-TREE/"
        }
      ]
    },
    {
      "name": "1.7",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT DISTINCT c, b, a FROM t1 WHERE +a=0;",
          "expect": "/B-TREE/"
        }
      ]
    },
    {
      "name": "2.1a",
      "steps": [
        {
          "type": "query",
          "sql": "CREATE TABLE t2(a,b,c);\n  CREATE INDEX t2bc ON t2(b,c);\n  ANALYZE;\n  INSERT INTO sqlite_stat1 VALUES('t1','t1bc','1000000 10 9');\n  INSERT INTO sqlite_stat1 VALUES('t2','t2bc','100 10 5');\n  ANALYZE sqlite_master;\n\n  EXPLAIN QUERY PLAN\n  SELECT * FROM t2 WHERE a=0 ORDER BY a, b, c;",
          "expect": "~/B-TREE/"
        }
      ]
    },
    {
      "name": "2.1b",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT * FROM t1 WHERE likelihood(a=0, 0.03) ORDER BY a, b, c;",
          "expect": "/B-TREE/"
        }
      ]
    },
    {
      "name": "2.2",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT * FROM t1 WHERE +a=0 ORDER BY a, b, c;",
          "expect": "/B-TREE/"
        }
      ]
    },
    {
      "name": "2.3",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT * FROM t1 WHERE a=0 ORDER BY b, a, c;",
          "expect": "~/B-TREE/"
        }
      ]
    },
    {
      "name": "2.4",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT * FROM t1 WHERE a=0 ORDER BY b, c, a;",
          "expect": "~/B-TREE/"
        }
      ]
    },
    {
      "name": "2.5",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT * FROM t1 WHERE a=0 ORDER BY a, c, b;",
          "expect": "/B-TREE/"
        }
      ]
    },
    {
      "name": "2.6",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT * FROM t1 WHERE a=0 ORDER BY c, a, b;",
          "expect": "/B-TREE/"
        }
      ]
    },
    {
      "name": "2.7",
      "steps": [
        {
          "type": "query",
          "sql": "EXPLAIN QUERY PLAN\n  SELECT * FROM t1 WHERE a=0 ORDER BY c, b, a;",
          "expect": "/B-TREE/"
        }
      ]
    },
    {
      "name": "3.0",
      "steps": [
        {
          "type": "query",
          "sql": "CREATE TABLE t3(a INTEGER PRIMARY KEY, b, c, d, e, f);\n  CREATE INDEX t3bcde ON t3(b, c, d, e);\n  EXPLAIN QUERY PLAN\n  SELECT a FROM t3 WHERE b=2 AND c=3 ORDER BY d DESC, e DESC, b, c, a DESC;",
          "expect": "~/B-TREE/"
        }
      ]
    },
    {
      "name": "3.1",
      "steps": [
        {
          "type": "query",
          "sql": "DROP TABLE t3;\n  CREATE TABLE t3(a INTEGER PRIMARY KEY, b, c, d, e, f) WITHOUT rowid;\n  CREATE INDEX t3bcde ON t3(b, c, d, e);\n  EXPLAIN QUERY PLAN\n  SELECT a FROM t3 WHERE b=2 AND c=3 ORDER BY d DESC, e DESC, b, c, a DESC;",
          "expect": "~/B-TREE/"
        }
      ]
    },
    {
      "name": "4.1.0",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE TABLE t4(b COLLATE nocase);\n  INSERT INTO t4 VALUES('abc');\n  INSERT INTO t4 VALUES('ABC');\n  INSERT INTO t4 VALUES('aBC');"
        }
      ]
    },
    {
      "name": "4.1.1",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT * FROM t4 ORDER BY b COLLATE binary",
          "expect": "ABC aBC abc"
        }
      ]
    },
    {
      "name": "4.1.2",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT * FROM t4 WHERE b='abc' ORDER BY b COLLATE binary",
          "expect": "ABC aBC abc"
        }
      ]
    },
    {
      "name": "4.2.1",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE TABLE Records(typeID INTEGER, key TEXT COLLATE nocase, value TEXT);\n  CREATE INDEX RecordsIndex ON Records(typeID, key, value);"
        }
      ]
    },
    {
      "name": "4.2.2",
      "steps": [
        {
          "type": "query",
          "sql": "explain query plan\n  SELECT typeID, key, value FROM Records \n  WHERE typeID = 2 AND key = 'x' \n  ORDER BY key, value;",
          "expect": "~/TEMP B-TREE/"
        }
      ]
    },
    {
      "name": "4.2.3",
      "steps": [
        {
          "type": "query",
          "sql": "explain query plan\n  SELECT typeID, key, value FROM Records \n  WHERE typeID = 2 AND (key = 'x' COLLATE binary)\n  ORDER BY key, value;",
          "expect": "~/TEMP B-TREE/"
        }
      ]
    },
    {
      "name": "4.2.4",
      "steps": [
        {
          "type": "query",
          "sql": "explain query plan\n  SELECT typeID, key, value FROM Records \n  WHERE typeID = 2 \n  ORDER BY key, value;",
          "expect": "~/TEMP B-TREE/"
        }
      ]
    },
    {
      "name": "4.3.1",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE TABLE t5(a INTEGER PRIMARY KEY, b COLLATE hello, c, d);"
        }
      ]
    },
    {
      "name": "__RESET_DB__",
      "steps": [
        {
          "type": "reset_db"
        }
      ]
    },
    {
      "name": "4.3.2",
      "steps": [
        {
          "type": "exec",
          "sql": "SELECT a FROM t5 WHERE b='def' ORDER BY b;",
          "expect": "1 {no such collation sequence: hello}"
        }
      ]
    },
    {
      "name": "4.4.0",
      "steps": [
        {
          "type": "query",
          "sql": "DROP TABLE t1;\n  CREATE TABLE t1(a);\n  DROP TABLE t2;\n  CREATE TABLE t2(b INTEGER PRIMARY KEY, c INT);\n  SELECT DISTINCT *\n    FROM t1 LEFT JOIN t2 ON b=c AND b=(SELECT a FROM t1)\n   WHERE c>10;"
        }
      ]
    }
  ]
}