{
  "file": "speed1p.test",
  "name": "speed1p",
  "tests": [
    {
      "name": "speed1p-1.0",
      "steps": [
        {
          "type": "exec",
          "sql": "PRAGMA page_size=1024;\n    PRAGMA cache_size=500;\n    PRAGMA locking_mode=EXCLUSIVE;\n    CREATE TABLE t1(a INTEGER, b INTEGER, c TEXT);\n    CREATE TABLE t2(a INTEGER, b INTEGER, c TEXT);\n    CREATE INDEX i2a ON t2(a);\n    CREATE INDEX i2b ON t2(b);"
        },
        {
          "type": "exec",
          "sql": "PRAGMA page_size=1024;\n    PRAGMA cache_size=500;\n    PRAGMA locking_mode=EXCLUSIVE;\n    CREATE TABLE t1(a INTEGER, b INTEGER, c TEXT);\n    CREATE TABLE t2(a INTEGER, b INTEGER, c TEXT);\n    CREATE INDEX i2a ON t2(a);\n    CREATE INDEX i2b ON t2(b);"
        },
        {
          "type": "exec",
          "sql": "SELECT name FROM sqlite_master ORDER BY 1;"
        },
        {
          "type": "exec",
          "sql": "SELECT c FROM t1 ORDER BY random() LIMIT 50000"
        }
      ]
    },
    {
      "name": "__RESET_DB__",
      "steps": [
        {
          "type": "reset_db"
        }
      ]
    }
  ],
  "source": "capi"
}