{
  "file": "swarmvtab2.test",
  "name": "swarmvtab2",
  "tests": [
    {
      "name": "100",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE TABLE t1(filename, tablename, istart, iend);\n  WITH RECURSIVE c(x) AS (VALUES(1) UNION ALL SELECT x+1 FROM c WHERE x<99)\n  INSERT INTO t1 SELECT printf('test%03d.db',x),'t2',x*1000,x*1000+999 FROM c;\n  CREATE VIRTUAL TABLE temp.v1 USING swarmvtab(\n    'SELECT * FROM t1', 'create_database'\n  );"
        }
      ]
    },
    {
      "name": "110",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT b FROM v1 WHERE a=3875;",
          "expect": "**03875**"
        }
      ]
    },
    {
      "name": "130",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT b FROM v1 WHERE a BETWEEN 3999 AND 4000 ORDER BY a;",
          "expect": "**03999** **04000**"
        }
      ]
    },
    {
      "name": "150",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT b FROM v1 WHERE a>=99998;",
          "expect": "**99998** **99999**"
        }
      ]
    }
  ]
}