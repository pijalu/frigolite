{
  "file": "tkt2767.test",
  "name": "tkt2767",
  "tests": [
    {
      "name": "tkt2767-1.2",
      "steps": [
        {
          "type": "query",
          "sql": "DELETE FROM t1 WHERE x>0;\n    SELECT count(*), sum(x) FROM t1;",
          "expect": "32 528"
        },
        {
          "type": "exec",
          "sql": "DELETE FROM t1 WHERE x>0;\n    SELECT count(*), sum(x) FROM t1;"
        }
      ]
    },
    {
      "name": "tkt2767-1.3",
      "steps": [
        {
          "type": "query",
          "sql": "UPDATE t1 SET x=x+1;\n    SELECT count(*), sum(x) FROM t1;",
          "expect": "32 528"
        },
        {
          "type": "exec",
          "sql": "UPDATE t1 SET x=x+1;\n    SELECT count(*), sum(x) FROM t1;"
        }
      ]
    },
    {
      "name": "tkt2767-1.4",
      "steps": [
        {
          "type": "query",
          "sql": "INSERT INTO t1 SELECT x+32 FROM t1;\n    SELECT count(*), sum(x) FROM t1;",
          "expect": "32 528"
        },
        {
          "type": "exec",
          "sql": "INSERT INTO t1 SELECT x+32 FROM t1;\n    SELECT count(*), sum(x) FROM t1;"
        }
      ]
    }
  ]
}