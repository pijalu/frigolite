{
  "file": "tkt3442.test",
  "name": "tkt3442",
  "tests": [
    {
      "name": "tkt3442-1.1",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE TABLE listhash(\n       key INTEGER PRIMARY KEY,\n       id TEXT,\n       node INTEGER\n     );\n     CREATE UNIQUE INDEX ididx ON listhash(id);"
        }
      ]
    },
    {
      "name": "tkt3442-1.5",
      "steps": [
        {
          "type": "exec",
          "sql": "SELECT node FROM listhash WHERE id=[5000] LIMIT 1;",
          "expect": "1 {no such column: 5000}"
        },
        {
          "type": "exec",
          "sql": "CREATE TABLE listhash(\n       key INTEGER PRIMARY KEY,\n       id TEXT,\n       node INTEGER\n     );\n     CREATE UNIQUE INDEX ididx ON listhash(id);"
        }
      ]
    }
  ],
  "source": "capi"
}