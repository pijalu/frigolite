{
  "file": "tkt3554.test",
  "name": "tkt3554",
  "tests": [
    {
      "name": "tkt3544-1.1",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE TABLE test ( obj, t1, t2, PRIMARY KEY(obj, t1, t2) );\n  \n    CREATE TRIGGER test_insert BEFORE INSERT ON test BEGIN\n      UPDATE test SET t1 = new.t1 \n        WHERE obj = new.obj AND new.t1 < t1 AND new.t2 >= t1;\n  \n      UPDATE test SET t2 = new.t2 \n        WHERE obj = new.obj AND new.t2 > t2 AND new.t1 <= t2;\n  \n      SELECT RAISE(IGNORE) WHERE EXISTS (\n        SELECT obj FROM test \n        WHERE obj = new.obj AND new.t1 >= t1 AND new.t2 <= t2\n      );\n    END;"
        }
      ]
    },
    {
      "name": "tkt3544-1.2",
      "steps": [
        {
          "type": "query",
          "sql": "INSERT INTO test VALUES('a', 10000, 11000);\n    SELECT * FROM test;",
          "expect": "a 10000 11000"
        }
      ]
    },
    {
      "name": "tkt3544-1.3",
      "steps": [
        {
          "type": "exec",
          "sql": "INSERT INTO test VALUES('a', 9000, 10500);"
        }
      ]
    },
    {
      "name": "tkt3544-1.4",
      "steps": [
        {
          "type": "exec",
          "sql": "INSERT INTO test VALUES('a', 10000, 12000);"
        },
        {
          "type": "exec",
          "sql": "CREATE TABLE test ( obj, t1, t2, PRIMARY KEY(obj, t1, t2) );\n  \n    CREATE TRIGGER test_insert BEFORE INSERT ON test BEGIN\n      UPDATE test SET t1 = new.t1 \n        WHERE obj = new.obj AND new.t1 < t1 AND new.t2 >= t1;\n  \n      UPDATE test SET t2 = new.t2 \n        WHERE obj = new.obj AND new.t2 > t2 AND new.t1 <= t2;\n  \n      SELECT RAISE(IGNORE) WHERE EXISTS (\n        SELECT obj FROM test \n        WHERE obj = new.obj AND new.t1 >= t1 AND new.t2 <= t2\n      );\n    END;"
        },
        {
          "type": "exec",
          "sql": "INSERT INTO test VALUES('a', 10000, 11000);\n    SELECT * FROM test;"
        },
        {
          "type": "exec",
          "sql": "INSERT INTO test VALUES('a', 9000, 10500);"
        },
        {
          "type": "exec",
          "sql": "SELECT * FROM test"
        },
        {
          "type": "exec",
          "sql": "INSERT INTO test VALUES('a', 10000, 12000);"
        },
        {
          "type": "exec",
          "sql": "SELECT * FROM test"
        }
      ]
    }
  ]
}