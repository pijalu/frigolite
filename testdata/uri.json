{
  "file": "uri.test",
  "name": "uri",
  "tests": [
    {
      "name": "7.3",
      "steps": [
        {
          "type": "exec",
          "sql": "INSERT INTO t2 VALUES('c', 'd')"
        }
      ]
    },
    {
      "name": "7.4",
      "steps": [
        {
          "type": "exec",
          "sql": "INSERT INTO t1 VALUES(3, 4)",
          "expect": "1 {attempt to write a readonly database}"
        }
      ]
    },
    {
      "name": "5.1.1",
      "steps": [
        {
          "type": "exec",
          "sql": "ATTACH 'file:test.db2?vfs=tvfs2' AS aux;\n      PRAGMA main.journal_mode = PERSIST;\n      PRAGMA aux.journal_mode = PERSIST;\n      CREATE TABLE t1(a, b);\n      CREATE TABLE aux.t2(a, b);\n      PRAGMA main.journal_mode = WAL;\n      PRAGMA aux.journal_mode = WAL;\n      INSERT INTO t1 VALUES('x', 'y');\n      INSERT INTO t2 VALUES('x', 'y');"
        }
      ]
    },
    {
      "name": "7.1",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE TABLE t1(a, b);\n    INSERT INTO t1 VALUES(1, 2);\n    ATTACH 'test.db2' AS aux;\n    CREATE TABLE aux.t2(a, b);\n    INSERT INTO t1 VALUES('a', 'b');"
        }
      ]
    },
    {
      "name": "7.2",
      "steps": [
        {
          "type": "exec",
          "sql": "ATTACH 'file:test.db2?mode=rw' AS aux"
        },
        {
          "type": "exec",
          "sql": "ATTACH 'file:test.db2?vfs=tvfs2' AS aux;\n      PRAGMA main.journal_mode = PERSIST;\n      PRAGMA aux.journal_mode = PERSIST;\n      CREATE TABLE t1(a, b);\n      CREATE TABLE aux.t2(a, b);\n      PRAGMA main.journal_mode = WAL;\n      PRAGMA aux.journal_mode = WAL;\n      INSERT INTO t1 VALUES('x', 'y');\n      INSERT INTO t2 VALUES('x', 'y');"
        },
        {
          "type": "exec",
          "sql": "CREATE TABLE t1(a, b);\n    INSERT INTO t1 VALUES(1, 2);\n    ATTACH 'test.db2' AS aux;\n    CREATE TABLE aux.t2(a, b);\n    INSERT INTO t1 VALUES('a', 'b');"
        },
        {
          "type": "exec",
          "sql": "ATTACH 'file:test.db2?mode=rw' AS aux"
        },
        {
          "type": "exec",
          "sql": "CREATE TABLE t1(a, b)"
        }
      ]
    }
  ],
  "source": "capi"
}