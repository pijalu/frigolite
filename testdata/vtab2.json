{
  "file": "vtab2.test",
  "name": "vtab2",
  "tests": [
    {
      "name": "vtab2-1.1",
      "steps": [
        {
          "type": "query",
          "sql": "CREATE VIRTUAL TABLE schema USING schema;\n    SELECT * FROM schema;"
        }
      ]
    },
    {
      "name": "vtab2-1.2",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT length(tablename) FROM schema GROUP by tablename;",
          "expect": "6"
        }
      ]
    },
    {
      "name": "vtab2-1.3",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT tablename FROM schema GROUP by length(tablename);",
          "expect": "schema"
        }
      ]
    },
    {
      "name": "vtab2-1.4",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT length(tablename) FROM schema GROUP by length(tablename);",
          "expect": "6"
        }
      ]
    },
    {
      "name": "vtab2-2.1",
      "steps": [
        {
          "type": "query",
          "sql": "CREATE VIRTUAL TABLE vars USING tclvar;\n    SELECT name, arrayname, value FROM vars WHERE name='abc';"
        }
      ]
    },
    {
      "name": "vtab2-2.2",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT name, arrayname, value FROM vars WHERE name='A';"
        }
      ]
    },
    {
      "name": "vtab2-2.3",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT name, value FROM vars\n      WHERE name MATCH 'tcl_*' AND arrayname = '' \n      ORDER BY name;"
        }
      ]
    },
    {
      "name": "vtab2-3.1",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT * FROM schema WHERE dflt_value IS NULL LIMIT 1",
          "expect": "main schema 0 database {} 0 {} 0"
        }
      ]
    },
    {
      "name": "vtab2-3.2",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT *, b.rowid\n      FROM schema a LEFT JOIN schema b ON a.dflt_value=b.dflt_value\n     WHERE a.rowid=1",
          "expect": "main schema 0 database {} 0 {} 0 {} {} {} {} {} {} {} {} {}"
        }
      ]
    },
    {
      "name": "vtab2-3.3",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT *, b.rowid\n      FROM schema a LEFT JOIN schema b ON a.dflt_value IS b.dflt_value\n                                      AND a.dflt_value IS NOT NULL\n     WHERE a.rowid=1",
          "expect": "main schema 0 database {} 0 {} 0 {} {} {} {} {} {} {} {} {}"
        }
      ]
    },
    {
      "name": "vtab2-4.1",
      "steps": [
        {
          "type": "exec",
          "sql": "BEGIN TRANSACTION;\n    CREATE TABLE t1(a INTEGER PRIMARY KEY, b, c, UNIQUE(b, c));\n    CREATE TABLE fkey(\n      to_tbl,\n      to_col\n    );\n    INSERT INTO \"fkey\" VALUES('t1',NULL);\n    COMMIT;"
        }
      ]
    },
    {
      "name": "vtab2-4.2",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE VIRTUAL TABLE v_col USING schema"
        }
      ]
    },
    {
      "name": "vtab2-4.3",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT name FROM v_col WHERE tablename = 't1' AND pk",
          "expect": "a"
        }
      ]
    },
    {
      "name": "vtab2-4.4",
      "steps": [
        {
          "type": "exec",
          "sql": "UPDATE fkey \n    SET to_col = (SELECT name FROM v_col WHERE tablename = 't1' AND pk);"
        }
      ]
    },
    {
      "name": "vtab2-4.5",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT * FROM fkey",
          "expect": "t1 a"
        },
        {
          "type": "exec",
          "sql": "CREATE VIRTUAL TABLE schema USING schema;\n    SELECT * FROM schema;"
        },
        {
          "type": "exec",
          "sql": "SELECT length(tablename) FROM schema GROUP by tablename;"
        },
        {
          "type": "exec",
          "sql": "SELECT tablename FROM schema GROUP by length(tablename);"
        },
        {
          "type": "exec",
          "sql": "SELECT length(tablename) FROM schema GROUP by length(tablename);"
        },
        {
          "type": "exec",
          "sql": "CREATE VIRTUAL TABLE vars USING tclvar;\n    SELECT name, arrayname, value FROM vars WHERE name='abc';"
        },
        {
          "type": "exec",
          "sql": "SELECT name, arrayname, value FROM vars WHERE name='A';"
        },
        {
          "type": "exec",
          "sql": "SELECT name, value FROM vars\n      WHERE name MATCH 'tcl_*' AND arrayname = '' \n      ORDER BY name;"
        },
        {
          "type": "exec",
          "sql": "SELECT * FROM schema WHERE dflt_value IS NULL LIMIT 1"
        },
        {
          "type": "exec",
          "sql": "SELECT *, b.rowid\n      FROM schema a LEFT JOIN schema b ON a.dflt_value=b.dflt_value\n     WHERE a.rowid=1"
        },
        {
          "type": "exec",
          "sql": "SELECT *, b.rowid\n      FROM schema a LEFT JOIN schema b ON a.dflt_value IS b.dflt_value\n                                      AND a.dflt_value IS NOT NULL\n     WHERE a.rowid=1"
        },
        {
          "type": "exec",
          "sql": "BEGIN TRANSACTION;\n    CREATE TABLE t1(a INTEGER PRIMARY KEY, b, c, UNIQUE(b, c));\n    CREATE TABLE fkey(\n      to_tbl,\n      to_col\n    );\n    INSERT INTO \"fkey\" VALUES('t1',NULL);\n    COMMIT;"
        },
        {
          "type": "exec",
          "sql": "CREATE VIRTUAL TABLE v_col USING schema"
        },
        {
          "type": "exec",
          "sql": "SELECT name FROM v_col WHERE tablename = 't1' AND pk"
        },
        {
          "type": "exec",
          "sql": "UPDATE fkey \n    SET to_col = (SELECT name FROM v_col WHERE tablename = 't1' AND pk);"
        },
        {
          "type": "exec",
          "sql": "SELECT * FROM fkey"
        }
      ]
    }
  ],
  "source": "capi"
}