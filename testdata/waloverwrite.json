{
  "file": "waloverwrite.test",
  "name": "waloverwrite",
  "tests": [
    {
      "name": "setup_0",
      "steps": [
        {
          "type": "exec",
          "sql": "PRAGMA journal_mode = wal"
        },
        {
          "type": "exec",
          "sql": "PRAGMA cache_size = 5"
        },
        {
          "type": "exec",
          "sql": "SELECT sum(length(y)) FROM t1"
        },
        {
          "type": "exec",
          "sql": "SELECT sum(length(y)) FROM t1"
        },
        {
          "type": "exec",
          "sql": "PRAGMA integrity_check"
        },
        {
          "type": "exec",
          "sql": "PRAGMA wal_checkpoint"
        },
        {
          "type": "exec",
          "sql": "WITH cnt(i) AS (SELECT 1 UNION ALL SELECT i+1 FROM cnt WHERE i<20)\n        INSERT INTO t2 SELECT i, randomblob(800) FROM cnt;"
        },
        {
          "type": "exec",
          "sql": "SAVEPOINT abc"
        },
        {
          "type": "exec",
          "sql": "ROLLBACK TO abc"
        },
        {
          "type": "exec",
          "sql": "SELECT sum(length(y)) FROM t1"
        },
        {
          "type": "exec",
          "sql": "SELECT sum(length(y)) FROM t1"
        },
        {
          "type": "exec",
          "sql": "PRAGMA integrity_check"
        },
        {
          "type": "exec",
          "sql": "SELECT x FROM t1"
        },
        {
          "type": "exec",
          "sql": "SELECT x FROM t1"
        },
        {
          "type": "exec",
          "sql": "SELECT x FROM t1"
        }
      ]
    },
    {
      "name": "__RESET_DB__",
      "steps": [
        {
          "type": "reset_db"
        }
      ]
    }
  ]
}