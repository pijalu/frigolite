{
  "file": "whereF.test",
  "name": "whereF",
  "tests": [
    {
      "name": "__RESET_DB__",
      "steps": [
        {
          "type": "reset_db"
        }
      ]
    },
    {
      "name": "5.0",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE TABLE t1(f1);\n  CREATE TABLE t2(f2);\n  CREATE INDEX t2f ON t2(f2);\n\n  INSERT INTO t1 VALUES(-1);\n  INSERT INTO t1 VALUES(-1);\n  INSERT INTO t1 VALUES(-1);\n  INSERT INTO t1 VALUES(-1);\n\n  WITH w(i) AS (\n    SELECT 1 UNION ALL SELECT i+1 FROM w WHERE i<1000\n  )\n  INSERT INTO t2 SELECT -1 FROM w;"
        }
      ]
    },
    {
      "name": "5.1",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT count(*) FROM t1, t2 WHERE t2.rowid = +t1.rowid",
          "expect": "{4}"
        }
      ]
    },
    {
      "name": "5.3",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT count(*) FROM t1, t2 WHERE (\n    t2.rowid = +t1.rowid OR t2.f2 = t1.f1\n  )",
          "expect": "{4000}"
        }
      ]
    },
    {
      "name": "5.5",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT count(*) FROM t1, t2 WHERE (\n    t2.rowid = +t1.rowid OR (t2.f2 = t1.f1 AND t1.f1!=-1)\n  )",
          "expect": "{4}"
        }
      ]
    }
  ]
}