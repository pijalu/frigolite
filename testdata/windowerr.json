{
  "file": "windowerr.test",
  "name": "windowerr",
  "tests": [
    {
      "name": "1.0",
      "steps": [
        {
          "type": "exec",
          "sql": "DROP TABLE IF EXISTS t1;\n  CREATE TABLE t1(a INTEGER, b INTEGER);\n  INSERT INTO t1 VALUES(1, 1);\n  INSERT INTO t1 VALUES(2, 2);\n  INSERT INTO t1 VALUES(3, 3);\n  INSERT INTO t1 VALUES(4, 4);\n  INSERT INTO t1 VALUES(5, 5);"
        }
      ]
    },
    {
      "name": "1.1",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT a, sum(b) OVER (\n    ORDER BY a ROWS BETWEEN -1 PRECEDING AND 1 FOLLOWING\n  ) FROM t1 ORDER BY 1"
        },
        {
          "type": "exec",
          "sql": "SELECT a, sum(b) OVER (\n    ORDER BY a ROWS BETWEEN -1 PRECEDING AND 1 FOLLOWING\n  ) FROM t1 ORDER BY 1"
        }
      ]
    },
    {
      "name": "1.2",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT a, sum(b) OVER (\n    ORDER BY a ROWS BETWEEN  1 PRECEDING AND -1 FOLLOWING\n  ) FROM t1 ORDER BY 1"
        },
        {
          "type": "exec",
          "sql": "SELECT a, sum(b) OVER (\n    ORDER BY a ROWS BETWEEN  1 PRECEDING AND -1 FOLLOWING\n  ) FROM t1 ORDER BY 1"
        }
      ]
    },
    {
      "name": "1.3",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT a, sum(b) OVER (\n    ORDER BY a RANGE BETWEEN -1 PRECEDING AND 1 FOLLOWING\n  ) FROM t1 ORDER BY 1"
        },
        {
          "type": "exec",
          "sql": "SELECT a, sum(b) OVER (\n    ORDER BY a RANGE BETWEEN -1 PRECEDING AND 1 FOLLOWING\n  ) FROM t1 ORDER BY 1"
        }
      ]
    },
    {
      "name": "1.4",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT a, sum(b) OVER (\n    ORDER BY a RANGE BETWEEN  1 PRECEDING AND -1 FOLLOWING\n  ) FROM t1 ORDER BY 1"
        },
        {
          "type": "exec",
          "sql": "SELECT a, sum(b) OVER (\n    ORDER BY a RANGE BETWEEN  1 PRECEDING AND -1 FOLLOWING\n  ) FROM t1 ORDER BY 1"
        }
      ]
    },
    {
      "name": "1.5",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT a, sum(b) OVER (\n    ORDER BY a GROUPS BETWEEN -1 PRECEDING AND 1 FOLLOWING\n  ) FROM t1 ORDER BY 1"
        },
        {
          "type": "exec",
          "sql": "SELECT a, sum(b) OVER (\n    ORDER BY a GROUPS BETWEEN -1 PRECEDING AND 1 FOLLOWING\n  ) FROM t1 ORDER BY 1"
        }
      ]
    },
    {
      "name": "1.6",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT a, sum(b) OVER (\n    ORDER BY a GROUPS BETWEEN  1 PRECEDING AND -1 FOLLOWING\n  ) FROM t1 ORDER BY 1"
        },
        {
          "type": "exec",
          "sql": "SELECT a, sum(b) OVER (\n    ORDER BY a GROUPS BETWEEN  1 PRECEDING AND -1 FOLLOWING\n  ) FROM t1 ORDER BY 1"
        }
      ]
    },
    {
      "name": "1.7",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT a, sum(b) OVER (\n    ORDER BY a,b RANGE BETWEEN  1 PRECEDING AND 1 FOLLOWING\n  ) FROM t1 ORDER BY 1"
        },
        {
          "type": "exec",
          "sql": "SELECT a, sum(b) OVER (\n    ORDER BY a,b RANGE BETWEEN  1 PRECEDING AND 1 FOLLOWING\n  ) FROM t1 ORDER BY 1"
        }
      ]
    },
    {
      "name": "1.8",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT a, sum(b) OVER (\n    PARTITION BY a RANGE BETWEEN  1 PRECEDING AND 1 FOLLOWING\n  ) FROM t1 ORDER BY 1"
        },
        {
          "type": "exec",
          "sql": "SELECT a, sum(b) OVER (\n    PARTITION BY a RANGE BETWEEN  1 PRECEDING AND 1 FOLLOWING\n  ) FROM t1 ORDER BY 1"
        }
      ]
    },
    {
      "name": "2.1",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT sum( sum(a) OVER () ) FROM t1;"
        },
        {
          "type": "exec",
          "sql": "SELECT sum( sum(a) OVER () ) FROM t1;"
        }
      ]
    },
    {
      "name": "2.2",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT sum(a) OVER () AS xyz FROM t1 ORDER BY sum(xyz);"
        },
        {
          "type": "exec",
          "sql": "SELECT sum(a) OVER () AS xyz FROM t1 ORDER BY sum(xyz);"
        }
      ]
    },
    {
      "name": "3.0",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT sum(a) OVER win FROM t1\n  WINDOW win AS (ROWS BETWEEN 'hello' PRECEDING AND 10 FOLLOWING)"
        },
        {
          "type": "exec",
          "sql": "SELECT sum(a) OVER win FROM t1\n  WINDOW win AS (ROWS BETWEEN 'hello' PRECEDING AND 10 FOLLOWING)"
        }
      ]
    },
    {
      "name": "3.2",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT sum(a) OVER win FROM t1\n  WINDOW win AS (ROWS BETWEEN 10 PRECEDING AND x'ABCD' FOLLOWING)"
        },
        {
          "type": "exec",
          "sql": "SELECT sum(a) OVER win FROM t1\n  WINDOW win AS (ROWS BETWEEN 10 PRECEDING AND x'ABCD' FOLLOWING)"
        }
      ]
    },
    {
      "name": "3.3",
      "steps": [
        {
          "type": "query",
          "sql": "SELECT row_number(a) OVER () FROM t1;"
        },
        {
          "type": "exec",
          "sql": "SELECT row_number(a) OVER () FROM t1;"
        }
      ]
    }
  ]
}