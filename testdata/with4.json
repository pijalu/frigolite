{
  "file": "with4.test",
  "name": "with4",
  "tests": [
    {
      "name": "100",
      "steps": [
        {
          "type": "exec",
          "sql": "ATTACH ':memory:' AS aux;\n  CREATE TABLE main.t1(a,b);\n  CREATE TABLE aux.t2(x,y);\n  INSERT INTO t1 VALUES(1,2);\n  INSERT INTO t2 VALUES(3,4);",
          "expect": "{}"
        }
      ]
    },
    {
      "name": "110",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE VIEW v1 AS SELECT * FROM t1, aux.t2;",
          "expect": "{1 {view v1 cannot reference objects in database aux}}"
        }
      ]
    },
    {
      "name": "120",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE VIEW v2 AS WITH v(m,n) AS (SELECT x,y FROM aux.t2) SELECT * FROM t1, v;",
          "expect": "{1 {view v2 cannot reference objects in database aux}}"
        }
      ]
    },
    {
      "name": "200",
      "steps": [
        {
          "type": "exec",
          "sql": "CREATE TRIGGER r1 AFTER INSERT ON t1 BEGIN\n     WITH v(m,n) AS (SELECT x,y FROM aux.t2) SELECT * FROM t1, v;\n  END;",
          "expect": "{1 {trigger r1 cannot reference objects in database aux}}"
        }
      ]
    }
  ]
}