// Code generated by tcl2go; DO NOT EDIT.
//go:build testgen
// +build testgen

package eqp

import (
"fmt"
"github.com/pijalu/frigolite"
"github.com/pijalu/frigolite/internal/vtab"
"os"
"testing"
)

func Test_eqp(t *testing.T) {
	if err := os.Chdir(t.TempDir()); err != nil { t.Fatal(err) }
	db, err := frigolite.Open("test.db")
	if err != nil {
		t.Fatal(err)
	}
	defer db.Close()

	var _res *frigolite.Result
	var r *frigolite.Result
	var msg string
	var _r string
	var _berr error
	_ = _berr // suppress unused warning
	_ = msg // suppress unused warning
	_ = _res // suppress unused warning
	_ = r    // suppress unused warning
	_ = _r   // suppress unused warning
	tcl_nullvalue = "{}" // default NULL rendering

	var db1 *frigolite.DB
	_ = db1
	var db2 *frigolite.DB
	_ = db2
	var db3 *frigolite.DB
	_ = db3
	var db4 *frigolite.DB
	_ = db4
	var db5 *frigolite.DB
	_ = db5
	var db6 *frigolite.DB
	_ = db6
	var db7 *frigolite.DB
	_ = db7
	var db8 *frigolite.DB
	_ = db8
	var db9 *frigolite.DB
	_ = db9
	var DUMMY string
	_ = DUMMY // prepared-statement tail var

	var testdir string
	_ = testdir // pre-declared from TCL source
	var testprefix string
	_ = testprefix // pre-declared from TCL source
	var boilerplate string
	_ = boilerplate // pre-declared from TCL source
	var fd string
	_ = fd // pre-declared from TCL source
	var argv0 string
	_ = argv0 // pre-declared from TCL source
	var args string
	_ = args // pre-declared from TCL source
	var sql string
	_ = sql // pre-declared from TCL source
	var stmt string
	_ = stmt // pre-declared from TCL source
	var tn string
	_ = tn // pre-declared from TCL source
	var res string
	_ = res // pre-declared from TCL source

	// set testdir: test directory (not used in Go test context)
	vtab.TclVarSet("testprefix", "", "eqp")
	testprefix = "eqp"
	_ = testprefix // suppress unused warning
	// proc definition (not transpiled)
	{ // "1.1"
		_res = db.Exec("\n  CREATE TABLE t1(a INT, b INT, ex TEXT);\n  CREATE INDEX i1 ON t1(a);\n  CREATE INDEX i2 ON t1(b);\n  CREATE TABLE t2(a INT, b INT, ex TEXT);\n  CREATE TABLE t3(a INT, b INT, ex TEXT);\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", _res.Error, "\n  CREATE TABLE t1(a INT, b INT, ex TEXT);\n  CREATE INDEX i1 ON t1(a);\n  CREATE INDEX i2 ON t1(b);\n  CREATE TABLE t2(a INT, b INT, ex TEXT);\n  CREATE TABLE t3(a INT, b INT, ex TEXT);\n")
		}
	}
	{ // "1.2"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t2, t1 WHERE t1.a=1 OR t1.b=2;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t2, t1 WHERE t1.a=1 OR t1.b=2;\n")
		}
	}
	{ // "1.3"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t2 CROSS JOIN t1 WHERE t1.a=1 OR t1.b=2;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t2 CROSS JOIN t1 WHERE t1.a=1 OR t1.b=2;\n")
		}
	}
	{ // "1.3"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT a FROM t1 ORDER BY a\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT a FROM t1 ORDER BY a\n")
		}
	}
	{ // "1.4"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT a FROM t1 ORDER BY +a\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT a FROM t1 ORDER BY +a\n")
		}
	}
	{ // "1.5"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT a FROM t1 WHERE a=4\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT a FROM t1 WHERE a=4\n")
		}
	}
	{ // "1.6"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT DISTINCT count(*) FROM t3 GROUP BY a;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT DISTINCT count(*) FROM t3 GROUP BY a;\n")
		}
	}
	{ // "1.7.1"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t3 JOIN (SELECT 1)\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t3 JOIN (SELECT 1)\n")
		}
	}
	{ // "1.7.2"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t3 JOIN (SELECT 1) AS v1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t3 JOIN (SELECT 1) AS v1\n")
		}
	}
	{ // "1.7.3"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t3 AS xx JOIN (SELECT 1) AS yy\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t3 AS xx JOIN (SELECT 1) AS yy\n")
		}
	}
	{ // "1.8"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t3 JOIN (SELECT 1 UNION SELECT 2)\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t3 JOIN (SELECT 1 UNION SELECT 2)\n")
		}
	}
	{ // "1.9"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t3 JOIN (SELECT 1 EXCEPT SELECT a FROM t3 LIMIT 17) AS abc\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t3 JOIN (SELECT 1 EXCEPT SELECT a FROM t3 LIMIT 17) AS abc\n")
		}
	}
	{ // "1.10"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t3 JOIN (SELECT 1 INTERSECT SELECT a FROM t3 LIMIT 17) AS abc\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t3 JOIN (SELECT 1 INTERSECT SELECT a FROM t3 LIMIT 17) AS abc\n")
		}
	}
	{ // "1.11"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t3 JOIN (SELECT 1 UNION ALL SELECT a FROM t3 LIMIT 17) abc\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t3 JOIN (SELECT 1 UNION ALL SELECT a FROM t3 LIMIT 17) abc\n")
		}
	}
	_res = db.Exec("PRAGMA foreign_keys = OFF")
	for _, _t := range db.Query("SELECT name, type FROM sqlite_master WHERE type IN('table','view')").Rows {
		db.Exec("DROP " + fmt.Sprint(_t[1]) + " " + tclQuoteIdent(fmt.Sprint(_t[0])))
	}
	for _, _t := range db.Query("SELECT name, type FROM temp.sqlite_master WHERE type IN('table','view')").Rows {
		db.Exec("DROP " + fmt.Sprint(_t[1]) + " temp." + tclQuoteIdent(fmt.Sprint(_t[0])))
	}
	for _, _t := range db.Query("PRAGMA database_list").Rows {
		if len(_t) > 1 {
			dbname := fmt.Sprint(_t[1])
			if dbname != "main" && dbname != "temp" {
				for _, _u := range db.Query("SELECT name, type FROM " + dbname + ".sqlite_master WHERE type IN('table','view')").Rows {
					db.Exec("DROP " + fmt.Sprint(_u[1]) + " " + dbname + "." + tclQuoteIdent(fmt.Sprint(_u[0])))
				}
			}
		}
	}
	_res = db.Exec("PRAGMA foreign_keys = ON")
	{ // "2.1"
		_res = db.Exec("\n  CREATE TABLE t1(x INT, y INT, ex TEXT);\n\n  CREATE TABLE t2(x INT, y INT, ex TEXT);\n  CREATE INDEX t2i1 ON t2(x);\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", _res.Error, "\n  CREATE TABLE t1(x INT, y INT, ex TEXT);\n\n  CREATE TABLE t2(x INT, y INT, ex TEXT);\n  CREATE INDEX t2i1 ON t2(x);\n")
		}
	}
	// det 2.2.1 SELECT DISTINCT min(x), max(x) FROM t1 GROUP BY x ... {\n  QUERY PLAN\n  |... (unsupported command, not transpiled)
	// det 2.2.2 SELECT DISTINCT min(x), max(x) FROM t2 GROUP BY x ... {\n  QUERY PLAN\n  |... (unsupported command, not transpiled)
	// det 2.2.3 SELECT DISTINCT * FROM t1 {\n  QUERY PLAN\n  |--SCAN t1\n  `--USE TEMP B-T... (unsupported command, not transpiled)
	// det 2.2.4 SELECT DISTINCT * FROM t1, t2 {\n  QUERY PLAN\n  |--SCAN t1\n  |--SCAN t2\... (unsupported command, not transpiled)
	// det 2.2.5 SELECT DISTINCT * FROM t1, t2 ORDER BY t1.x {\n  QUERY PLAN\n  |--SCAN t1\... (unsupported command, not transpiled)
	// det 2.2.6 SELECT DISTINCT t2.x FROM t1, t2 ORDER BY t2.x {\n  QUERY PLAN\n  |--SCAN ... (unsupported command, not transpiled)
	// det 2.3.1 SELECT max(x) FROM t2 {\n  QUERY PLAN\n  `--SEARCH t2 USING COVERING INDE.... (unsupported command, not transpiled)
	// det 2.3.2 SELECT min(x) FROM t2 {\n  QUERY PLAN\n  `--SEARCH t2 USING COVERING INDE.... (unsupported command, not transpiled)
	// det 2.3.3 SELECT min(x), max(x) FROM t2 {\n  QUERY PLAN\n  `--SCAN t2 USING COVERING... (unsupported command, not transpiled)
	// det 2.4.1 SELECT * FROM t1 WHERE rowid=? {\n  QUERY PLAN\n  `--SEARCH t1 USING INTEG... (unsupported command, not transpiled)
	{ // "3.1.1"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT (SELECT x FROM t1 AS sub) FROM t1;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT (SELECT x FROM t1 AS sub) FROM t1;\n")
		}
	}
	{ // "3.1.2"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t1 WHERE (SELECT x FROM t1 AS sub);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t1 WHERE (SELECT x FROM t1 AS sub);\n")
		}
	}
	{ // "3.1.3"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t1 WHERE (SELECT x FROM t1 AS sub ORDER BY y);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t1 WHERE (SELECT x FROM t1 AS sub ORDER BY y);\n")
		}
	}
	{ // "3.1.4"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t1 WHERE (SELECT x FROM t2 ORDER BY x);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t1 WHERE (SELECT x FROM t2 ORDER BY x);\n")
		}
	}
	// det 3.2.1 {\n  SELECT * FROM (SELECT * FROM t1 ORDER BY x LIM...} {\n  QUERY PLAN\n ... (unsupported command, not transpiled)
	// det 3.2.2 {\n  SELECT * FROM \n    (SELECT * FROM t1 ORDER BY...} {\n  QUERY PLAN\n ... (unsupported command, not transpiled)
	// det 3.2.3 {\n  SELECT * FROM (SELECT * FROM t1 ORDER BY x LIM...} {\n  QUERY PLAN\n ... (unsupported command, not transpiled)
	// det 3.3.1 {\n  SELECT * FROM t1 WHERE y IN (SELECT y FROM t2)...} {\n  QUERY PLAN\n ... (unsupported command, not transpiled)
	// det 3.3.2 {\n  SELECT * FROM t1 WHERE y IN (SELECT y FROM t2 ...} {\n  QUERY PLAN\n ... (unsupported command, not transpiled)
	// det 3.3.3 {\n  SELECT * FROM t1 WHERE EXISTS (SELECT y FROM t...} {\n  QUERY PLAN\n ... (unsupported command, not transpiled)
	{ // "4.1.1"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t1 UNION ALL SELECT * FROM t2\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t1 UNION ALL SELECT * FROM t2\n")
		}
	}
	{ // "4.1.2"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t1 UNION ALL SELECT * FROM t2 ORDER BY 2\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t1 UNION ALL SELECT * FROM t2 ORDER BY 2\n")
		}
	}
	{ // "4.1.3"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t1 UNION SELECT * FROM t2 ORDER BY 2\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t1 UNION SELECT * FROM t2 ORDER BY 2\n")
		}
	}
	{ // "4.1.4"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t1 INTERSECT SELECT * FROM t2 ORDER BY 2\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t1 INTERSECT SELECT * FROM t2 ORDER BY 2\n")
		}
	}
	{ // "4.1.5"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t1 EXCEPT SELECT * FROM t2 ORDER BY 2\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t1 EXCEPT SELECT * FROM t2 ORDER BY 2\n")
		}
	}
	{ // "4.2.2"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t1 UNION ALL SELECT * FROM t2 ORDER BY 1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t1 UNION ALL SELECT * FROM t2 ORDER BY 1\n")
		}
	}
	{ // "4.2.3"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t1 UNION SELECT * FROM t2 ORDER BY 1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t1 UNION SELECT * FROM t2 ORDER BY 1\n")
		}
	}
	{ // "4.2.4"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t1 INTERSECT SELECT * FROM t2 ORDER BY 1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t1 INTERSECT SELECT * FROM t2 ORDER BY 1\n")
		}
	}
	{ // "4.2.5"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT * FROM t1 EXCEPT SELECT * FROM t2 ORDER BY 1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT * FROM t1 EXCEPT SELECT * FROM t2 ORDER BY 1\n")
		}
	}
	{ // "4.3.1"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT x FROM t1 UNION SELECT x FROM t2\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT x FROM t1 UNION SELECT x FROM t2\n")
		}
	}
	{ // "4.3.2"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT x FROM t1 UNION SELECT x FROM t2 UNION SELECT x FROM t1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT x FROM t1 UNION SELECT x FROM t2 UNION SELECT x FROM t1\n")
		}
	}
	{ // "4.3.3"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  SELECT x FROM t1 UNION SELECT x FROM t2 UNION SELECT x FROM t1 ORDER BY 1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  SELECT x FROM t1 UNION SELECT x FROM t2 UNION SELECT x FROM t1 ORDER BY 1\n")
		}
	}
	if false {
		_res = db.Exec("PRAGMA foreign_keys = OFF")
		for _, _t := range db.Query("SELECT name, type FROM sqlite_master WHERE type IN('table','view')").Rows {
			db.Exec("DROP " + fmt.Sprint(_t[1]) + " " + tclQuoteIdent(fmt.Sprint(_t[0])))
		}
		for _, _t := range db.Query("SELECT name, type FROM temp.sqlite_master WHERE type IN('table','view')").Rows {
			db.Exec("DROP " + fmt.Sprint(_t[1]) + " temp." + tclQuoteIdent(fmt.Sprint(_t[0])))
		}
		for _, _t := range db.Query("PRAGMA database_list").Rows {
			if len(_t) > 1 {
				dbname := fmt.Sprint(_t[1])
				if dbname != "main" && dbname != "temp" {
					for _, _u := range db.Query("SELECT name, type FROM " + dbname + ".sqlite_master WHERE type IN('table','view')").Rows {
						db.Exec("DROP " + fmt.Sprint(_u[1]) + " " + dbname + "." + tclQuoteIdent(fmt.Sprint(_u[0])))
					}
				}
			}
		}
		_res = db.Exec("PRAGMA foreign_keys = ON")
		{ // "5.1.0"
			_res = db.Exec(" CREATE TABLE t1(a INT, b INT, ex TEXT) ")
			if _res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", _res.Error, " CREATE TABLE t1(a INT, b INT, ex TEXT) ")
			}
		}
		// det 5.1.1 SELECT a, b FROM t1 WHERE a=1 {\n  0 0 0 {SCAN t1}\n} (unsupported command, not transpiled)
		{ // "5.2.0"
			_res = db.Exec(" CREATE INDEX i1 ON t1(a) ")
			if _res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", _res.Error, " CREATE INDEX i1 ON t1(a) ")
			}
		}
		// det 5.2.1 SELECT a, b FROM t1 WHERE a=1 {\n  0 0 0 {SEARCH t1 USING INDEX i1 (a=?)}\... (unsupported command, not transpiled)
		{ // "5.3.0"
			_res = db.Exec(" CREATE INDEX i2 ON t1(a, b) ")
			if _res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", _res.Error, " CREATE INDEX i2 ON t1(a, b) ")
			}
		}
		// det 5.3.1 SELECT a, b FROM t1 WHERE a=1 {\n  0 0 0 {SEARCH t1 USING COVERING INDEX i... (unsupported command, not transpiled)
		{ // "5.4.0"
			_res = db.Exec("CREATE TABLE t2(c INT, d INT, ex TEXT)")
			if _res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", _res.Error, "CREATE TABLE t2(c INT, d INT, ex TEXT)")
			}
		}
		// det 5.4.1 SELECT t1.a, t2.c FROM t1, t2 WHERE t1.a=1 AND t1.... {\n  0 0 0 {SEARCH t... (unsupported command, not transpiled)
		// det 5.5 SELECT t1.a, t2.c FROM t2, t1 WHERE t1.a=1 AND t1.... {\n  0 0 1 {SEARCH t1 ... (unsupported command, not transpiled)
		{ // "5.5.0"
			_res = db.Exec("CREATE INDEX i3 ON t1(b)")
			if _res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", _res.Error, "CREATE INDEX i3 ON t1(b)")
			}
		}
		// det 5.6.1 SELECT a, b FROM t1 WHERE a=1 OR b=2 {\n  0 0 0 {SEARCH t1 USING COVERING ... (unsupported command, not transpiled)
		// det 5.7 SELECT c, d FROM t2 ORDER BY c {\n  0 0 0 {SCAN t2}\n  0 0 0 {USE TEMP B-TRE... (unsupported command, not transpiled)
		{ // "5.8.0"
			_res = db.Exec("CREATE INDEX i4 ON t2(c)")
			if _res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", _res.Error, "CREATE INDEX i4 ON t2(c)")
			}
		}
		// det 5.8.1 SELECT c, d FROM t2 ORDER BY c {\n  0 0 0 {SCAN t2 USING INDEX i4}\n} (unsupported command, not transpiled)
		// det 5.9 {\n  SELECT (SELECT b FROM t1 WHERE a=0), (SELECT a...} {\n  0 0 0 {SCAN t2 ... (unsupported command, not transpiled)
		// det 5.10 {\n  SELECT count(*) FROM (SELECT max(b) AS x FROM ...} {\n  1 0 0 {SCAN t1... (unsupported command, not transpiled)
		// det 5.11 SELECT a, b FROM (SELECT * FROM t2 WHERE c=1), t1 {\n  0 0 0 {SEARCH t2 USI... (unsupported command, not transpiled)
		// det 5.12 SELECT a,b FROM t1 UNION SELECT c, 99 FROM t2 {\n  1 0 0 {SCAN t1 USING COV... (unsupported command, not transpiled)
		// det 5.13 SELECT a FROM t1 EXCEPT SELECT d FROM t2 ORDER BY ... {\n  1 0 0 {SCAN t1 U... (unsupported command, not transpiled)
		if tclBool("!" + "nonzero_reserved_bytes") {
			vtab.TclVarSet("boilerplate", "", "\n    proc explain_query_plan {db sql} {\n      set stmt [sqlite3_prepare_v2 db $sql -1 DUMMY]\n      print_explain_query_plan $stmt\n      sqlite3_finalize $stmt\n    }\n    sqlite3 db test.db\n    explain_query_plan db {%SQL%}\n    db close\n    exit\n  ")
			boilerplate = "\n    proc explain_query_plan {db sql} {\n      set stmt [sqlite3_prepare_v2 db $sql -1 DUMMY]\n      print_explain_query_plan $stmt\n      sqlite3_finalize $stmt\n    }\n    sqlite3 db test.db\n    explain_query_plan db {%SQL%}\n    db close\n    exit\n  "
			_ = boilerplate // suppress unused warning
			// proc definition (not transpiled)
			// do_peqp_test 6.1 {\n    SELECT a, b FROM t1 EXCEPT SELECT d, 99 FROM...} [string trimleft {\n... (unsupported command, not transpiled)
		}
	}
	_res = db.Exec("PRAGMA foreign_keys = OFF")
	for _, _t := range db.Query("SELECT name, type FROM sqlite_master WHERE type IN('table','view')").Rows {
		db.Exec("DROP " + fmt.Sprint(_t[1]) + " " + tclQuoteIdent(fmt.Sprint(_t[0])))
	}
	for _, _t := range db.Query("SELECT name, type FROM temp.sqlite_master WHERE type IN('table','view')").Rows {
		db.Exec("DROP " + fmt.Sprint(_t[1]) + " temp." + tclQuoteIdent(fmt.Sprint(_t[0])))
	}
	for _, _t := range db.Query("PRAGMA database_list").Rows {
		if len(_t) > 1 {
			dbname := fmt.Sprint(_t[1])
			if dbname != "main" && dbname != "temp" {
				for _, _u := range db.Query("SELECT name, type FROM " + dbname + ".sqlite_master WHERE type IN('table','view')").Rows {
					db.Exec("DROP " + fmt.Sprint(_u[1]) + " " + dbname + "." + tclQuoteIdent(fmt.Sprint(_u[0])))
				}
			}
		}
	}
	_res = db.Exec("PRAGMA foreign_keys = ON")
	{ // "7.0"
		_res = db.Exec("\n  CREATE TABLE t1(a INT, b INT, ex CHAR(100));\n  CREATE TABLE t2(a INT, b INT, ex CHAR(100));\n  CREATE INDEX i1 ON t2(a);\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", _res.Error, "\n  CREATE TABLE t1(a INT, b INT, ex CHAR(100));\n  CREATE TABLE t2(a INT, b INT, ex CHAR(100));\n  CREATE INDEX i1 ON t2(a);\n")
		}
	}
	// det 7.1 SELECT count(*) FROM t1 {\n  QUERY PLAN\n  `--SCAN t1\n} (unsupported command, not transpiled)
	// det 7.2 SELECT count(*) FROM t2 {\n  QUERY PLAN\n  `--SCAN t2 USING COVERING INDEX .... (unsupported command, not transpiled)
	{ // "7.3"
		_res = db.Exec("\n  INSERT INTO t1(a,b) VALUES(1, 2);\n  INSERT INTO t1(a,b) VALUES(3, 4);\n\n  INSERT INTO t2(a,b) VALUES(1, 2);\n  INSERT INTO t2(a,b) VALUES(3, 4);\n  INSERT INTO t2(a,b) VALUES(5, 6);\n \n  ANALYZE;\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", _res.Error, "\n  INSERT INTO t1(a,b) VALUES(1, 2);\n  INSERT INTO t1(a,b) VALUES(3, 4);\n\n  INSERT INTO t2(a,b) VALUES(1, 2);\n  INSERT INTO t2(a,b) VALUES(3, 4);\n  INSERT INTO t2(a,b) VALUES(5, 6);\n \n  ANALYZE;\n")
		}
	}
	db.Close()
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	// det 7.4 SELECT count(*) FROM t1 {\n  QUERY PLAN\n  `--SCAN t1\n} (unsupported command, not transpiled)
	// det 7.5 SELECT count(*) FROM t2 {\n  QUERY PLAN\n  `--SCAN t2 USING COVERING INDEX .... (unsupported command, not transpiled)
	_res = db.Exec("PRAGMA foreign_keys = OFF")
	for _, _t := range db.Query("SELECT name, type FROM sqlite_master WHERE type IN('table','view')").Rows {
		db.Exec("DROP " + fmt.Sprint(_t[1]) + " " + tclQuoteIdent(fmt.Sprint(_t[0])))
	}
	for _, _t := range db.Query("SELECT name, type FROM temp.sqlite_master WHERE type IN('table','view')").Rows {
		db.Exec("DROP " + fmt.Sprint(_t[1]) + " temp." + tclQuoteIdent(fmt.Sprint(_t[0])))
	}
	for _, _t := range db.Query("PRAGMA database_list").Rows {
		if len(_t) > 1 {
			dbname := fmt.Sprint(_t[1])
			if dbname != "main" && dbname != "temp" {
				for _, _u := range db.Query("SELECT name, type FROM " + dbname + ".sqlite_master WHERE type IN('table','view')").Rows {
					db.Exec("DROP " + fmt.Sprint(_u[1]) + " " + dbname + "." + tclQuoteIdent(fmt.Sprint(_u[0])))
				}
			}
		}
	}
	_res = db.Exec("PRAGMA foreign_keys = ON")
	{ // "8.0"
		_res = db.Exec("\n  CREATE TABLE t1(a, b, c, PRIMARY KEY(b, c)) WITHOUT ROWID;\n  CREATE TABLE t2(a, b, c);\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", _res.Error, "\n  CREATE TABLE t1(a, b, c, PRIMARY KEY(b, c)) WITHOUT ROWID;\n  CREATE TABLE t2(a, b, c);\n")
		}
	}
	// det 8.1.1 SELECT * FROM t2 {\n  QUERY PLAN\n  `--SCAN t2\n} (unsupported command, not transpiled)
	// det 8.1.2 SELECT * FROM t2 WHERE rowid=? {\n  QUERY PLAN\n  `--SEARCH t2 USING INTEG... (unsupported command, not transpiled)
	// det 8.1.3 SELECT count(*) FROM t2 {\n  QUERY PLAN\n  `--SCAN t2\n} (unsupported command, not transpiled)
	// det 8.2.1 SELECT * FROM t1 {\n  QUERY PLAN\n  `--SCAN t1\n} (unsupported command, not transpiled)
	// det 8.2.2 SELECT * FROM t1 WHERE b=? {\n  QUERY PLAN\n  `--SEARCH t1 USING PRIMARY K... (unsupported command, not transpiled)
	// det 8.2.3 SELECT * FROM t1 WHERE b=? AND c=? {\n  QUERY PLAN\n  `--SEARCH t1 USING P... (unsupported command, not transpiled)
	// det 8.2.4 SELECT count(*) FROM t1 {\n  QUERY PLAN\n  `--SCAN t1\n} (unsupported command, not transpiled)
	{ // "9.0"
		_res = db.Exec("\n  -- Schema from Fossil 2018-08-16\n  CREATE TABLE forumpost(\n    fpid INTEGER PRIMARY KEY,\n    froot INT,\n    fprev INT,\n    firt INT,\n    fmtime REAL\n  );\n  CREATE INDEX forumthread ON forumpost(froot,fmtime);\n  CREATE TABLE blob(\n    rid INTEGER PRIMARY KEY,\n    rcvid INTEGER,\n    size INTEGER,\n    uuid TEXT UNIQUE NOT NULL,\n    content BLOB,\n    CHECK( length(uuid)>=40 AND rid>0 )\n  );\n  CREATE TABLE event(\n    type TEXT,\n    mtime DATETIME,\n    objid INTEGER PRIMARY KEY,\n    tagid INTEGER,\n    uid INTEGER REFERENCES user,\n    bgcolor TEXT,\n    euser TEXT,\n    user TEXT,\n    ecomment TEXT,\n    comment TEXT,\n    brief TEXT,\n    omtime DATETIME\n  );\n  CREATE INDEX event_i1 ON event(mtime);\n  CREATE TABLE private(rid INTEGER PRIMARY KEY);\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", _res.Error, "\n  -- Schema from Fossil 2018-08-16\n  CREATE TABLE forumpost(\n    fpid INTEGER PRIMARY KEY,\n    froot INT,\n    fprev INT,\n    firt INT,\n    fmtime REAL\n  );\n  CREATE INDEX forumthread ON forumpost(froot,fmtime);\n  CREATE TABLE blob(\n    rid INTEGER PRIMARY KEY,\n    rcvid INTEGER,\n    size INTEGER,\n    uuid TEXT UNIQUE NOT NULL,\n    content BLOB,\n    CHECK( length(uuid)>=40 AND rid>0 )\n  );\n  CREATE TABLE event(\n    type TEXT,\n    mtime DATETIME,\n    objid INTEGER PRIMARY KEY,\n    tagid INTEGER,\n    uid INTEGER REFERENCES user,\n    bgcolor TEXT,\n    euser TEXT,\n    user TEXT,\n    ecomment TEXT,\n    comment TEXT,\n    brief TEXT,\n    omtime DATETIME\n  );\n  CREATE INDEX event_i1 ON event(mtime);\n  CREATE TABLE private(rid INTEGER PRIMARY KEY);\n")
		}
	}
	// optimization_control order-by-subquery off (no PRAGMA equivalent; ignored)
	{ // "9.1"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  WITH thread(age,duration,cnt,root,last) AS (\n    SELECT\n      julianday('now') - max(fmtime) AS age,\n      max(fmtime) - min(fmtime) AS duration,\n      sum(fprev IS NULL) AS msg_count,\n      froot,\n      (SELECT fpid FROM forumpost\n        WHERE froot=x.froot\n          AND fpid NOT IN private\n        ORDER BY fmtime DESC LIMIT 1)\n    FROM forumpost AS x\n    WHERE fpid NOT IN private  --- Ensure this table mentioned in EQP output!\n    GROUP BY froot\n    ORDER BY 1 LIMIT 26 OFFSET 5\n  )\n  SELECT\n    thread.age,\n    thread.duration,\n    thread.cnt,\n    blob.uuid,\n    substr(event.comment,instr(event.comment,':')+1)\n  FROM thread, blob, event\n  WHERE blob.rid=thread.last\n    AND event.objid=thread.last\n  ORDER BY 1;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  WITH thread(age,duration,cnt,root,last) AS (\n    SELECT\n      julianday('now') - max(fmtime) AS age,\n      max(fmtime) - min(fmtime) AS duration,\n      sum(fprev IS NULL) AS msg_count,\n      froot,\n      (SELECT fpid FROM forumpost\n        WHERE froot=x.froot\n          AND fpid NOT IN private\n        ORDER BY fmtime DESC LIMIT 1)\n    FROM forumpost AS x\n    WHERE fpid NOT IN private  --- Ensure this table mentioned in EQP output!\n    GROUP BY froot\n    ORDER BY 1 LIMIT 26 OFFSET 5\n  )\n  SELECT\n    thread.age,\n    thread.duration,\n    thread.cnt,\n    blob.uuid,\n    substr(event.comment,instr(event.comment,':')+1)\n  FROM thread, blob, event\n  WHERE blob.rid=thread.last\n    AND event.objid=thread.last\n  ORDER BY 1;\n")
		}
	}
	_res = db.Exec("PRAGMA skip_scan = 1")
	if _res.Error != nil {
		t.Errorf("optimization_control all skip-scan error: %v", _res.Error)
	}
	{ // "9.2"
		r = db.Query("EXPLAIN QUERY PLAN " + "\n  WITH thread(age,duration,cnt,root,last) AS (\n    SELECT\n      julianday('now') - max(fmtime) AS age,\n      max(fmtime) - min(fmtime) AS duration,\n      sum(fprev IS NULL) AS msg_count,\n      froot,\n      (SELECT fpid FROM forumpost\n        WHERE froot=x.froot\n          AND fpid NOT IN private\n        ORDER BY fmtime DESC LIMIT 1)\n    FROM forumpost AS x\n    WHERE fpid NOT IN private  --- Ensure this table mentioned in EQP output!\n    GROUP BY froot\n    ORDER BY 1 LIMIT 26 OFFSET 5\n  )\n  SELECT\n    thread.age,\n    thread.duration,\n    thread.cnt,\n    blob.uuid,\n    substr(event.comment,instr(event.comment,':')+1)\n  FROM thread, blob, event\n  WHERE blob.rid=thread.last\n    AND event.objid=thread.last\n  ORDER BY 1;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "EXPLAIN QUERY PLAN "+"\n  WITH thread(age,duration,cnt,root,last) AS (\n    SELECT\n      julianday('now') - max(fmtime) AS age,\n      max(fmtime) - min(fmtime) AS duration,\n      sum(fprev IS NULL) AS msg_count,\n      froot,\n      (SELECT fpid FROM forumpost\n        WHERE froot=x.froot\n          AND fpid NOT IN private\n        ORDER BY fmtime DESC LIMIT 1)\n    FROM forumpost AS x\n    WHERE fpid NOT IN private  --- Ensure this table mentioned in EQP output!\n    GROUP BY froot\n    ORDER BY 1 LIMIT 26 OFFSET 5\n  )\n  SELECT\n    thread.age,\n    thread.duration,\n    thread.cnt,\n    blob.uuid,\n    substr(event.comment,instr(event.comment,':')+1)\n  FROM thread, blob, event\n  WHERE blob.rid=thread.last\n    AND event.objid=thread.last\n  ORDER BY 1;\n")
		}
	}
}
