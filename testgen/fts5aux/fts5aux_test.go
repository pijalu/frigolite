// Code generated by tcl2go; DO NOT EDIT.
//go:build testgen
// +build testgen

package fts5aux

import (
"github.com/pijalu/frigolite"
"github.com/pijalu/frigolite/internal/vtab"
"os"
"strings"
"testing"
)

func Test_fts5aux(t *testing.T) {
	if err := os.Chdir(t.TempDir()); err != nil { t.Fatal(err) }
	db, err := frigolite.Open("test.db")
	if err != nil {
		t.Fatal(err)
	}
	defer db.Close()

	var _res *frigolite.Result
	var r *frigolite.Result
	var msg string
	var _r string
	var _berr error
	_ = _berr // suppress unused warning
	_ = msg // suppress unused warning
	_ = _res // suppress unused warning
	_ = r    // suppress unused warning
	_ = _r   // suppress unused warning
	tcl_nullvalue = "{}" // default NULL rendering

	var db1 *frigolite.DB
	_ = db1
	var db2 *frigolite.DB
	_ = db2
	var db3 *frigolite.DB
	_ = db3
	var db4 *frigolite.DB
	_ = db4
	var db5 *frigolite.DB
	_ = db5
	var db6 *frigolite.DB
	_ = db6
	var db7 *frigolite.DB
	_ = db7
	var db8 *frigolite.DB
	_ = db8
	var db9 *frigolite.DB
	_ = db9

	var testprefix string
	_ = testprefix // pre-declared from TCL source
	var res string
	_ = res // pre-declared from TCL source
	var cb string
	_ = cb // pre-declared from TCL source
	var _var string
	_ = _var // pre-declared from TCL source
	var q string
	_ = q // pre-declared from TCL source
	var sql string
	_ = sql // pre-declared from TCL source
	var tn string
	_ = tn // pre-declared from TCL source
	var lRow string
	_ = lRow // pre-declared from TCL source
	var row string
	_ = row // pre-declared from TCL source
	var hitcount string
	_ = hitcount // pre-declared from TCL source
	var cmd string
	_ = cmd // pre-declared from TCL source
	var i string
	_ = i // pre-declared from TCL source
	var add string
	_ = add // pre-declared from TCL source
	var rowid string
	_ = rowid // pre-declared from TCL source
	var code string
	_ = code // pre-declared from TCL source
	var s1 string
	_ = s1 // pre-declared from TCL source
	var s2 string
	_ = s2 // pre-declared from TCL source
	var s3 string
	_ = s3 // pre-declared from TCL source
	var s4 string
	_ = s4 // pre-declared from TCL source
	var iPhrase string
	_ = iPhrase // pre-declared from TCL source

	vtab.TclVarSet("testprefix", "", "fts5aux")
	testprefix = "fts5aux"
	_ = testprefix // suppress unused warning
	// proc definition (not transpiled)
	// sqlite3_fts5_create_function db inst inst (unsupported command, not transpiled)
	// proc definition (not transpiled)
	// sqlite3_fts5_create_function db colsize colsize (unsupported command, not transpiled)
	// proc definition (not transpiled)
	// sqlite3_fts5_create_function db totalsize totalsize (unsupported command, not transpiled)
	{ // "1.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE f1 USING fts5(a, b);\n  INSERT INTO f1 VALUES('one two', 'two one zero');\n  INSERT INTO f1 VALUES('one one', 'one one one');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE f1 USING fts5(a, b);\n  INSERT INTO f1 VALUES('one two', 'two one zero');\n  INSERT INTO f1 VALUES('one one', 'one one one');\n")
		}
	}
	{ // "1.1"
		_res = db.Exec("\n  SELECT inst(f1, -1) FROM f1 WHERE f1 MATCH 'two';\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "SQLITE_RANGE") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "SQLITE_RANGE", resErrString(_res), "\n  SELECT inst(f1, -1) FROM f1 WHERE f1 MATCH 'two';\n")
		}
	}
	{ // "1.2"
		_res = db.Exec("\n  SELECT inst(f1, 0) FROM f1 WHERE f1 MATCH 'two';\n")
		if _res.Error != nil {
			t.Errorf("expected success, got error: %v\n  sql: %s", resErrString(_res), "\n  SELECT inst(f1, 0) FROM f1 WHERE f1 MATCH 'two';\n")
		}
	}
	{ // "1.3"
		_res = db.Exec("\n  SELECT inst(f1, 1) FROM f1 WHERE f1 MATCH 'two';\n")
		if _res.Error != nil {
			t.Errorf("expected success, got error: %v\n  sql: %s", resErrString(_res), "\n  SELECT inst(f1, 1) FROM f1 WHERE f1 MATCH 'two';\n")
		}
	}
	{ // "1.4"
		_res = db.Exec("\n  SELECT inst(f1, 2) FROM f1 WHERE f1 MATCH 'two';\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "SQLITE_RANGE") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "SQLITE_RANGE", resErrString(_res), "\n  SELECT inst(f1, 2) FROM f1 WHERE f1 MATCH 'two';\n")
		}
	}
	{ // "2.1"
		_res = db.Exec("\n  SELECT colsize(f1, 2) FROM f1 WHERE f1 MATCH 'two';\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "SQLITE_RANGE") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "SQLITE_RANGE", resErrString(_res), "\n  SELECT colsize(f1, 2) FROM f1 WHERE f1 MATCH 'two';\n")
		}
	}
	{ // "2.2"
		r = db.Query("\n  SELECT colsize(f1, 0), colsize(f1, 1) FROM f1 WHERE f1 MATCH 'zero';\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT colsize(f1, 0), colsize(f1, 1) FROM f1 WHERE f1 MATCH 'zero';\n")
			return
		}
		got := flatten(r)
		want := "2 3"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "2.3"
		r = db.Query("\n  SELECT colsize(f1, -1) FROM f1 WHERE f1 MATCH 'zero';\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT colsize(f1, -1) FROM f1 WHERE f1 MATCH 'zero';\n")
			return
		}
		got := flatten(r)
		want := "5"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "2.4.1"
		r = db.Query("\n  SELECT totalsize(f1, -1) FROM f1 WHERE f1 MATCH 'zero';\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT totalsize(f1, -1) FROM f1 WHERE f1 MATCH 'zero';\n")
			return
		}
		got := flatten(r)
		want := "10"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "2.4.2"
		r = db.Query("\n  SELECT totalsize(f1, 0) FROM f1 WHERE f1 MATCH 'zero';\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT totalsize(f1, 0) FROM f1 WHERE f1 MATCH 'zero';\n")
			return
		}
		got := flatten(r)
		want := "4"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "2.4.3"
		r = db.Query("\n  SELECT totalsize(f1, 1) FROM f1 WHERE f1 MATCH 'zero';\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT totalsize(f1, 1) FROM f1 WHERE f1 MATCH 'zero';\n")
			return
		}
		got := flatten(r)
		want := "6"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "2.4.4"
		_res = db.Exec("\n  SELECT totalsize(f1, 2) FROM f1 WHERE f1 MATCH 'zero';\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "SQLITE_RANGE") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "SQLITE_RANGE", resErrString(_res), "\n  SELECT totalsize(f1, 2) FROM f1 WHERE f1 MATCH 'zero';\n")
		}
	}
	// proc definition (not transpiled)
	// sqlite3_fts5_create_function db prevrowid [list prevrowid 0] (unsupported command, not transpiled)
	// sqlite3_fts5_create_function db prevrowid1 [list prevrowid 1] (unsupported command, not transpiled)
	{ // "3.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE e5 USING fts5(x);\n  INSERT INTO e5 VALUES('a b c');\n  INSERT INTO e5 VALUES('d e f');\n  INSERT INTO e5 VALUES('a b c');\n  INSERT INTO e5 VALUES('d e f');\n  INSERT INTO e5 VALUES('a b c');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE e5 USING fts5(x);\n  INSERT INTO e5 VALUES('a b c');\n  INSERT INTO e5 VALUES('d e f');\n  INSERT INTO e5 VALUES('a b c');\n  INSERT INTO e5 VALUES('d e f');\n  INSERT INTO e5 VALUES('a b c');\n")
		}
	}
	{ // "3.1"
		r = db.Query("\n  SELECT prevrowid(e5) || '+' || rowid FROM e5 WHERE e5 MATCH 'c'\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT prevrowid(e5) || '+' || rowid FROM e5 WHERE e5 MATCH 'c'\n")
			return
		}
		got := flatten(r)
		want := "0+1 1+3 3+5"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "3.2"
		r = db.Query("\n  SELECT prevrowid(e5) || '+' || prevrowid1(e5) || '+' || rowid \n  FROM e5 WHERE e5 MATCH 'e'\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT prevrowid(e5) || '+' || prevrowid1(e5) || '+' || rowid \n  FROM e5 WHERE e5 MATCH 'e'\n")
			return
		}
		got := flatten(r)
		want := "0+1+2 2+3+4"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "4.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE e7 USING fts5(x);\n  INSERT INTO e7 VALUES('a x a');\n  INSERT INTO e7 VALUES('b x b');\n  INSERT INTO e7 VALUES('c x c');\n  INSERT INTO e7 VALUES('d x d');\n  INSERT INTO e7 VALUES('e x e');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE e7 USING fts5(x);\n  INSERT INTO e7 VALUES('a x a');\n  INSERT INTO e7 VALUES('b x b');\n  INSERT INTO e7 VALUES('c x c');\n  INSERT INTO e7 VALUES('d x d');\n  INSERT INTO e7 VALUES('e x e');\n")
		}
	}
	// proc definition (not transpiled)
	// proc definition (not transpiled)
	// sqlite3_fts5_create_function db phrasequery phrasequery (unsupported command, not transpiled)
	{ // "4.1"
		r = db.Query("\n  SELECT phrasequery(e7, 'SQLITE_OK') FROM e7 WHERE e7 MATCH 'c x'\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT phrasequery(e7, 'SQLITE_OK') FROM e7 WHERE e7 MATCH 'c x'\n")
			return
		}
		got := flatten(r)
		want := "1 2 3 4 5"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "4.2"
		r = db.Query("\n  SELECT phrasequery(e7, 'SQLITE_DONE') FROM e7 WHERE e7 MATCH 'c x'\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT phrasequery(e7, 'SQLITE_DONE') FROM e7 WHERE e7 MATCH 'c x'\n")
			return
		}
		got := flatten(r)
		want := "1 2 3"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "4.3"
		_res = db.Exec("\n  SELECT phrasequery(e7, 'SQLITE_ERROR') FROM e7 WHERE e7 MATCH 'c x'\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "SQLITE_ERROR") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "SQLITE_ERROR", resErrString(_res), "\n  SELECT phrasequery(e7, 'SQLITE_ERROR') FROM e7 WHERE e7 MATCH 'c x'\n")
		}
	}
	{ // "5.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE e9 USING fts5(y);\n  INSERT INTO e9(rowid, y) VALUES(1, 'i iii');\n  INSERT INTO e9(rowid, y) VALUES(2, 'ii iv');\n  INSERT INTO e9(rowid, y) VALUES(3, 'ii');\n  INSERT INTO e9(rowid, y) VALUES(4, 'i iv');\n  INSERT INTO e9(rowid, y) VALUES(5, 'iii');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE e9 USING fts5(y);\n  INSERT INTO e9(rowid, y) VALUES(1, 'i iii');\n  INSERT INTO e9(rowid, y) VALUES(2, 'ii iv');\n  INSERT INTO e9(rowid, y) VALUES(3, 'ii');\n  INSERT INTO e9(rowid, y) VALUES(4, 'i iv');\n  INSERT INTO e9(rowid, y) VALUES(5, 'iii');\n")
		}
	}
	// proc definition (not transpiled)
	// sqlite3_fts5_create_function db my_rowid my_rowid (unsupported command, not transpiled)
	// foreach {var q} "s1 i\n  s2 ii\n  s3 iii\n  s4 iv"
	_items0 := tclSplitList("s1 i\n  s2 ii\n  s3 iii\n  s4 iv")
	for _idx0 := 0; _idx0+2 <= len(_items0); _idx0 += 2 {
		_var := _items0[_idx0+0]
		_ = _var // suppress unused warning
		q := _items0[_idx0+1]
		_ = q // suppress unused warning
		_ = _idx0
			vtab.TclVarSet("sql", "", "SELECT my_rowid(e9) FROM e9 WHERE e9 MATCH '" + q + "'")
			sql = "SELECT my_rowid(e9) FROM e9 WHERE e9 MATCH '" + q + "'"
			_ = sql // suppress unused warning
			// prepared _var: $sql (bind/step emulation)
			tclPrepareStep(db, sql, "_var")
			_ = _var // prepared statement handle
		}
		{ // "5.1.1" (prepare-step internals; SQL side effects only)
			// sqlite3_step $s1 (unknown prepared statement)
			// sqlite3_column_int $s1 0 (unsupported command, not transpiled)
		}
		{ // "5.1.2" (prepare-step internals; SQL side effects only)
			// sqlite3_step $s2 (unknown prepared statement)
			// sqlite3_column_int $s2 0 (unsupported command, not transpiled)
		}
		{ // "5.1.3" (prepare-step internals; SQL side effects only)
			// sqlite3_step $s3 (unknown prepared statement)
			// sqlite3_column_int $s3 0 (unsupported command, not transpiled)
		}
		{ // "5.1.4" (prepare-step internals; SQL side effects only)
			// sqlite3_step $s4 (unknown prepared statement)
			// sqlite3_column_int $s4 0 (unsupported command, not transpiled)
		}
		{ // "5.2.1" (prepare-step internals; SQL side effects only)
			// sqlite3_step $s1 (unknown prepared statement)
			// sqlite3_column_int $s1 0 (unsupported command, not transpiled)
		}
		{ // "5.2.2" (prepare-step internals; SQL side effects only)
			// sqlite3_step $s2 (unknown prepared statement)
			// sqlite3_column_int $s2 0 (unsupported command, not transpiled)
		}
		{ // "5.2.3" (prepare-step internals; SQL side effects only)
			// sqlite3_step $s3 (unknown prepared statement)
			// sqlite3_column_int $s3 0 (unsupported command, not transpiled)
		}
		{ // "5.2.4" (prepare-step internals; SQL side effects only)
			// sqlite3_step $s4 (unknown prepared statement)
			// sqlite3_column_int $s4 0 (unsupported command, not transpiled)
		}
		tclFinalizePrepared("s1")
		// sqlite3_finalize $s1
		tclFinalizePrepared("s2")
		// sqlite3_finalize $s2
		tclFinalizePrepared("s3")
		// sqlite3_finalize $s3
		tclFinalizePrepared("s4")
		// sqlite3_finalize $s4
		{ // "6.0"
			_res = db.Exec("\n  CREATE VIRTUAL TABLE e11 USING fts5(y, z);\n  INSERT INTO e11(rowid, y, z) VALUES(1, 'a b', 45);\n  INSERT INTO e11(rowid, y, z) VALUES(2, 'b c', 46);\n")
			if _res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE e11 USING fts5(y, z);\n  INSERT INTO e11(rowid, y, z) VALUES(1, 'a b', 45);\n  INSERT INTO e11(rowid, y, z) VALUES(2, 'b c', 46);\n")
			}
		}
		{ // "6.1"
			_res = db.Exec("\n  SELECT my_rowid(z) FROM e11 WHERE e11 MATCH 'b'\n")
			if _res.Error == nil || !strings.Contains(_res.Error.Error(), "no such cursor: 45") {
				t.Errorf("expected error containing %q, got: %v\n  sql: %s", "no such cursor: 45", resErrString(_res), "\n  SELECT my_rowid(z) FROM e11 WHERE e11 MATCH 'b'\n")
			}
		}
		{ // "6.2"
			_res = db.Exec("\n  SELECT my_rowid(y) FROM e11 WHERE e11 MATCH 'b'\n")
			if _res.Error == nil || !strings.Contains(_res.Error.Error(), "no such cursor: 0") {
				t.Errorf("expected error containing %q, got: %v\n  sql: %s", "no such cursor: 0", resErrString(_res), "\n  SELECT my_rowid(y) FROM e11 WHERE e11 MATCH 'b'\n")
			}
		}
		// proc definition (not transpiled)
		// sqlite3_fts5_create_function db my_phrasesize my_phrasesize (unsupported command, not transpiled)
		{ // "7.1"
			_res = db.Exec("\n  CREATE VIRTUAL TABLE t1 USING fts5(a);\n  INSERT INTO t1 VALUES('a b c');\n")
			if _res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE t1 USING fts5(a);\n  INSERT INTO t1 VALUES('a b c');\n")
			}
		}
		{ // "7.2"
			r = db.Query("\n  SELECT \n    my_phrasesize(t1, -1),\n    my_phrasesize(t1, 0),\n    my_phrasesize(t1, 1),\n    my_phrasesize(t1, 2)\n  FROM t1 WHERE t1 MATCH 'a OR b+c'\n")
			if r.Error != nil {
				t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT \n    my_phrasesize(t1, -1),\n    my_phrasesize(t1, 0),\n    my_phrasesize(t1, 1),\n    my_phrasesize(t1, 2)\n  FROM t1 WHERE t1 MATCH 'a OR b+c'\n")
				return
			}
			got := flatten(r)
			want := "0 1 2 0"
			if got != want {
				t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
			}
		}
		{ // "8.0"
			_res = db.Exec("\n  CREATE VIRTUAL TABLE x1 USING fts5(a);\n")
			if _res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE x1 USING fts5(a);\n")
			}
		}
		// foreach {tn lRow res} "4  {\"a a a\" \"b\" \"a d\"} {\"[a] [a] [a]\" \"[a] d\"}\n  1  {\"b d\" \"a b\"}       {\"[b] [d]\" \"[a] b\"}\n  2  {\"d b\" \"a d\"}       {\"[d] [b]\" \"[a] d\"}\n  3  {\"a a d\"}           {\"[a] [a] d\"}"
		_items1 := tclSplitList("4  {\"a a a\" \"b\" \"a d\"} {\"[a] [a] [a]\" \"[a] d\"}\n  1  {\"b d\" \"a b\"}       {\"[b] [d]\" \"[a] b\"}\n  2  {\"d b\" \"a d\"}       {\"[d] [b]\" \"[a] d\"}\n  3  {\"a a d\"}           {\"[a] [a] d\"}")
		for _idx1 := 0; _idx1+3 <= len(_items1); _idx1 += 3 {
			tn := _items1[_idx1+0]
			_ = tn // suppress unused warning
			lRow := _items1[_idx1+1]
			_ = lRow // suppress unused warning
			res := _items1[_idx1+2]
			_ = res // suppress unused warning
			_ = _idx1
				_res = db.Exec(" DELETE FROM x1 ")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", _res.Error, " DELETE FROM x1 ")
				}
				for _, row := range tclSplitList(lRow) {
				_ = row // suppress unused warning
					_res = db.Exec(" INSERT INTO x1 VALUES(" + sqlLiteral(row) + ") ")
					if _res.Error != nil {
						t.Errorf("exec error: %v\n  sql: %s", _res.Error, " INSERT INTO x1 VALUES(" + sqlLiteral(row) + ") ")
					}
				}
				{ // "8." + tn
					r = db.Query("\n    SELECT highlight(x1, 0, '[', ']') FROM x1 WHERE x1 MATCH 'a OR (b AND d)';\n  ")
					if r.Error != nil {
						t.Errorf("query error: %v\n  sql: %s", r.Error, "\n    SELECT highlight(x1, 0, '[', ']') FROM x1 WHERE x1 MATCH 'a OR (b AND d)';\n  ")
						return
					}
					got := flatten(r)
					want := tclListFlatten(res)
					if got != want {
						t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
					}
				}
			}
			db.Close()
			os.Remove("test.db")
			os.Remove("test.db-journal")
			os.Remove("test.db-wal")
			db, err = frigolite.Open("test.db")
			if err != nil { t.Fatal(err) }
			tcl_nullvalue = "{}" // fresh connection resets nullvalue
			{ // "9.1"
				_res = db.Exec("\n  CREATE VIRTUAL TABLE t1 USING fts5(a, b);\n  INSERT INTO t1 VALUES('a',   NULL);           -- 1\n  INSERT INTO t1 VALUES('a',   NULL);           -- 2\n  INSERT INTO t1 VALUES('a',   NULL);           -- 3\n  INSERT INTO t1 VALUES('a',   NULL);           -- 4\n  INSERT INTO t1 VALUES('a',   NULL);           -- 5\n  INSERT INTO t1 VALUES('a',   NULL);           -- 6\n  INSERT INTO t1 VALUES('a',   NULL);           -- 7\n  INSERT INTO t1 VALUES('a',   NULL);           -- 8\n  INSERT INTO t1 VALUES(NULL,  'a a b');        -- 9\n  INSERT INTO t1 VALUES(NULL,  'b b a');        -- 10\n")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE t1 USING fts5(a, b);\n  INSERT INTO t1 VALUES('a',   NULL);           -- 1\n  INSERT INTO t1 VALUES('a',   NULL);           -- 2\n  INSERT INTO t1 VALUES('a',   NULL);           -- 3\n  INSERT INTO t1 VALUES('a',   NULL);           -- 4\n  INSERT INTO t1 VALUES('a',   NULL);           -- 5\n  INSERT INTO t1 VALUES('a',   NULL);           -- 6\n  INSERT INTO t1 VALUES('a',   NULL);           -- 7\n  INSERT INTO t1 VALUES('a',   NULL);           -- 8\n  INSERT INTO t1 VALUES(NULL,  'a a b');        -- 9\n  INSERT INTO t1 VALUES(NULL,  'b b a');        -- 10\n")
				}
			}
			{ // "9.2"
				r = db.Query("\n  SELECT rowid FROM t1('a AND b') ORDER BY rank;\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid FROM t1('a AND b') ORDER BY rank;\n")
					return
				}
				got := flatten(r)
				want := "10 9"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "9.3"
				r = db.Query("\n  SELECT rowid FROM t1('b:a AND b:b') ORDER BY rank;\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid FROM t1('b:a AND b:b') ORDER BY rank;\n")
					return
				}
				got := flatten(r)
				want := "9 10"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			db.Close()
			os.Remove("test.db")
			os.Remove("test.db-journal")
			os.Remove("test.db-wal")
			db, err = frigolite.Open("test.db")
			if err != nil { t.Fatal(err) }
			tcl_nullvalue = "{}" // fresh connection resets nullvalue
			{ // "10.0"
				_res = db.Exec("\n  CREATE VIRTUAL TABLE t1 USING fts5(x, y, z);\n  INSERT INTO t1 VALUES('a', 'one two', 1);\n  INSERT INTO t1 VALUES('b', 'two three', 2);\n  INSERT INTO t1 VALUES('c', 'three four', 1);\n  INSERT INTO t1 VALUES('d', 'four five', 2);\n  INSERT INTO t1 VALUES('e', 'five six', 1);\n  INSERT INTO t1 VALUES('f', 'six seven', 2);\n")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE t1 USING fts5(x, y, z);\n  INSERT INTO t1 VALUES('a', 'one two', 1);\n  INSERT INTO t1 VALUES('b', 'two three', 2);\n  INSERT INTO t1 VALUES('c', 'three four', 1);\n  INSERT INTO t1 VALUES('d', 'four five', 2);\n  INSERT INTO t1 VALUES('e', 'five six', 1);\n  INSERT INTO t1 VALUES('f', 'six seven', 2);\n")
				}
			}
			// proc definition (not transpiled)
			// sqlite3_fts5_create_function db firstcol firstcol (unsupported command, not transpiled)
			{ // "10.1.1"
				r = db.Query("\n  SELECT firstcol(t1) FROM t1\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT firstcol(t1) FROM t1\n")
					return
				}
				got := flatten(r)
				want := "a b c d e f"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "10.1.2"
				r = db.Query("\n  SELECT group_concat(x, '.') FROM t1\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT group_concat(x, '.') FROM t1\n")
					return
				}
				got := flatten(r)
				want := "a.b.c.d.e.f"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "10.1.3"
				_res = db.Exec("\n  SELECT group_concat(firstcol(t1), '.') FROM t1\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "unable to use function firstcol in the requested context") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "unable to use function firstcol in the requested context", resErrString(_res), "\n  SELECT group_concat(firstcol(t1), '.') FROM t1\n")
				}
			}
			{ // "10.1.4"
				_res = db.Exec("\n  SELECT group_concat(firstcol(t1), '.') FROM t1 GROUP BY rowid\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "unable to use function firstcol in the requested context") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "unable to use function firstcol in the requested context", resErrString(_res), "\n  SELECT group_concat(firstcol(t1), '.') FROM t1 GROUP BY rowid\n")
				}
			}
			db.Close()
			os.Remove("test.db")
			os.Remove("test.db-journal")
			os.Remove("test.db-wal")
			db, err = frigolite.Open("test.db")
			if err != nil { t.Fatal(err) }
			tcl_nullvalue = "{}" // fresh connection resets nullvalue
			// proc xCallback increments counter var hitcount__cmd_xInstCount____return_SQLITE_OK (registered via db func)
			// proc definition (not transpiled)
			// sqlite3_fts5_create_function db fts5_hitcount fts5_hitcount (unsupported command, not transpiled)
			{ // "11.1"
				_res = db.Exec("\n  CREATE VIRTUAL TABLE x1 USING fts5(z);\n  INSERT INTO x1 VALUES('one two three');\n  INSERT INTO x1 VALUES('one two one three one');\n  INSERT INTO x1 VALUES('one two three');\n")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE x1 USING fts5(z);\n  INSERT INTO x1 VALUES('one two three');\n  INSERT INTO x1 VALUES('one two one three one');\n  INSERT INTO x1 VALUES('one two three');\n")
				}
			}
			{ // "11.2"
				r = db.Query("\n  SELECT fts5_hitcount(x1) FROM x1('one') LIMIT 1;\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT fts5_hitcount(x1) FROM x1('one') LIMIT 1;\n")
					return
				}
				got := flatten(r)
				want := "5"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			db.Close()
			os.Remove("test.db")
			os.Remove("test.db-journal")
			os.Remove("test.db-wal")
			db, err = frigolite.Open("test.db")
			if err != nil { t.Fatal(err) }
			tcl_nullvalue = "{}" // fresh connection resets nullvalue
			// fts5_aux_test_functions db (unsupported command, not transpiled)
			{ // "12.0"
				_res = db.Exec("\n  CREATE VIRTUAL TABLE t1 USING fts5(a, b, c);\n  INSERT INTO t1 VALUES('one', 'two', 'three');\n  INSERT INTO t1 VALUES('one', 'one', 'one');\n  INSERT INTO t1 VALUES('two', 'two', 'two');\n  INSERT INTO t1 VALUES('three', 'three', 'three');\n")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE t1 USING fts5(a, b, c);\n  INSERT INTO t1 VALUES('one', 'two', 'three');\n  INSERT INTO t1 VALUES('one', 'one', 'one');\n  INSERT INTO t1 VALUES('two', 'two', 'two');\n  INSERT INTO t1 VALUES('three', 'three', 'three');\n")
				}
			}
			{ // "12.1.1"
				_res = db.Exec("\n  SELECT fts5_columntext(t1, -1) FROM t1('two');\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "SQLITE_RANGE") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "SQLITE_RANGE", resErrString(_res), "\n  SELECT fts5_columntext(t1, -1) FROM t1('two');\n")
				}
			}
			{ // "12.1.2"
				_res = db.Exec("\n  SELECT fts5_columntext(t1, 3) FROM t1('two');\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "SQLITE_RANGE") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "SQLITE_RANGE", resErrString(_res), "\n  SELECT fts5_columntext(t1, 3) FROM t1('two');\n")
				}
			}
			{ // "12.1.2"
				_res = db.Exec("\n  SELECT fts5_columntext(t1, 1) FROM t1('one AND two');\n")
				if _res.Error != nil {
					t.Errorf("expected success, got error: %v\n  sql: %s", resErrString(_res), "\n  SELECT fts5_columntext(t1, 1) FROM t1('one AND two');\n")
				}
			}
			{ // "12.2.1"
				_res = db.Exec("\n  SELECT fts5_queryphrase(t1, -1) FROM t1('one AND two');\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "SQLITE_RANGE") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "SQLITE_RANGE", resErrString(_res), "\n  SELECT fts5_queryphrase(t1, -1) FROM t1('one AND two');\n")
				}
			}
			{ // "12.2.2"
				_res = db.Exec("\n  SELECT fts5_queryphrase(t1, 2) FROM t1('one AND two');\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "SQLITE_RANGE") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "SQLITE_RANGE", resErrString(_res), "\n  SELECT fts5_queryphrase(t1, 2) FROM t1('one AND two');\n")
				}
			}
			{ // "12.2.3"
				_res = db.Exec("\n  SELECT fts5_queryphrase(t1, 1) FROM t1('one AND two');\n")
				if _res.Error != nil {
					t.Errorf("expected success, got error: %v\n  sql: %s", resErrString(_res), "\n  SELECT fts5_queryphrase(t1, 1) FROM t1('one AND two');\n")
				}
			}
			{ // "12.3.1"
				_res = db.Exec("\n  SELECT fts5_collist(t1, -1) FROM t1('one AND two');\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "SQLITE_RANGE") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "SQLITE_RANGE", resErrString(_res), "\n  SELECT fts5_collist(t1, -1) FROM t1('one AND two');\n")
				}
			}
			{ // "12.3.2"
				_res = db.Exec("\n  SELECT fts5_collist(t1, 2) FROM t1('one AND two');\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "SQLITE_RANGE") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "SQLITE_RANGE", resErrString(_res), "\n  SELECT fts5_collist(t1, 2) FROM t1('one AND two');\n")
				}
			}
			{ // "12.3.3"
				_res = db.Exec("\n  SELECT fts5_collist(t1, 1) FROM t1('one AND two');\n")
				if _res.Error != nil {
					t.Errorf("expected success, got error: %v\n  sql: %s", resErrString(_res), "\n  SELECT fts5_collist(t1, 1) FROM t1('one AND two');\n")
				}
			}
			db.Close()
			os.Remove("test.db")
			os.Remove("test.db-journal")
			os.Remove("test.db-wal")
			db, err = frigolite.Open("test.db")
			if err != nil { t.Fatal(err) }
			tcl_nullvalue = "{}" // fresh connection resets nullvalue
			{ // "13.1"
				r = db.Query("\n  CREATE VIRTUAL TABLE t1 USING fts5(a, tokenize=ascii);\n  INSERT INTO t1 VALUES('a b c'), ('d e f');\n  PRAGMA integrity_check;\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  CREATE VIRTUAL TABLE t1 USING fts5(a, tokenize=ascii);\n  INSERT INTO t1 VALUES('a b c'), ('d e f');\n  PRAGMA integrity_check;\n")
					return
				}
				got := flatten(r)
				want := "ok"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "13.2"
				_res = db.Exec("\n  SELECT highlight(t1, 0, '[', ']') FROM t1\n")
				if _res.Error != nil {
					t.Errorf("expected success, got error: %v\n  sql: %s", resErrString(_res), "\n  SELECT highlight(t1, 0, '[', ']') FROM t1\n")
				}
			}
			{ // "13.3"
				r = db.Query("\n  PRAGMA writable_schema = 1;\n  UPDATE sqlite_schema SET sql = 'CREATE VIRTUAL TABLE t1 USING fts5(a, tokenize=blah)'\n  WHERE name = 't1';\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  PRAGMA writable_schema = 1;\n  UPDATE sqlite_schema SET sql = 'CREATE VIRTUAL TABLE t1 USING fts5(a, tokenize=blah)'\n  WHERE name = 't1';\n")
				}
			}
			db.Close()
			db, err = frigolite.Open("test.db")
			tclConnRegister("db", db)
			if err != nil { t.Fatal(err) }
			{ // "13.4"
				_res = db.Exec("\n  SELECT highlight(t1, 0, '[', ']') FROM t1\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "SQL logic error") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "SQL logic error", resErrString(_res), "\n  SELECT highlight(t1, 0, '[', ']') FROM t1\n")
				}
			}
}
