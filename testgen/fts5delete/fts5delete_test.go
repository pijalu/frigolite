// Code generated by tcl2go; DO NOT EDIT.
//go:build testgen
// +build testgen

package fts5delete

import (
"github.com/pijalu/frigolite"
"github.com/pijalu/frigolite/internal/vtab"
"os"
"strconv"
"strings"
"testing"
)

func Test_fts5delete(t *testing.T) {
	if err := os.Chdir(t.TempDir()); err != nil { t.Fatal(err) }
	db, err := frigolite.Open("test.db")
	if err != nil {
		t.Fatal(err)
	}
	defer db.Close()

	var _res *frigolite.Result
	var r *frigolite.Result
	var msg string
	var _r string
	var _berr error
	_ = _berr // suppress unused warning
	_ = msg // suppress unused warning
	_ = _res // suppress unused warning
	_ = r    // suppress unused warning
	_ = _r   // suppress unused warning
	tcl_nullvalue = "{}" // default NULL rendering

	var db1 *frigolite.DB
	_ = db1
	var db2 *frigolite.DB
	_ = db2
	var db3 *frigolite.DB
	_ = db3
	var db4 *frigolite.DB
	_ = db4
	var db5 *frigolite.DB
	_ = db5
	var db6 *frigolite.DB
	_ = db6
	var db7 *frigolite.DB
	_ = db7
	var db8 *frigolite.DB
	_ = db8
	var db9 *frigolite.DB
	_ = db9

	var testprefix string
	_ = testprefix // pre-declared from TCL source
	var i string
	_ = i // pre-declared from TCL source

	vtab.TclVarSet("testprefix", "", "fts5delete")
	testprefix = "fts5delete"
	_ = testprefix // suppress unused warning
	// fts5_aux_test_functions db (unsupported command, not transpiled)
	{ // "1.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE t1 USING fts5(x);\n  WITH s(i) AS (\n    SELECT 1 UNION ALL SELECT i+1 FROM s WHERE i<5000\n  )\n  INSERT INTO t1(rowid, x) SELECT i, (i/2)*2 FROM s;\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE t1 USING fts5(x);\n  WITH s(i) AS (\n    SELECT 1 UNION ALL SELECT i+1 FROM s WHERE i<5000\n  )\n  INSERT INTO t1(rowid, x) SELECT i, (i/2)*2 FROM s;\n")
		}
	}
	{ // do_test "1.1"
		_res = db.Exec("BEGIN")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", _res.Error, "BEGIN")
		}
		vtab.TclVarSet("i", "", "1")
		i = "1"
		_ = i // suppress unused warning
		for func() bool { i_n, _i_e := strconv.Atoi(i); if _i_e != nil { return false }; return i_n <= 5000 }() {
			if tclBool(i + " % 2") {
				_res = db.Exec(" INSERT INTO t1 VALUES(" + sqlLiteral(i) + ") ")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", _res.Error, " INSERT INTO t1 VALUES(" + sqlLiteral(i) + ") ")
				}
			} else {
				_res = db.Exec(" DELETE FROM t1 WHERE rowid = " + sqlLiteral(i) + " ")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", _res.Error, " DELETE FROM t1 WHERE rowid = " + sqlLiteral(i) + " ")
				}
			}
			// incr i 1
			{
				_n, _err := strconv.Atoi(i)
				if _err == nil {
					i = strconv.Itoa(_n + 1)
				}
			}
		}
		_res = db.Exec("COMMIT")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", _res.Error, "COMMIT")
		}
	}
	{ // do_test "1.2"
		_res = db.Exec(" INSERT INTO t1(t1, rank) VALUES('usermerge', 2); ")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", _res.Error, " INSERT INTO t1(t1, rank) VALUES('usermerge', 2); ")
		}
		vtab.TclVarSet("i", "", "0")
		i = "0"
		_ = i // suppress unused warning
		for func() bool { i_n, _i_e := strconv.Atoi(i); if _i_e != nil { return false }; return i_n < 5 }() {
			_res = db.Exec(" INSERT INTO t1(t1, rank) VALUES('merge', 1) ")
			if _res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", _res.Error, " INSERT INTO t1(t1, rank) VALUES('merge', 1) ")
			}
			_res = db.Exec(" INSERT INTO t1(t1) VALUES('integrity-check') ")
			if _res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", _res.Error, " INSERT INTO t1(t1) VALUES('integrity-check') ")
			}
			// incr i 1
			{
				_n, _err := strconv.Atoi(i)
				if _err == nil {
					i = strconv.Itoa(_n + 1)
				}
			}
		}
	}
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "2.0"
		_res = db.Exec("\n  CREATE TABLE test (\n      id INTEGER PRIMARY KEY,\n      name TEXT,\n      value TEXT\n  );\n  CREATE VIRTUAL TABLE test_idx USING fts5(\n      name, content=test, content_rowid=id\n  );\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE TABLE test (\n      id INTEGER PRIMARY KEY,\n      name TEXT,\n      value TEXT\n  );\n  CREATE VIRTUAL TABLE test_idx USING fts5(\n      name, content=test, content_rowid=id\n  );\n")
		}
	}
	{ // "2.1"
		_res = db.Exec("\n  INSERT INTO test_idx (test_idx, rowid, name) VALUES('delete', 1, 'quick');\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "database disk image is malformed") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "database disk image is malformed", resErrString(_res), "\n  INSERT INTO test_idx (test_idx, rowid, name) VALUES('delete', 1, 'quick');\n")
		}
	}
	{ // "2.2"
		_res = db.Exec("\n  INSERT INTO test_idx(rowid, name) VALUES(123, 'one one one');\n  INSERT INTO test_idx (test_idx, rowid, name) VALUES('delete', 123, 'one');\n  INSERT INTO test_idx (test_idx, rowid, name) VALUES('delete', 123, 'one');\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "database disk image is malformed") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "database disk image is malformed", resErrString(_res), "\n  INSERT INTO test_idx(rowid, name) VALUES(123, 'one one one');\n  INSERT INTO test_idx (test_idx, rowid, name) VALUES('delete', 123, 'one');\n  INSERT INTO test_idx (test_idx, rowid, name) VALUES('delete', 123, 'one');\n")
		}
	}
	{ // "2.3"
		_res = db.Exec("\n  DROP TABLE test_idx;\n  CREATE VIRTUAL TABLE test_idx USING fts5(\n      name, content=test, content_rowid=id\n  );\n\n  INSERT INTO test_idx(rowid, name) VALUES(123, 'one one one');\n  INSERT INTO test_idx(rowid, name) VALUES(124, 'two two two');\n  INSERT INTO test_idx(rowid, name) VALUES(125, 'two two two');\n  INSERT INTO test_idx (test_idx, rowid, name) VALUES('delete', 123, 'one');\n  INSERT INTO test_idx (test_idx, rowid, name) VALUES('delete', 123, 'one');\n  INSERT INTO test_idx (test_idx, rowid, name) VALUES('delete', 123, 'one');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  DROP TABLE test_idx;\n  CREATE VIRTUAL TABLE test_idx USING fts5(\n      name, content=test, content_rowid=id\n  );\n\n  INSERT INTO test_idx(rowid, name) VALUES(123, 'one one one');\n  INSERT INTO test_idx(rowid, name) VALUES(124, 'two two two');\n  INSERT INTO test_idx(rowid, name) VALUES(125, 'two two two');\n  INSERT INTO test_idx (test_idx, rowid, name) VALUES('delete', 123, 'one');\n  INSERT INTO test_idx (test_idx, rowid, name) VALUES('delete', 123, 'one');\n  INSERT INTO test_idx (test_idx, rowid, name) VALUES('delete', 123, 'one');\n")
		}
	}
	{ // "2.4"
		_res = db.Exec("\n  SELECT rowid FROM test_idx WHERE test_idx MATCH 'two' ORDER BY rank;\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "database disk image is malformed") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "database disk image is malformed", resErrString(_res), "\n  SELECT rowid FROM test_idx WHERE test_idx MATCH 'two' ORDER BY rank;\n")
		}
	}
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "3.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE tx USING fts5(a, b, c, d, content=);\n  INSERT INTO tx(rowid, a, c) VALUES(1, 'abc def', 'a b c');\n  INSERT INTO tx(rowid, a, c) VALUES(5, 'a b c', 'a b d def');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE tx USING fts5(a, b, c, d, content=);\n  INSERT INTO tx(rowid, a, c) VALUES(1, 'abc def', 'a b c');\n  INSERT INTO tx(rowid, a, c) VALUES(5, 'a b c', 'a b d def');\n")
		}
	}
	{ // "3.1"
		_res = db.Exec("\n  INSERT INTO tx(tx, rowid, a, b, c, d) \n    VALUES('delete', 5, 'a b c', NULL, 'a b d def', NULL);\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO tx(tx, rowid, a, b, c, d) \n    VALUES('delete', 5, 'a b c', NULL, 'a b d def', NULL);\n")
		}
	}
	{ // "3.2"
		_res = db.Exec("\n  INSERT INTO tx(tx) VALUES('integrity-check');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO tx(tx) VALUES('integrity-check');\n")
		}
	}
	{ // "3.3"
		_res = db.Exec("\n  INSERT INTO tx(tx, rowid, a, b, c, d) \n    VALUES('delete', 1, 'abc def', NULL, 'a b c', NULL);\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO tx(tx, rowid, a, b, c, d) \n    VALUES('delete', 1, 'abc def', NULL, 'a b c', NULL);\n")
		}
	}
	{ // "3.4"
		_res = db.Exec("\n  INSERT INTO tx(tx) VALUES('integrity-check');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO tx(tx) VALUES('integrity-check');\n")
		}
	}
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "4.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE ft1 USING fts5(a, b UNINDEXED, \n      content='', contentless_unindexed=1\n  );\n  CREATE VIRTUAL TABLE ft2 USING fts5(a, b UNINDEXED, \n      content='', contentless_unindexed=1, contentless_delete=1\n  );\n\n  INSERT INTO ft1(rowid, a, b) VALUES(1, 'one', 'i');\n  INSERT INTO ft1(rowid, a, b) VALUES(2, 'two', 'ii');\n  INSERT INTO ft1(rowid, a, b) VALUES(3, 'three', 'iii');\n  INSERT INTO ft2(rowid, a, b) VALUES(1, 'one', 'i');\n  INSERT INTO ft2(rowid, a, b) VALUES(2, 'two', 'ii');\n  INSERT INTO ft2(rowid, a, b) VALUES(3, 'three', 'iii');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE ft1 USING fts5(a, b UNINDEXED, \n      content='', contentless_unindexed=1\n  );\n  CREATE VIRTUAL TABLE ft2 USING fts5(a, b UNINDEXED, \n      content='', contentless_unindexed=1, contentless_delete=1\n  );\n\n  INSERT INTO ft1(rowid, a, b) VALUES(1, 'one', 'i');\n  INSERT INTO ft1(rowid, a, b) VALUES(2, 'two', 'ii');\n  INSERT INTO ft1(rowid, a, b) VALUES(3, 'three', 'iii');\n  INSERT INTO ft2(rowid, a, b) VALUES(1, 'one', 'i');\n  INSERT INTO ft2(rowid, a, b) VALUES(2, 'two', 'ii');\n  INSERT INTO ft2(rowid, a, b) VALUES(3, 'three', 'iii');\n")
		}
	}
	{ // "4.1"
		_res = db.Exec("\n  DELETE FROM ft1 WHERE rowid=2\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "cannot DELETE from contentless fts5 table: ft1") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "cannot DELETE from contentless fts5 table: ft1", resErrString(_res), "\n  DELETE FROM ft1 WHERE rowid=2\n")
		}
	}
	{ // "4.2"
		_res = db.Exec("\n  DELETE FROM ft2 WHERE rowid=2\n")
		if _res.Error != nil {
			t.Errorf("expected success, got error: %v\n  sql: %s", resErrString(_res), "\n  DELETE FROM ft2 WHERE rowid=2\n")
		}
	}
	{ // "4.3"
		_res = db.Exec("\n  INSERT INTO ft1(ft1, rowid, a) VALUES('delete', 2, 'two');\n")
		if _res.Error != nil {
			t.Errorf("expected success, got error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO ft1(ft1, rowid, a) VALUES('delete', 2, 'two');\n")
		}
	}
	{ // "4.2"
		_res = db.Exec("\n  INSERT INTO ft2(ft2, rowid, a) VALUES('delete', 2, 'two');\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "'delete' may not be used with a contentless_delete=1 table") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "'delete' may not be used with a contentless_delete=1 table", resErrString(_res), "\n  INSERT INTO ft2(ft2, rowid, a) VALUES('delete', 2, 'two');\n")
		}
	}
	{ // "4.3"
		r = db.Query("\n  SELECT rowid, * FROM ft1;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, * FROM ft1;\n")
			return
		}
		got := flatten(r)
		want := "1 {} i 3 {} iii"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "4.4"
		r = db.Query("\n  SELECT rowid, * FROM ft2;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, * FROM ft2;\n")
			return
		}
		got := flatten(r)
		want := "1 {} i 3 {} iii"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "4.5"
		r = db.Query("\n  SELECT * FROM ft1_content\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT * FROM ft1_content\n")
			return
		}
		got := flatten(r)
		want := "1 i 3 iii"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "4.6"
		r = db.Query("\n  SELECT * FROM ft2_content\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT * FROM ft2_content\n")
			return
		}
		got := flatten(r)
		want := "1 i 3 iii"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
}
