// Code generated by tcl2go; DO NOT EDIT.
//go:build testgen
// +build testgen

package fts5fault4

import (
"github.com/pijalu/frigolite"
"github.com/pijalu/frigolite/internal/vtab"
"os"
"testing"
)

func Test_fts5fault4(t *testing.T) {
	if err := os.Chdir(t.TempDir()); err != nil { t.Fatal(err) }
	db, err := frigolite.Open("test.db")
	if err != nil {
		t.Fatal(err)
	}
	defer db.Close()

	var _res *frigolite.Result
	var r *frigolite.Result
	var msg string
	var _r string
	var _berr error
	_ = _berr // suppress unused warning
	_ = msg // suppress unused warning
	_ = _res // suppress unused warning
	_ = r    // suppress unused warning
	_ = _r   // suppress unused warning
	tcl_nullvalue = "{}" // default NULL rendering

	var db1 *frigolite.DB
	_ = db1
	var db2 *frigolite.DB
	_ = db2
	var db3 *frigolite.DB
	_ = db3
	var db4 *frigolite.DB
	_ = db4
	var db5 *frigolite.DB
	_ = db5
	var db6 *frigolite.DB
	_ = db6
	var db7 *frigolite.DB
	_ = db7
	var db8 *frigolite.DB
	_ = db8
	var db9 *frigolite.DB
	_ = db9

	var testprefix string
	_ = testprefix // pre-declared from TCL source
	var TMPDBERROR string
	_ = TMPDBERROR // pre-declared from TCL source
	var res string
	_ = res // pre-declared from TCL source
	var row string
	_ = row // pre-declared from TCL source
	var str string
	_ = str // pre-declared from TCL source
	var p string
	_ = p // pre-declared from TCL source
	var c string
	_ = c // pre-declared from TCL source
	var o string
	_ = o // pre-declared from TCL source
	var testdir string
	_ = testdir // pre-declared from TCL source
	var rowid string
	_ = rowid // pre-declared from TCL source
	var cmd string
	_ = cmd // pre-declared from TCL source
	var mod string
	_ = mod // pre-declared from TCL source

	vtab.TclVarSet("testprefix", "", "fts5fault4")
	testprefix = "fts5fault4"
	_ = testprefix // suppress unused warning
	vtab.TclVarSet("TMPDBERROR", "", "1"+" "+"{unable to open a temporary database file for storing temporary tables}")
	TMPDBERROR = "1"+" "+"{unable to open a temporary database file for storing temporary tables}" // TCL namespace variable
	_ = TMPDBERROR // suppress unused warning
	db.RegisterFunction("rnddoc", func(args []interface{}) (interface{}, error) { return nil, nil }, 0, -1)
	{ // do_test "1.0"
		_res = db.Exec(" CREATE VIRTUAL TABLE xx USING fts5(x) ")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", _res.Error, " CREATE VIRTUAL TABLE xx USING fts5(x) ")
		}
	}
	// db_save_and_close: snapshot test.db* under sv_ prefix
	for _, _sf := range tclSplitList(tclGlob("test.db*")) {
		tclFileCopy(_sf, "sv_"+_sf)
	}
	db.Close()
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	// do_faultsim_test 1 -faults oom-* -prep {\n  faultsim_restore_and_reopen\n  execsql { SELEC...} -b... (unsupported command, not transpiled)
	{ // "3.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE jj USING fts5(j);\n  INSERT INTO jj(rowid, j) VALUES(101, 'm t w t f s s');\n  INSERT INTO jj(rowid, j) VALUES(202, 't w t f s');\n  INSERT INTO jj(rowid, j) VALUES(303, 'w t f');\n  INSERT INTO jj(rowid, j) VALUES(404, 't');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE jj USING fts5(j);\n  INSERT INTO jj(rowid, j) VALUES(101, 'm t w t f s s');\n  INSERT INTO jj(rowid, j) VALUES(202, 't w t f s');\n  INSERT INTO jj(rowid, j) VALUES(303, 'w t f');\n  INSERT INTO jj(rowid, j) VALUES(404, 't');\n")
		}
	}
	// db_save_and_close: snapshot test.db* under sv_ prefix
	for _, _sf := range tclSplitList(tclGlob("test.db*")) {
		tclFileCopy(_sf, "sv_"+_sf)
	}
	db.Close()
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	// do_faultsim_test 3 -faults oom-* -prep {\n  faultsim_restore_and_reopen\n  execsql { SELEC...} -b... (unsupported command, not transpiled)
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	db.RegisterFunction("rnddoc", func(args []interface{}) (interface{}, error) { return nil, nil }, 0, -1)
	{ // "4.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE x1 USING fts5(x);\n  INSERT INTO x1(x1, rank) VALUES('pgsz', 32);\n\n  WITH ii(i) AS ( SELECT 1 UNION ALL SELECT i+1 FROM ii WHERE i<10 )\n  INSERT INTO x1 SELECT rnddoc(5) FROM ii;\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE x1 USING fts5(x);\n  INSERT INTO x1(x1, rank) VALUES('pgsz', 32);\n\n  WITH ii(i) AS ( SELECT 1 UNION ALL SELECT i+1 FROM ii WHERE i<10 )\n  INSERT INTO x1 SELECT rnddoc(5) FROM ii;\n")
		}
	}
	vtab.TclVarSet("res", "", tclExecSQL(db, "SELECT rowid, x1 FROM x1 WHERE x1 MATCH '*reads'"))
	res = tclExecSQL(db, "SELECT rowid, x1 FROM x1 WHERE x1 MATCH '*reads'") // TCL namespace variable
	_ = res // suppress unused warning
	// do_faultsim_test 4 -faults oom-* -body {\n  db eval {SELECT rowid, x, x1 FROM x1 WHERE x1 ...} -t... (unsupported command, not transpiled)
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "5.0"
		r = db.Query("\n  PRAGMA encoding='utf16';\n  CREATE VIRTUAL TABLE x2 USING fts5(x);\n  INSERT INTO x2(rowid, x) VALUES(10, 'a b c'); -- 3\n  INSERT INTO x2(rowid, x) VALUES(20, 'a b c'); -- 6\n  INSERT INTO x2(rowid, x) VALUES(30, 'a b c'); -- 2\n  INSERT INTO x2(rowid, x) VALUES(40, 'a b c'); -- 5\n  INSERT INTO x2(rowid, x) VALUES(50, 'a b c'); -- 1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  PRAGMA encoding='utf16';\n  CREATE VIRTUAL TABLE x2 USING fts5(x);\n  INSERT INTO x2(rowid, x) VALUES(10, 'a b c'); -- 3\n  INSERT INTO x2(rowid, x) VALUES(20, 'a b c'); -- 6\n  INSERT INTO x2(rowid, x) VALUES(30, 'a b c'); -- 2\n  INSERT INTO x2(rowid, x) VALUES(40, 'a b c'); -- 5\n  INSERT INTO x2(rowid, x) VALUES(50, 'a b c'); -- 1\n")
		}
	}
	// proc definition (not transpiled)
	// sqlite3_fts5_create_function db rowidmod rowidmod (unsupported command, not transpiled)
	// do_faultsim_test 5.1 -faults oom-* -body {\n  db eval {\n    SELECT rowid || '-' || rank FRO...} ... (unsupported command, not transpiled)
	// proc definition (not transpiled)
	// sqlite3_fts5_create_function db rowidprefix rowidprefix (unsupported command, not transpiled)
	str = tclStringRepeat("abcdefghijklmnopqrstuvwxyz", "10")
	_ = str // suppress unused warning
	// do_faultsim_test 5.2 -faults oom-* -body {\n  db eval "\n    SELECT rank, x FROM x2 WHERE x2...} ... (unsupported command, not transpiled)
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "6.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE x3 USING fts5(xxx);\n  INSERT INTO x3 VALUES('a b c d c b a');\n  INSERT INTO x3 VALUES('a a a a a a a');\n  INSERT INTO x3 VALUES('a a a a a a a');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE x3 USING fts5(xxx);\n  INSERT INTO x3 VALUES('a b c d c b a');\n  INSERT INTO x3 VALUES('a a a a a a a');\n  INSERT INTO x3 VALUES('a a a a a a a');\n")
		}
	}
	// do_faultsim_test 6.1 -faults oom-t* -body {\n  db eval { SELECT highlight(x3, 0, '*', '*') FR...}... (unsupported command, not transpiled)
	// proc definition (not transpiled)
	// sqlite3_fts5_create_function db firstinst firstinst (unsupported command, not transpiled)
	// do_faultsim_test 6.2 -faults oom-t* -body {\n  db eval { SELECT firstinst(x3) FROM x3 WHERE x...}... (unsupported command, not transpiled)
	// proc definition (not transpiled)
	// sqlite3_fts5_create_function db previc previc (unsupported command, not transpiled)
	// do_faultsim_test 6.2 -faults oom-t* -body {\n  db eval { SELECT previc(x3) FROM x3 WHERE x3 M...}... (unsupported command, not transpiled)
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "7.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE tt USING fts5(x, y);\n  INSERT INTO tt VALUES('f b g b c b', 'f a d c c b');  -- 1\n  INSERT INTO tt VALUES('d a e f e d', 'f b b d e e');  -- 2\n  INSERT INTO tt VALUES('f b g a d c', 'e f c f a d');  -- 3\n  INSERT INTO tt VALUES('f f c d g f', 'f a e b g b');  -- 4\n  INSERT INTO tt VALUES('a g b d a g', 'e g a e a c');  -- 5\n  INSERT INTO tt VALUES('c d b d e f', 'f g e g e e');  -- 6\n  INSERT INTO tt VALUES('e g f f b c', 'f c e f g f');  -- 7\n  INSERT INTO tt VALUES('e g c f c e', 'f e e a f g');  -- 8\n  INSERT INTO tt VALUES('e a e b e e', 'd c c f f f');  -- 9\n  INSERT INTO tt VALUES('f a g g c c', 'e g d g c e');  -- 10\n  INSERT INTO tt VALUES('c d b a e f', 'f g e h e e');  -- 11\n\n  CREATE VIRTUAL TABLE tt2 USING fts5(o);\n  INSERT INTO tt2(rowid, o) SELECT rowid, x||' '||y FROM tt;\n  INSERT INTO tt2(rowid, o) VALUES(12, 'a b c d e f g h i j k l');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE tt USING fts5(x, y);\n  INSERT INTO tt VALUES('f b g b c b', 'f a d c c b');  -- 1\n  INSERT INTO tt VALUES('d a e f e d', 'f b b d e e');  -- 2\n  INSERT INTO tt VALUES('f b g a d c', 'e f c f a d');  -- 3\n  INSERT INTO tt VALUES('f f c d g f', 'f a e b g b');  -- 4\n  INSERT INTO tt VALUES('a g b d a g', 'e g a e a c');  -- 5\n  INSERT INTO tt VALUES('c d b d e f', 'f g e g e e');  -- 6\n  INSERT INTO tt VALUES('e g f f b c', 'f c e f g f');  -- 7\n  INSERT INTO tt VALUES('e g c f c e', 'f e e a f g');  -- 8\n  INSERT INTO tt VALUES('e a e b e e', 'd c c f f f');  -- 9\n  INSERT INTO tt VALUES('f a g g c c', 'e g d g c e');  -- 10\n  INSERT INTO tt VALUES('c d b a e f', 'f g e h e e');  -- 11\n\n  CREATE VIRTUAL TABLE tt2 USING fts5(o);\n  INSERT INTO tt2(rowid, o) SELECT rowid, x||' '||y FROM tt;\n  INSERT INTO tt2(rowid, o) VALUES(12, 'a b c d e f g h i j k l');\n")
		}
	}
	// do_faultsim_test 7.2 -faults oom-* -body {\n  db eval { SELECT rowid FROM tt WHERE tt MATCH ...} ... (unsupported command, not transpiled)
	// do_faultsim_test 7.3 -faults oom-* -body {\n  db eval { SELECT rowid FROM tt WHERE tt MATCH ...} ... (unsupported command, not transpiled)
	// do_faultsim_test 7.4 -faults oom-t* -body {\n  db eval { SELECT rowid FROM tt2 WHERE tt2 MATC...}... (unsupported command, not transpiled)
	// do_faultsim_test 7.5 -faults oom-* -body {\n  db eval {SELECT rowid FROM tt2 WHERE tt2 MATCH...} ... (unsupported command, not transpiled)
	// do_faultsim_test 7.6 -faults oom-* -body {\n  db eval {SELECT rowid FROM tt WHERE tt MATCH '...} ... (unsupported command, not transpiled)
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "8.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE tt USING fts5(x);\n  INSERT INTO tt(tt, rank) VALUES('pgsz', 32);\n  BEGIN;\n    INSERT INTO tt(rowid, x) VALUES(1, 'a b c d x x');\n    WITH ii(i) AS (SELECT 2 UNION ALL SELECT i+1 FROM ii WHERE i<99)\n      INSERT INTO tt(rowid, x) SELECT i, 'a b c x x d' FROM ii;\n    INSERT INTO tt(rowid, x) VALUES(100, 'a b c d x x');\n  COMMIT;\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE tt USING fts5(x);\n  INSERT INTO tt(tt, rank) VALUES('pgsz', 32);\n  BEGIN;\n    INSERT INTO tt(rowid, x) VALUES(1, 'a b c d x x');\n    WITH ii(i) AS (SELECT 2 UNION ALL SELECT i+1 FROM ii WHERE i<99)\n      INSERT INTO tt(rowid, x) SELECT i, 'a b c x x d' FROM ii;\n    INSERT INTO tt(rowid, x) VALUES(100, 'a b c d x x');\n  COMMIT;\n")
		}
	}
	// do_faultsim_test 8.1 -faults oom-t* -body {\n  db eval { SELECT rowid FROM tt WHERE tt MATCH ...}... (unsupported command, not transpiled)
	// do_faultsim_test 8.2 -faults oom-t* -body {\n  db eval { SELECT count(*) FROM tt WHERE tt MAT...}... (unsupported command, not transpiled)
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "9.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE tt USING fts5(x);\n  INSERT INTO tt(tt, rank) VALUES('pgsz', 32);\n  BEGIN;\n    WITH ii(i) AS (SELECT 1 UNION ALL SELECT i+1 FROM ii WHERE i<200)\n      INSERT INTO tt(rowid, x) \n      SELECT i, CASE WHEN (i%50)==0 THEN 'a a a a a a' ELSE 'a x a x a x' END \n      FROM ii;\n  COMMIT;\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE tt USING fts5(x);\n  INSERT INTO tt(tt, rank) VALUES('pgsz', 32);\n  BEGIN;\n    WITH ii(i) AS (SELECT 1 UNION ALL SELECT i+1 FROM ii WHERE i<200)\n      INSERT INTO tt(rowid, x) \n      SELECT i, CASE WHEN (i%50)==0 THEN 'a a a a a a' ELSE 'a x a x a x' END \n      FROM ii;\n  COMMIT;\n")
		}
	}
	// do_faultsim_test 9.1 -faults oom-* -body {\n  db eval { SELECT rowid FROM tt WHERE tt MATCH ...} ... (unsupported command, not transpiled)
	// do_faultsim_test 10.1 -faults oom-t* -body {\n  db one { SELECT fts5_expr('a AND b NEAR(a b)')...... (unsupported command, not transpiled)
	// do_faultsim_test 10.2 -faults oom-t* -body {\n  db one { SELECT fts5_expr_tcl('x:"a b c" AND b...... (unsupported command, not transpiled)
	// do_faultsim_test 10.3 -faults oom-t* -body {\n  db one { SELECT fts5_expr('x:a', 'x') }\n} -test ... (unsupported command, not transpiled)
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "11.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE ft USING fts5(x);\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE ft USING fts5(x);\n")
		}
	}
	// do_faultsim_test 11.1 -faults oom-t* -body {\n  db eval { INSERT INTO ft(ft, rank) VALUES('ran...... (unsupported command, not transpiled)
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "12.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE ft USING fts5(x);\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE ft USING fts5(x);\n")
		}
	}
	// db_save_and_close: snapshot test.db* under sv_ prefix
	for _, _sf := range tclSplitList(tclGlob("test.db*")) {
		tclFileCopy(_sf, "sv_"+_sf)
	}
	db.Close()
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	// do_faultsim_test 12.1 -faults oom-t* -prep {\n  faultsim_restore_and_reopen\n  db eval { SELEC...... (unsupported command, not transpiled)
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "13.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE ft USING fts5(x);\n  INSERT INTO ft VALUES('a b');\n  CREATE VIRTUAL TABLE vv USING fts5vocab(ft, 'row');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE ft USING fts5(x);\n  INSERT INTO ft VALUES('a b');\n  CREATE VIRTUAL TABLE vv USING fts5vocab(ft, 'row');\n")
		}
	}
	// db_save_and_close: snapshot test.db* under sv_ prefix
	for _, _sf := range tclSplitList(tclGlob("test.db*")) {
		tclFileCopy(_sf, "sv_"+_sf)
	}
	db.Close()
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	// do_faultsim_test 13.1 -faults oom-t* -prep {\n  faultsim_restore_and_reopen\n  db eval { SELEC...... (unsupported command, not transpiled)
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "13.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE ft USING fts5(x, y, z);\n  INSERT INTO ft(ft, rank) VALUES('pgsz', 32);\n  INSERT INTO ft VALUES(\n      'x x x x x x x x x x x x x x x x',\n      'y y y y y y y y y y y y y y y y',\n      'z z z z z z z z x x x x x x x x'\n  );\n  INSERT INTO ft SELECT * FROM ft;\n  INSERT INTO ft SELECT * FROM ft;\n  INSERT INTO ft SELECT * FROM ft;\n  INSERT INTO ft SELECT * FROM ft;\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE ft USING fts5(x, y, z);\n  INSERT INTO ft(ft, rank) VALUES('pgsz', 32);\n  INSERT INTO ft VALUES(\n      'x x x x x x x x x x x x x x x x',\n      'y y y y y y y y y y y y y y y y',\n      'z z z z z z z z x x x x x x x x'\n  );\n  INSERT INTO ft SELECT * FROM ft;\n  INSERT INTO ft SELECT * FROM ft;\n  INSERT INTO ft SELECT * FROM ft;\n  INSERT INTO ft SELECT * FROM ft;\n")
		}
	}
	// db_save_and_close: snapshot test.db* under sv_ prefix
	for _, _sf := range tclSplitList(tclGlob("test.db*")) {
		tclFileCopy(_sf, "sv_"+_sf)
	}
	db.Close()
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	// do_faultsim_test 13.1 -faults oom-t* -prep {\n  faultsim_restore_and_reopen\n  db eval { SELEC...... (unsupported command, not transpiled)
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "14.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE \"tbl one\" USING fts5(x, y, z);\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE \"tbl one\" USING fts5(x, y, z);\n")
		}
	}
	// db_save_and_close: snapshot test.db* under sv_ prefix
	for _, _sf := range tclSplitList(tclGlob("test.db*")) {
		tclFileCopy(_sf, "sv_"+_sf)
	}
	db.Close()
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	// do_faultsim_test 14.1 -faults oom-t* -prep {\n  faultsim_restore_and_reopen\n  db eval { SELEC...... (unsupported command, not transpiled)
}
