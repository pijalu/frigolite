// Code generated by tcl2go; DO NOT EDIT.
//go:build testgen
// +build testgen

package fts5locale

import (
"github.com/pijalu/frigolite"
"github.com/pijalu/frigolite/internal/vtab"
"os"
"strings"
"testing"
)

func Test_fts5locale(t *testing.T) {
	if err := os.Chdir(t.TempDir()); err != nil { t.Fatal(err) }
	db, err := frigolite.Open("test.db")
	if err != nil {
		t.Fatal(err)
	}
	defer db.Close()

	var _res *frigolite.Result
	var r *frigolite.Result
	var msg string
	var _r string
	var _berr error
	_ = _berr // suppress unused warning
	_ = msg // suppress unused warning
	_ = _res // suppress unused warning
	_ = r    // suppress unused warning
	_ = _r   // suppress unused warning
	tcl_nullvalue = "{}" // default NULL rendering

	var db1 *frigolite.DB
	_ = db1
	var db2 *frigolite.DB
	_ = db2
	var db3 *frigolite.DB
	_ = db3
	var db4 *frigolite.DB
	_ = db4
	var db5 *frigolite.DB
	_ = db5
	var db6 *frigolite.DB
	_ = db6
	var db7 *frigolite.DB
	_ = db7
	var db8 *frigolite.DB
	_ = db8
	var db9 *frigolite.DB
	_ = db9

	var testprefix string
	_ = testprefix // pre-declared from TCL source
	var ret string
	_ = ret // pre-declared from TCL source
	var c string
	_ = c // pre-declared from TCL source
	var token string
	_ = token // pre-declared from TCL source
	var iToken string
	_ = iToken // pre-declared from TCL source
	var bSkip string
	_ = bSkip // pre-declared from TCL source
	var w string
	_ = w // pre-declared from TCL source
	var iStart string
	_ = iStart // pre-declared from TCL source
	var iEnd string
	_ = iEnd // pre-declared from TCL source
	var tn string
	_ = tn // pre-declared from TCL source
	var opt string
	_ = opt // pre-declared from TCL source
	var detail string
	_ = detail // pre-declared from TCL source
	var v string
	_ = v // pre-declared from TCL source
	var locale string
	_ = locale // pre-declared from TCL source
	var text string
	_ = text // pre-declared from TCL source

	vtab.TclVarSet("testprefix", "", "fts5locale")
	testprefix = "fts5locale"
	_ = testprefix // suppress unused warning
	// proc definition (not transpiled)
	// proc definition (not transpiled)
	// proc definition (not transpiled)
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	// sqlite3_fts5_create_tokenizer -v2 db tcl tcl_create (unsupported command, not transpiled)
	{ // "1.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE t1 USING fts5(a, tokenize=tcl);\n  INSERT INTO t1 VALUES('abc');\n  INSERT INTO t1 VALUES('cba');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE t1 USING fts5(a, tokenize=tcl);\n  INSERT INTO t1 VALUES('abc');\n  INSERT INTO t1 VALUES('cba');\n")
		}
	}
	{ // "1.1"
		r = db.Query("\n  SELECT rowid, a FROM t1( fts5_locale('en_US', 'abc') );\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, a FROM t1( fts5_locale('en_US', 'abc') );\n")
			return
		}
		got := flatten(r)
		want := "1 abc"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.2"
		r = db.Query("\n  SELECT rowid, a FROM t1( fts5_locale('reverse', 'abc') );\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, a FROM t1( fts5_locale('reverse', 'abc') );\n")
			return
		}
		got := flatten(r)
		want := "2 cba"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	// sqlite3_fts5_create_tokenizer -v2 db tcl tcl_create (unsupported command, not transpiled)
	{ // "2.1"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE b1 USING fts5(x, y, locale=1, tokenize=tcl);\n  CREATE VIRTUAL TABLE b2 USING fts5(x, y, locale=0, tokenize=tcl);\n\n  CREATE VIRTUAL TABLE ttt USING fts5vocab('b1', instance);\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE b1 USING fts5(x, y, locale=1, tokenize=tcl);\n  CREATE VIRTUAL TABLE b2 USING fts5(x, y, locale=0, tokenize=tcl);\n\n  CREATE VIRTUAL TABLE ttt USING fts5vocab('b1', instance);\n")
		}
	}
	{ // "2.2.1"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE b3 USING fts5(x, y, locale=2);\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "malformed locale=... directive") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "malformed locale=... directive", resErrString(_res), "\n  CREATE VIRTUAL TABLE b3 USING fts5(x, y, locale=2);\n")
		}
	}
	{ // "2.2.2"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE b3 USING fts5(x, y, locale=111);\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "malformed locale=... directive") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "malformed locale=... directive", resErrString(_res), "\n  CREATE VIRTUAL TABLE b3 USING fts5(x, y, locale=111);\n")
		}
	}
	{ // "2.3"
		_res = db.Exec("\n  INSERT INTO b1(b1, rank) VALUES('locale', 0);\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "SQL logic error") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "SQL logic error", resErrString(_res), "\n  INSERT INTO b1(b1, rank) VALUES('locale', 0);\n")
		}
	}
	{ // "2.4.1"
		_res = db.Exec("\n  INSERT INTO b1 VALUES('abc', 'one two three');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO b1 VALUES('abc', 'one two three');\n")
		}
	}
	{ // "2.4.2"
		_res = db.Exec("\n  INSERT INTO b1 VALUES('def', fts5_locale('reverse', 'four five six'));\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO b1 VALUES('def', fts5_locale('reverse', 'four five six'));\n")
		}
	}
	{ // "2.5"
		_res = db.Exec("\n  INSERT INTO b2 VALUES('abc', 'one two three');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO b2 VALUES('abc', 'one two three');\n")
		}
	}
	{ // "2.6"
		_res = db.Exec("\n  INSERT INTO b2 VALUES('def', fts5_locale('reverse', 'four five six'));\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "fts5_locale() requires locale=1") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "fts5_locale() requires locale=1", resErrString(_res), "\n  INSERT INTO b2 VALUES('def', fts5_locale('reverse', 'four five six'));\n")
		}
	}
	{ // "2.7"
		r = db.Query(" SELECT rowid FROM b1('one')  ")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, " SELECT rowid FROM b1('one')  ")
			return
		}
		got := flatten(r)
		want := "1"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "2.8"
		r = db.Query(" SELECT rowid FROM b1('four') ")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, " SELECT rowid FROM b1('four') ")
		}
	}
	{ // "2.9"
		r = db.Query(" SELECT rowid FROM b1('ruof') ")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, " SELECT rowid FROM b1('ruof') ")
			return
		}
		got := flatten(r)
		want := "2"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "2.10"
		r = db.Query(" SELECT rowid FROM b1(fts5_locale('reverse', 'five'))")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, " SELECT rowid FROM b1(fts5_locale('reverse', 'five'))")
			return
		}
		got := flatten(r)
		want := "2"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "2.11"
		r = db.Query("\n  SELECT x, quote(y) FROM b1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT x, quote(y) FROM b1\n")
			return
		}
		got := flatten(r)
		want := "abc 'one two three' def 'four five six'"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "2.12"
		r = db.Query(" SELECT quote(y) FROM b1('ruof') ")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, " SELECT quote(y) FROM b1('ruof') ")
			return
		}
		got := flatten(r)
		want := "'four five six'"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "2.13"
		_res = db.Exec("\n  INSERT INTO b1(b1) VALUES('integrity-check');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO b1(b1) VALUES('integrity-check');\n")
		}
	}
	{ // "2.14"
		_res = db.Exec("\n  INSERT INTO b1(b1) VALUES('rebuild');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO b1(b1) VALUES('rebuild');\n")
		}
	}
	{ // "2.15"
		_res = db.Exec("\n  INSERT INTO b1(b1) VALUES('integrity-check');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO b1(b1) VALUES('integrity-check');\n")
		}
	}
	{ // "2.16"
		_res = db.Exec("\n  DELETE FROM b1 WHERE rowid=2\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  DELETE FROM b1 WHERE rowid=2\n")
		}
	}
	{ // "2.17"
		_res = db.Exec("\n  INSERT INTO b1(b1) VALUES('integrity-check');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO b1(b1) VALUES('integrity-check');\n")
		}
	}
	{ // "2.18"
		_res = db.Exec("\n  INSERT INTO b1(rowid, x, y) VALUES(\n      test_setsubtype(45, 76), 'abc def', 'def abc'\n  );\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO b1(rowid, x, y) VALUES(\n      test_setsubtype(45, 76), 'abc def', 'def abc'\n  );\n")
		}
	}
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	// sqlite3_fts5_create_tokenizer -v2 db tcl tcl_create (unsupported command, not transpiled)
	{ // "3.1"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE c1 USING fts5(x, content=, tokenize=tcl, locale=1);\n  CREATE VIRTUAL TABLE c2 USING fts5vocab('c1', instance);\n\n  INSERT INTO c1 VALUES('hello world');\n  INSERT INTO c1 VALUES( fts5_locale('reverse', 'one two three') );\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE c1 USING fts5(x, content=, tokenize=tcl, locale=1);\n  CREATE VIRTUAL TABLE c2 USING fts5vocab('c1', instance);\n\n  INSERT INTO c1 VALUES('hello world');\n  INSERT INTO c1 VALUES( fts5_locale('reverse', 'one two three') );\n")
		}
	}
	{ // "3.2"
		r = db.Query("\n  SELECT DISTINCT term FROM c2 ORDER BY 1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT DISTINCT term FROM c2 ORDER BY 1\n")
			return
		}
		got := flatten(r)
		want := "eerht eno hello owt world"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "3.3"
		_res = db.Exec("\n  INSERT INTO c1(c1, rowid, x) \n    VALUES('delete', 2, fts5_locale('reverse', 'one two three') );\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO c1(c1, rowid, x) \n    VALUES('delete', 2, fts5_locale('reverse', 'one two three') );\n")
		}
	}
	{ // "3.4"
		r = db.Query("\n  SELECT DISTINCT term FROM c2 ORDER BY 1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT DISTINCT term FROM c2 ORDER BY 1\n")
			return
		}
		got := flatten(r)
		want := "hello world"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	// sqlite3_fts5_create_tokenizer -v2 db tcl tcl_create (unsupported command, not transpiled)
	{ // "4.1"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE d1 USING fts5(x, y, locale=1, tokenize=tcl);\n  CREATE VIRTUAL TABLE d2 USING fts5vocab('d1', instance);\n\n  INSERT INTO d1(rowid, x, y) VALUES(1, 'abc', 'def');\n  INSERT INTO d1(rowid, x, y) VALUES(2, 'ghi', fts5_locale('reverse', 'hello'));\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE d1 USING fts5(x, y, locale=1, tokenize=tcl);\n  CREATE VIRTUAL TABLE d2 USING fts5vocab('d1', instance);\n\n  INSERT INTO d1(rowid, x, y) VALUES(1, 'abc', 'def');\n  INSERT INTO d1(rowid, x, y) VALUES(2, 'ghi', fts5_locale('reverse', 'hello'));\n")
		}
	}
	{ // "4.2"
		r = db.Query("\n  SELECT DISTINCT term FROM d2 ORDER BY 1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT DISTINCT term FROM d2 ORDER BY 1\n")
			return
		}
		got := flatten(r)
		want := "abc def ghi olleh"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "4.3"
		_res = db.Exec("\n  UPDATE d1 SET x='jkl' WHERE rowid=2;\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  UPDATE d1 SET x='jkl' WHERE rowid=2;\n")
		}
	}
	{ // "4.4"
		r = db.Query("\n  SELECT DISTINCT term FROM d2 ORDER BY 1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT DISTINCT term FROM d2 ORDER BY 1\n")
			return
		}
		got := flatten(r)
		want := "abc def jkl olleh"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "4.5"
		r = db.Query("\n  SELECT rowid, * FROM d1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, * FROM d1\n")
			return
		}
		got := flatten(r)
		want := "1 abc def 2 jkl hello"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "4.6"
		_res = db.Exec("\n  UPDATE d1 SET rowid=4 WHERE rowid=2\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  UPDATE d1 SET rowid=4 WHERE rowid=2\n")
		}
	}
	{ // "4.7"
		r = db.Query("\n  SELECT rowid, * FROM d1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, * FROM d1\n")
			return
		}
		got := flatten(r)
		want := "1 abc def 4 jkl hello"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	// fts5_aux_test_functions db (unsupported command, not transpiled)
	{ // "4.8.1"
		r = db.Query("\n  SELECT fts5_test_columntext(d1) FROM d1('jkl')\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT fts5_test_columntext(d1) FROM d1('jkl')\n")
			return
		}
		got := flatten(r)
		want := "jkl hello"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "4.8.2"
		r = db.Query("\n  SELECT fts5_test_columntext(d1) FROM d1(fts5_locale('reverse', 'hello'))\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT fts5_test_columntext(d1) FROM d1(fts5_locale('reverse', 'hello'))\n")
			return
		}
		got := flatten(r)
		want := "jkl hello"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "4.9"
		r = db.Query("\n  SELECT fts5_test_columnlocale(d1) FROM d1(fts5_locale('reverse', 'hello'))\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT fts5_test_columnlocale(d1) FROM d1(fts5_locale('reverse', 'hello'))\n")
			return
		}
		got := flatten(r)
		want := "{} reverse"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "4.10"
		r = db.Query("\n  SELECT fts5_test_columnlocale(d1) FROM d1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT fts5_test_columnlocale(d1) FROM d1\n")
			return
		}
		got := flatten(r)
		want := "{} {} {} reverse"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	// sqlite3_fts5_create_tokenizer -v2 db tcl tcl_create (unsupported command, not transpiled)
	{ // "5.1"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE t1 USING fts5(\n      x, y UNINDEXED, locale=1, tokenize=tcl\n  );\n\n  INSERT INTO t1(rowid, x, y) VALUES(111,\n      fts5_locale('reverse', 'one two three'),\n      fts5_locale('reverse', 'four five six')\n  );\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE t1 USING fts5(\n      x, y UNINDEXED, locale=1, tokenize=tcl\n  );\n\n  INSERT INTO t1(rowid, x, y) VALUES(111,\n      fts5_locale('reverse', 'one two three'),\n      fts5_locale('reverse', 'four five six')\n  );\n")
		}
	}
	{ // "5.2"
		r = db.Query("\n  SELECT rowid, x, y FROM t1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, x, y FROM t1\n")
			return
		}
		got := flatten(r)
		want := "111 one two three four five six"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "5.3"
		r = db.Query("\n  SELECT typeof(c0), typeof(c1), typeof(l0) FROM t1_content\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT typeof(c0), typeof(c1), typeof(l0) FROM t1_content\n")
			return
		}
		got := flatten(r)
		want := "text text text"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	// foreach {tn opt} "1 {}\n  2 {, columnsize=0}"
	_items0 := tclSplitList("1 {}\n  2 {, columnsize=0}")
	for _idx0 := 0; _idx0+2 <= len(_items0); _idx0 += 2 {
		tn := _items0[_idx0+0]
		_ = tn // suppress unused warning
		opt := _items0[_idx0+1]
		_ = opt // suppress unused warning
		_ = _idx0
			db.Close()
			os.Remove("test.db")
			os.Remove("test.db-journal")
			os.Remove("test.db-wal")
			db, err = frigolite.Open("test.db")
			if err != nil { t.Fatal(err) }
			tcl_nullvalue = "{}" // fresh connection resets nullvalue
			// sqlite3_fts5_create_tokenizer -v2 db tcl tcl_create (unsupported command, not transpiled)
			{ // "6." + tn + ".1"
				_res = db.Exec("\n    CREATE VIRTUAL TABLE y1 USING fts5(t, locale=1, tokenize=tcl " + opt + ");\n  ")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n    CREATE VIRTUAL TABLE y1 USING fts5(t, locale=1, tokenize=tcl " + opt + ");\n  ")
				}
			}
			{ // "6." + tn + ".2"
				_res = db.Exec("\n    INSERT INTO y1(rowid, t) VALUES\n      (1, fts5_locale('second', 'the city of London')),\n      (2, fts5_locale('second', 'shall have all the old')),\n      (3, fts5_locale('second', 'Liberties and Customs')),\n      (4, fts5_locale('second', 'which it hath been used to have'));\n  ")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n    INSERT INTO y1(rowid, t) VALUES\n      (1, fts5_locale('second', 'the city of London')),\n      (2, fts5_locale('second', 'shall have all the old')),\n      (3, fts5_locale('second', 'Liberties and Customs')),\n      (4, fts5_locale('second', 'which it hath been used to have'));\n  ")
				}
			}
			// fts5_aux_test_functions db (unsupported command, not transpiled)
			{ // "6." + tn + ".3"
				r = db.Query("\n    SELECT fts5_test_columnsize(y1) FROM y1\n  ")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n    SELECT fts5_test_columnsize(y1) FROM y1\n  ")
					return
				}
				got := flatten(r)
				want := "2 3 2 4"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "6." + tn + ".4"
				r = db.Query("\n    SELECT rowid, fts5_test_columnsize(y1) FROM y1('shall');\n  ")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n    SELECT rowid, fts5_test_columnsize(y1) FROM y1('shall');\n  ")
					return
				}
				got := flatten(r)
				want := "2 3"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "6." + tn + ".5"
				r = db.Query("\n    SELECT rowid, fts5_test_columnsize(y1) FROM y1('shall');\n  ")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n    SELECT rowid, fts5_test_columnsize(y1) FROM y1('shall');\n  ")
					return
				}
				got := flatten(r)
				want := "2 3"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "6." + tn + ".6"
				r = db.Query("\n    SELECT rowid, fts5_test_columnsize(y1) FROM y1('have');\n  ")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n    SELECT rowid, fts5_test_columnsize(y1) FROM y1('have');\n  ")
					return
				}
				got := flatten(r)
				want := "4 4"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "6." + tn + ".7"
				r = db.Query("\n    SELECT rowid, highlight(y1, 0, '[', ']') FROM y1('have');\n  ")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n    SELECT rowid, highlight(y1, 0, '[', ']') FROM y1('have');\n  ")
					return
				}
				got := flatten(r)
				want := "4 which it hath been used to [have]"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "6." + tn + ".8"
				r = db.Query("\n    SELECT rowid, \n           highlight(y1, 0, '[', ']'),\n           snippet(y1, 0, '[', ']', '...', 10)\n    FROM y1('Liberties + Customs');\n  ")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n    SELECT rowid, \n           highlight(y1, 0, '[', ']'),\n           snippet(y1, 0, '[', ']', '...', 10)\n    FROM y1('Liberties + Customs');\n  ")
					return
				}
				got := flatten(r)
				want := "3 [Liberties and Customs] [Liberties and Customs]"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
		}
		db.Close()
		os.Remove("test.db")
		os.Remove("test.db-journal")
		os.Remove("test.db-wal")
		db, err = frigolite.Open("test.db")
		if err != nil { t.Fatal(err) }
		tcl_nullvalue = "{}" // fresh connection resets nullvalue
		{ // "6.0"
			_res = db.Exec("\n  CREATE VIRTUAL TABLE x1 USING fts5(x);\n")
			if _res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE x1 USING fts5(x);\n")
			}
		}
		{ // "6.1"
			_res = db.Exec("\n  INSERT INTO x1(rowid, x) VALUES(123, fts5_locale('en_AU', 'hello world'));\n")
			if _res.Error == nil || !strings.Contains(_res.Error.Error(), "fts5_locale() requires locale=1") {
				t.Errorf("expected error containing %q, got: %v\n  sql: %s", "fts5_locale() requires locale=1", resErrString(_res), "\n  INSERT INTO x1(rowid, x) VALUES(123, fts5_locale('en_AU', 'hello world'));\n")
			}
		}
		{ // "6.2"
			r = db.Query("\n  SELECT typeof( fts5_locale(NULL, 'xyz') ), typeof( fts5_locale('', 'abc') );\n")
			if r.Error != nil {
				t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT typeof( fts5_locale(NULL, 'xyz') ), typeof( fts5_locale('', 'abc') );\n")
				return
			}
			got := flatten(r)
			want := "text text"
			if got != want {
				t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
			}
		}
		db.Close()
		os.Remove("test.db")
		os.Remove("test.db-journal")
		os.Remove("test.db-wal")
		db, err = frigolite.Open("test.db")
		if err != nil { t.Fatal(err) }
		tcl_nullvalue = "{}" // fresh connection resets nullvalue
		// sqlite3_fts5_create_tokenizer -v2 db tcl tcl_create (unsupported command, not transpiled)
		{ // "7.1"
			_res = db.Exec("\n  CREATE TABLE t1(ii INTEGER PRIMARY KEY, bb BLOB, tt TEXT, locale TEXT);\n  CREATE VIEW v1 AS \n      SELECT ii AS rowid, bb, fts5_locale(locale, tt) AS tt FROM t1;\n\n  CREATE VIRTUAL TABLE ft USING fts5(\n      bb, tt, locale=1, tokenize=tcl, content=v1\n  );\n\n  INSERT INTO t1 VALUES(1, NULL, 'one two three', NULL);\n  INSERT INTO t1 VALUES(2, '7800616263', 'four five six', 'reverse');\n  INSERT INTO t1 VALUES(3, '000000007800616263', 'seven eight nine', 'second');\n")
			if _res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE TABLE t1(ii INTEGER PRIMARY KEY, bb BLOB, tt TEXT, locale TEXT);\n  CREATE VIEW v1 AS \n      SELECT ii AS rowid, bb, fts5_locale(locale, tt) AS tt FROM t1;\n\n  CREATE VIRTUAL TABLE ft USING fts5(\n      bb, tt, locale=1, tokenize=tcl, content=v1\n  );\n\n  INSERT INTO t1 VALUES(1, NULL, 'one two three', NULL);\n  INSERT INTO t1 VALUES(2, '7800616263', 'four five six', 'reverse');\n  INSERT INTO t1 VALUES(3, '000000007800616263', 'seven eight nine', 'second');\n")
			}
		}
		{ // "7.2"
			_res = db.Exec("\n  INSERT INTO ft(ft) VALUES('rebuild');\n  INSERT INTO ft(ft) VALUES('integrity-check');\n")
			if _res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO ft(ft) VALUES('rebuild');\n  INSERT INTO ft(ft) VALUES('integrity-check');\n")
			}
		}
		{ // "7.3"
			r = db.Query("\n  SELECT rowid, quote(bb), quote(tt) FROM ft\n")
			if r.Error != nil {
				t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, quote(bb), quote(tt) FROM ft\n")
				return
			}
			got := flatten(r)
			want := "1 NULL 'one two three' 2 '7800616263' 'four five six' 3 '000000007800616263' 'seven eight nine'"
			if got != want {
				t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
			}
		}
		{ // "7.4"
			r = db.Query(" SELECT rowid FROM ft('six'); ")
			if r.Error != nil {
				t.Errorf("query error: %v\n  sql: %s", r.Error, " SELECT rowid FROM ft('six'); ")
			}
		}
		{ // "7.5"
			r = db.Query(" SELECT rowid FROM ft(fts5_locale('reverse','six')); ")
			if r.Error != nil {
				t.Errorf("query error: %v\n  sql: %s", r.Error, " SELECT rowid FROM ft(fts5_locale('reverse','six')); ")
				return
			}
			got := flatten(r)
			want := "2"
			if got != want {
				t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
			}
		}
		// fts5_aux_test_functions db (unsupported command, not transpiled)
		{ // "7.6"
			r = db.Query("\n  SELECT fts5_test_columnlocale(ft) FROM ft;\n")
			if r.Error != nil {
				t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT fts5_test_columnlocale(ft) FROM ft;\n")
				return
			}
			got := flatten(r)
			want := "{} {} {} reverse {} second"
			if got != want {
				t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
			}
		}
		db.Close()
		os.Remove("test.db")
		os.Remove("test.db-journal")
		os.Remove("test.db-wal")
		db, err = frigolite.Open("test.db")
		if err != nil { t.Fatal(err) }
		tcl_nullvalue = "{}" // fresh connection resets nullvalue
		// sqlite3_fts5_create_tokenizer -v2 db tcl tcl_create (unsupported command, not transpiled)
		{ // "8.1"
			_res = db.Exec("\n  CREATE VIRTUAL TABLE ft USING fts5(tt, locale=1, tokenize=\"porter tcl\");\n  CREATE VIRTUAL TABLE vocab USING fts5vocab('ft', instance);\n\n  INSERT INTO ft(rowid, tt) VALUES\n    (111, fts5_locale('second', 'the porter tokenizer is a wrapper tokenizer')),\n    (222, fts5_locale('reverse', 'This value may also be set'));\n")
			if _res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE ft USING fts5(tt, locale=1, tokenize=\"porter tcl\");\n  CREATE VIRTUAL TABLE vocab USING fts5vocab('ft', instance);\n\n  INSERT INTO ft(rowid, tt) VALUES\n    (111, fts5_locale('second', 'the porter tokenizer is a wrapper tokenizer')),\n    (222, fts5_locale('reverse', 'This value may also be set'));\n")
			}
		}
		{ // "8.1"
			r = db.Query("\n  SELECT DISTINCT term FROM vocab ORDER BY 1\n")
			if r.Error != nil {
				t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT DISTINCT term FROM vocab ORDER BY 1\n")
				return
			}
			got := flatten(r)
			want := "a eb eulav osla sihT te the token yam"
			if got != want {
				t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
			}
		}
		// foreach {tn detail} "1 detail=full\n  2 detail=none\n  3 detail=column"
		_items1 := tclSplitList("1 detail=full\n  2 detail=none\n  3 detail=column")
		for _idx1 := 0; _idx1+2 <= len(_items1); _idx1 += 2 {
			tn := _items1[_idx1+0]
			_ = tn // suppress unused warning
			detail := _items1[_idx1+1]
			_ = detail // suppress unused warning
			_ = _idx1
				db.Close()
				os.Remove("test.db")
				os.Remove("test.db-journal")
				os.Remove("test.db-wal")
				db, err = frigolite.Open("test.db")
				if err != nil { t.Fatal(err) }
				tcl_nullvalue = "{}" // fresh connection resets nullvalue
				// sqlite3_fts5_create_tokenizer -v2 db tcl tcl_create (unsupported command, not transpiled)
				{ // "9." + tn + ".0"
					_res = db.Exec("\n    CREATE VIRTUAL TABLE ft USING fts5(tt, locale=1, tokenize=tcl, " + detail + ");\n  ")
					if _res.Error != nil {
						t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n    CREATE VIRTUAL TABLE ft USING fts5(tt, locale=1, tokenize=tcl, " + detail + ");\n  ")
					}
				}
				{ // "9." + tn + ".1"
					_res = db.Exec("\n    CREATE VIRTUAL TABLE vocab USING fts5vocab('ft', instance);\n    INSERT INTO ft(rowid, tt) VALUES\n      (-1, fts5_locale('second', 'it is an ancient mariner'));\n  ")
					if _res.Error != nil {
						t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n    CREATE VIRTUAL TABLE vocab USING fts5vocab('ft', instance);\n    INSERT INTO ft(rowid, tt) VALUES\n      (-1, fts5_locale('second', 'it is an ancient mariner'));\n  ")
					}
				}
				{ // "9." + tn + ".2"
					r = db.Query("\n    SELECT DISTINCT term FROM vocab \n  ")
					if r.Error != nil {
						t.Errorf("query error: %v\n  sql: %s", r.Error, "\n    SELECT DISTINCT term FROM vocab \n  ")
						return
					}
					got := flatten(r)
					want := "an it mariner"
					if got != want {
						t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
					}
				}
				{ // "9." + tn + ".3"
					r = db.Query("\n    SELECT highlight(ft, 0, '[', ']') FROM ft('mariner')\n  ")
					if r.Error != nil {
						t.Errorf("query error: %v\n  sql: %s", r.Error, "\n    SELECT highlight(ft, 0, '[', ']') FROM ft('mariner')\n  ")
						return
					}
					got := flatten(r)
					want := "it is an ancient [mariner]"
					if got != want {
						t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
					}
				}
			}
			// foreach_detail_mode $::testprefix {\n\n  reset_db\n  sqlite3_fts5_create_tokenizer -v...} (unsupported command, not transpiled)
			db.Close()
			os.Remove("test.db")
			os.Remove("test.db-journal")
			os.Remove("test.db-wal")
			db, err = frigolite.Open("test.db")
			if err != nil { t.Fatal(err) }
			tcl_nullvalue = "{}" // fresh connection resets nullvalue
			// sqlite3_fts5_create_tokenizer -v2 db tcl tcl_create (unsupported command, not transpiled)
			// fts5_aux_test_functions db (unsupported command, not transpiled)
			{ // "11.0"
				_res = db.Exec("\n  CREATE VIRTUAL TABLE x1 USING fts5(abc, locale=1);\n  INSERT INTO x1(rowid, abc) VALUES(123, fts5_locale('en_US', 'one two three'));\n")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE x1 USING fts5(abc, locale=1);\n  INSERT INTO x1(rowid, abc) VALUES(123, fts5_locale('en_US', 'one two three'));\n")
				}
			}
			{ // "11.1"
				_res = db.Exec("\n  SELECT fts5_columnlocale(x1, -1) FROM x1('two');\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "SQLITE_RANGE") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "SQLITE_RANGE", resErrString(_res), "\n  SELECT fts5_columnlocale(x1, -1) FROM x1('two');\n")
				}
			}
			{ // "11.2"
				_res = db.Exec("\n  SELECT fts5_columnlocale(x1, 1) FROM x1('two');\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "SQLITE_RANGE") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "SQLITE_RANGE", resErrString(_res), "\n  SELECT fts5_columnlocale(x1, 1) FROM x1('two');\n")
				}
			}
			db.Close()
			os.Remove("test.db")
			os.Remove("test.db-journal")
			os.Remove("test.db-wal")
			db, err = frigolite.Open("test.db")
			if err != nil { t.Fatal(err) }
			tcl_nullvalue = "{}" // fresh connection resets nullvalue
			{ // do_test "12.0"
				_rc := "0"
				{
					var _catchErr error
					// sqlite3_fts5_create_tokenizer -v2 -version 3 db tcl tcl_create (unsupported command, not transpiled)
					if _catchErr != nil { msg = _catchErr.Error() } else { msg = "" }
					if _catchErr != nil { _rc = "1" }
				}
				_list2 := tclList([]string{_rc, msg})
				_ = _list2
				_r = _list2
			}
			db.Close()
			os.Remove("test.db")
			os.Remove("test.db-journal")
			os.Remove("test.db-wal")
			db, err = frigolite.Open("test.db")
			if err != nil { t.Fatal(err) }
			tcl_nullvalue = "{}" // fresh connection resets nullvalue
			{ // "13.1.0"
				_res = db.Exec("\n  CREATE VIRTUAL TABLE nolocale USING fts5(a, b);\n  INSERT INTO nolocale VALUES('one two three', 'four five six');\n  INSERT INTO nolocale VALUES('three two one', 'seven eight nine');\n")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE nolocale USING fts5(a, b);\n  INSERT INTO nolocale VALUES('one two three', 'four five six');\n  INSERT INTO nolocale VALUES('three two one', 'seven eight nine');\n")
				}
			}
			{ // "13.1.1"
				r = db.Query("\n  SELECT fts5_get_locale(nolocale, 0) IS NULL FROM nolocale;\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT fts5_get_locale(nolocale, 0) IS NULL FROM nolocale;\n")
					return
				}
				got := flatten(r)
				want := "1 1"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "13.1.2"
				r = db.Query("\n  SELECT fts5_get_locale(nolocale, 1) IS NULL FROM nolocale('one + two');\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT fts5_get_locale(nolocale, 1) IS NULL FROM nolocale('one + two');\n")
					return
				}
				got := flatten(r)
				want := "1"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "13.1.3"
				r = db.Query("\n  SELECT fts5_get_locale(nolocale, 0) IS NULL FROM nolocale('one AND two');\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT fts5_get_locale(nolocale, 0) IS NULL FROM nolocale('one AND two');\n")
					return
				}
				got := flatten(r)
				want := "1 1"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "13.1.4"
				r = db.Query("\n  SELECT \n      fts5_get_locale(nolocale, 1) IS NULL \n  FROM nolocale('three AND two') ORDER BY rank\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT \n      fts5_get_locale(nolocale, 1) IS NULL \n  FROM nolocale('three AND two') ORDER BY rank\n")
					return
				}
				got := flatten(r)
				want := "1 1"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "13.1.5"
				_res = db.Exec("\n  SELECT fts5_get_locale(nolocale, 2) IS NULL FROM nolocale('three AND two');\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "column index out of range") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "column index out of range", resErrString(_res), "\n  SELECT fts5_get_locale(nolocale, 2) IS NULL FROM nolocale('three AND two');\n")
				}
			}
			{ // "13.1.6"
				_res = db.Exec("\n  SELECT fts5_get_locale(nolocale, -1) IS NULL FROM nolocale('three AND two');\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "column index out of range") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "column index out of range", resErrString(_res), "\n  SELECT fts5_get_locale(nolocale, -1) IS NULL FROM nolocale('three AND two');\n")
				}
			}
			{ // "13.1.7"
				_res = db.Exec("\n  SELECT fts5_get_locale(nolocale) IS NULL FROM nolocale('three AND two');\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "wrong number of arguments to function fts5_get_locale()") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "wrong number of arguments to function fts5_get_locale()", resErrString(_res), "\n  SELECT fts5_get_locale(nolocale) IS NULL FROM nolocale('three AND two');\n")
				}
			}
			{ // "13.1.8"
				_res = db.Exec("\n  SELECT fts5_get_locale(nolocale, 0, 0) IS NULL FROM nolocale('three AND two');\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "wrong number of arguments to function fts5_get_locale()") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "wrong number of arguments to function fts5_get_locale()", resErrString(_res), "\n  SELECT fts5_get_locale(nolocale, 0, 0) IS NULL FROM nolocale('three AND two');\n")
				}
			}
			{ // "13.1.9"
				_res = db.Exec("\n  SELECT fts5_get_locale(nolocale, 'text') FROM nolocale('three AND two');\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "non-integer argument passed to function fts5_get_locale()") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "non-integer argument passed to function fts5_get_locale()", resErrString(_res), "\n  SELECT fts5_get_locale(nolocale, 'text') FROM nolocale('three AND two');\n")
				}
			}
			{ // "13.2.0"
				_res = db.Exec("\n  CREATE VIRTUAL TABLE ft USING fts5(a, b, locale=1);\n  INSERT INTO ft VALUES(\n      fts5_locale('th_TH', 'one two three'), 'four five six seven'\n  );\n  INSERT INTO ft VALUES(\n      'three two one', fts5_locale('en_AU', 'seven eight nine')\n  );\n")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE ft USING fts5(a, b, locale=1);\n  INSERT INTO ft VALUES(\n      fts5_locale('th_TH', 'one two three'), 'four five six seven'\n  );\n  INSERT INTO ft VALUES(\n      'three two one', fts5_locale('en_AU', 'seven eight nine')\n  );\n")
				}
			}
			{ // "13.2.1"
				r = db.Query("\n  SELECT quote(fts5_get_locale(ft, 0)), quote(fts5_get_locale(ft, 1)) FROM ft\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT quote(fts5_get_locale(ft, 0)), quote(fts5_get_locale(ft, 1)) FROM ft\n")
					return
				}
				got := flatten(r)
				want := "'th_TH' NULL NULL 'en_AU'"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "13.2.2"
				r = db.Query("\n  SELECT \n    quote(fts5_get_locale(ft, 0)), quote(fts5_get_locale(ft, 1)) \n  FROM ft('one AND three')\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT \n    quote(fts5_get_locale(ft, 0)), quote(fts5_get_locale(ft, 1)) \n  FROM ft('one AND three')\n")
					return
				}
				got := flatten(r)
				want := "'th_TH' NULL NULL 'en_AU'"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "13.2.3"
				r = db.Query("\n  SELECT \n    quote(fts5_get_locale(ft, 0)), quote(fts5_get_locale(ft, 1)) \n  FROM ft('one AND three') ORDER BY rank\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT \n    quote(fts5_get_locale(ft, 0)), quote(fts5_get_locale(ft, 1)) \n  FROM ft('one AND three') ORDER BY rank\n")
					return
				}
				got := flatten(r)
				want := "NULL 'en_AU' 'th_TH' NULL"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "13.2.4"
				r = db.Query("\n  SELECT \n    quote(fts5_get_locale(ft, 0)), quote(fts5_get_locale(ft, 1)) \n  FROM ft('one AND three') ORDER BY rowid\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT \n    quote(fts5_get_locale(ft, 0)), quote(fts5_get_locale(ft, 1)) \n  FROM ft('one AND three') ORDER BY rowid\n")
					return
				}
				got := flatten(r)
				want := "'th_TH' NULL NULL 'en_AU'"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "13.2.5"
				r = db.Query("\n  SELECT \n    quote(fts5_get_locale(ft, '0')), quote(fts5_get_locale(ft, 1)) \n  FROM ft('one AND three') ORDER BY rowid\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT \n    quote(fts5_get_locale(ft, '0')), quote(fts5_get_locale(ft, 1)) \n  FROM ft('one AND three') ORDER BY rowid\n")
					return
				}
				got := flatten(r)
				want := "'th_TH' NULL NULL 'en_AU'"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "13.2.6"
				_res = db.Exec("\n  SELECT \n    quote(fts5_get_locale(ft, '0.0')), quote(fts5_get_locale(ft, 1)) \n  FROM ft('one AND three') ORDER BY rowid\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "non-integer argument passed to function fts5_get_locale()") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "non-integer argument passed to function fts5_get_locale()", resErrString(_res), "\n  SELECT \n    quote(fts5_get_locale(ft, '0.0')), quote(fts5_get_locale(ft, 1)) \n  FROM ft('one AND three') ORDER BY rowid\n")
				}
			}
			{ // "13.2.7"
				_res = db.Exec("\n  SELECT \n    quote(fts5_get_locale(ft, 0.0)), quote(fts5_get_locale(ft, 1)) \n  FROM ft('one AND three') ORDER BY rowid\n")
				if _res.Error == nil || !strings.Contains(_res.Error.Error(), "non-integer argument passed to function fts5_get_locale()") {
					t.Errorf("expected error containing %q, got: %v\n  sql: %s", "non-integer argument passed to function fts5_get_locale()", resErrString(_res), "\n  SELECT \n    quote(fts5_get_locale(ft, 0.0)), quote(fts5_get_locale(ft, 1)) \n  FROM ft('one AND three') ORDER BY rowid\n")
				}
			}
			db.Close()
			os.Remove("test.db")
			os.Remove("test.db-journal")
			os.Remove("test.db-wal")
			db, err = frigolite.Open("test.db")
			if err != nil { t.Fatal(err) }
			tcl_nullvalue = "{}" // fresh connection resets nullvalue
			{ // "14.1"
				_res = db.Exec("\n  CREATE VIRTUAL TABLE ft USING fts5(a, b, locale=1);\n")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE ft USING fts5(a, b, locale=1);\n")
				}
			}
			{ // "14.2"
				_res = db.Exec("\n  INSERT INTO ft VALUES('hello', 'world');\n")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO ft VALUES('hello', 'world');\n")
				}
			}
			{ // "14.3"
				_res = db.Exec("\n  UPDATE ft SET b = fts5_locale('en_AU', 'world');\n")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  UPDATE ft SET b = fts5_locale('en_AU', 'world');\n")
				}
			}
			{ // "14.4"
				_res = db.Exec("\n  INSERT INTO ft VALUES(X'abcd', X'1234');\n")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO ft VALUES(X'abcd', X'1234');\n")
				}
			}
			{ // "14.5"
				r = db.Query("\n  SELECT quote(a), quote(b) FROM ft\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT quote(a), quote(b) FROM ft\n")
					return
				}
				got := flatten(r)
				want := "'hello' 'world' X'ABCD' X'1234'"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "14.6"
				r = db.Query("\n  DELETE FROM ft;\n  INSERT INTO ft VALUES(NULL, 'null');\n  INSERT INTO ft VALUES(123, 'int');\n  INSERT INTO ft VALUES(345.0, 'real');\n  INSERT INTO ft VALUES('abc', 'text');\n  INSERT INTO ft VALUES(fts5_locale('abc', 'def'), 'text');\n\n  SELECT a, typeof(a), b FROM ft\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  DELETE FROM ft;\n  INSERT INTO ft VALUES(NULL, 'null');\n  INSERT INTO ft VALUES(123, 'int');\n  INSERT INTO ft VALUES(345.0, 'real');\n  INSERT INTO ft VALUES('abc', 'text');\n  INSERT INTO ft VALUES(fts5_locale('abc', 'def'), 'text');\n\n  SELECT a, typeof(a), b FROM ft\n")
					return
				}
				got := flatten(r)
				want := "{} null null 123 integer int 345.0 real real abc text text def text text"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "14.7"
				r = db.Query("\n  SELECT quote(c0), typeof(c0) FROM ft_content\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT quote(c0), typeof(c0) FROM ft_content\n")
					return
				}
				got := flatten(r)
				want := "NULL null 123 integer 345.0 real 'abc' text 'def' text"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			db.Close()
			os.Remove("test.db")
			os.Remove("test.db-journal")
			os.Remove("test.db-wal")
			db, err = frigolite.Open("test.db")
			if err != nil { t.Fatal(err) }
			tcl_nullvalue = "{}" // fresh connection resets nullvalue
			// sqlite3_fts5_create_tokenizer -v2 db tcl tcl_create (unsupported command, not transpiled)
			// fts5_aux_test_functions db (unsupported command, not transpiled)
			{ // "15.1"
				_res = db.Exec("\n  CREATE VIRTUAL TABLE ft USING fts5(a, b UNINDEXED, c, locale=1, tokenize=tcl);\n")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE ft USING fts5(a, b UNINDEXED, c, locale=1, tokenize=tcl);\n")
				}
			}
			{ // "15.2"
				_res = db.Exec("\n  INSERT INTO ft VALUES('one', 'two', 'three');\n  INSERT INTO ft VALUES('one', 'two', fts5_locale('loc', 'three'));\n")
				if _res.Error != nil {
					t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO ft VALUES('one', 'two', 'three');\n  INSERT INTO ft VALUES('one', 'two', fts5_locale('loc', 'three'));\n")
				}
			}
			{ // "15.3"
				r = db.Query("\n  SELECT c2, l2 FROM ft_content\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT c2, l2 FROM ft_content\n")
					return
				}
				got := flatten(r)
				want := "three {} three loc"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
			{ // "15.4"
				r = db.Query("\n  SELECT c, fts5_columnlocale(ft, 2) FROM ft\n")
				if r.Error != nil {
					t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT c, fts5_columnlocale(ft, 2) FROM ft\n")
					return
				}
				got := flatten(r)
				want := "three {} three loc"
				if got != want {
					t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
				}
			}
}
