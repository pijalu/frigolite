// Code generated by tcl2go; DO NOT EDIT.
//go:build testgen
// +build testgen

package fts5origintext2

import (
"github.com/pijalu/frigolite"
"github.com/pijalu/frigolite/internal/vtab"
"os"
"testing"
)

func Test_fts5origintext2(t *testing.T) {
	if err := os.Chdir(t.TempDir()); err != nil { t.Fatal(err) }
	db, err := frigolite.Open("test.db")
	if err != nil {
		t.Fatal(err)
	}
	defer db.Close()

	var _res *frigolite.Result
	var r *frigolite.Result
	var msg string
	var _r string
	var _berr error
	_ = _berr // suppress unused warning
	_ = msg // suppress unused warning
	_ = _res // suppress unused warning
	_ = r    // suppress unused warning
	_ = _r   // suppress unused warning
	tcl_nullvalue = "{}" // default NULL rendering

	var db1 *frigolite.DB
	_ = db1
	var db2 *frigolite.DB
	_ = db2
	var db3 *frigolite.DB
	_ = db3
	var db4 *frigolite.DB
	_ = db4
	var db5 *frigolite.DB
	_ = db5
	var db6 *frigolite.DB
	_ = db6
	var db7 *frigolite.DB
	_ = db7
	var db8 *frigolite.DB
	_ = db8
	var db9 *frigolite.DB
	_ = db9

	var testprefix string
	_ = testprefix // pre-declared from TCL source
	var ret string
	_ = ret // pre-declared from TCL source
	var iTok string
	_ = iTok // pre-declared from TCL source
	var txt string
	_ = txt // pre-declared from TCL source
	var cmd string
	_ = cmd // pre-declared from TCL source

	vtab.TclVarSet("testprefix", "", "fts5origintext2")
	testprefix = "fts5origintext2"
	_ = testprefix // suppress unused warning
	// sqlite3_fts5_register_origintext db (unsupported command, not transpiled)
	{ // "1.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE ft USING fts5(\n      x, tokenize=\"origintext unicode61\", tokendata=1\n  );\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE ft USING fts5(\n      x, tokenize=\"origintext unicode61\", tokendata=1\n  );\n")
		}
	}
	{ // "1.1"
		_res = db.Exec("\n  BEGIN;\n  INSERT INTO ft VALUES('Hello');\n  INSERT INTO ft VALUES('hello');\n  INSERT INTO ft VALUES('HELLO');\n  INSERT INTO ft VALUES('today');\n  INSERT INTO ft VALUES('today');\n  INSERT INTO ft VALUES('today');\n  INSERT INTO ft VALUES('World');\n  INSERT INTO ft VALUES('world');\n  INSERT INTO ft VALUES('WORLD');\n  COMMIT;\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  BEGIN;\n  INSERT INTO ft VALUES('Hello');\n  INSERT INTO ft VALUES('hello');\n  INSERT INTO ft VALUES('HELLO');\n  INSERT INTO ft VALUES('today');\n  INSERT INTO ft VALUES('today');\n  INSERT INTO ft VALUES('today');\n  INSERT INTO ft VALUES('World');\n  INSERT INTO ft VALUES('world');\n  INSERT INTO ft VALUES('WORLD');\n  COMMIT;\n")
		}
	}
	{ // "1.2"
		r = db.Query(" SELECT rowid FROM ft('hello'); ")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, " SELECT rowid FROM ft('hello'); ")
			return
		}
		got := flatten(r)
		want := "1 2 3"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.3"
		r = db.Query(" SELECT rowid FROM ft('today'); ")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, " SELECT rowid FROM ft('today'); ")
			return
		}
		got := flatten(r)
		want := "4 5 6"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.4"
		r = db.Query(" SELECT rowid FROM ft('world'); ")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, " SELECT rowid FROM ft('world'); ")
			return
		}
		got := flatten(r)
		want := "7 8 9"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.5"
		r = db.Query("\n  SELECT count(*) FROM ft_data\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT count(*) FROM ft_data\n")
			return
		}
		got := flatten(r)
		want := "3"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.6"
		_res = db.Exec("\n  DELETE FROM ft;\n  INSERT INTO ft(ft, rank) VALUES('pgsz', 64);\n  BEGIN;\n    WITH s(i) AS (\n      SELECT 1 UNION ALL SELECT i+1 FROM s WHERE i<100\n    )\n    INSERT INTO ft SELECT 'Hello Hello Hello Hello Hello Hello Hello' FROM s;\n    INSERT INTO ft VALUES ('hELLO hELLO hELLO');\n    INSERT INTO ft VALUES('today today today today today today today');\n    INSERT INTO ft VALUES('today today today today today today today');\n    INSERT INTO ft VALUES('today today today today today today today');\n    INSERT INTO ft VALUES('today today today today today today today');\n    INSERT INTO ft VALUES('today today today today today today today');\n    INSERT INTO ft VALUES('today today today today today today today');\n    INSERT INTO ft VALUES('World World World World World World World');\n    INSERT INTO ft VALUES('world world world world world world world');\n    INSERT INTO ft VALUES('WORLD WORLD WORLD WORLD WORLD WORLD WORLD');\n    INSERT INTO ft VALUES('World World World World World World World');\n    INSERT INTO ft VALUES('world world world world world world world');\n    INSERT INTO ft VALUES('WORLD WORLD WORLD WORLD WORLD WORLD WORLD');\n  COMMIT;\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  DELETE FROM ft;\n  INSERT INTO ft(ft, rank) VALUES('pgsz', 64);\n  BEGIN;\n    WITH s(i) AS (\n      SELECT 1 UNION ALL SELECT i+1 FROM s WHERE i<100\n    )\n    INSERT INTO ft SELECT 'Hello Hello Hello Hello Hello Hello Hello' FROM s;\n    INSERT INTO ft VALUES ('hELLO hELLO hELLO');\n    INSERT INTO ft VALUES('today today today today today today today');\n    INSERT INTO ft VALUES('today today today today today today today');\n    INSERT INTO ft VALUES('today today today today today today today');\n    INSERT INTO ft VALUES('today today today today today today today');\n    INSERT INTO ft VALUES('today today today today today today today');\n    INSERT INTO ft VALUES('today today today today today today today');\n    INSERT INTO ft VALUES('World World World World World World World');\n    INSERT INTO ft VALUES('world world world world world world world');\n    INSERT INTO ft VALUES('WORLD WORLD WORLD WORLD WORLD WORLD WORLD');\n    INSERT INTO ft VALUES('World World World World World World World');\n    INSERT INTO ft VALUES('world world world world world world world');\n    INSERT INTO ft VALUES('WORLD WORLD WORLD WORLD WORLD WORLD WORLD');\n  COMMIT;\n")
		}
	}
	{ // "1.7"
		r = db.Query("\n  SELECT count(*) FROM ft_data;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT count(*) FROM ft_data;\n")
			return
		}
		got := flatten(r)
		want := "23"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.8"
		r = db.Query(" SELECT rowid FROM ft('hello') WHERE rowid>100; ")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, " SELECT rowid FROM ft('hello') WHERE rowid>100; ")
			return
		}
		got := flatten(r)
		want := "101"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.9"
		r = db.Query("\n  DELETE FROM ft;\n  INSERT INTO ft(ft) VALUES('optimize');\n  SELECT count(*) FROM ft_data;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  DELETE FROM ft;\n  INSERT INTO ft(ft) VALUES('optimize');\n  SELECT count(*) FROM ft_data;\n")
			return
		}
		got := flatten(r)
		want := "2"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.10"
		_res = db.Exec("\n  BEGIN;\n    INSERT INTO ft VALUES('Hello');\n    INSERT INTO ft VALUES('hello');\n    INSERT INTO ft VALUES('HELLO');\n    INSERT INTO ft VALUES('today');\n    INSERT INTO ft VALUES('today');\n    INSERT INTO ft VALUES('today');\n    INSERT INTO ft VALUES('World');\n    INSERT INTO ft VALUES('world');\n    INSERT INTO ft VALUES('WORLD');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  BEGIN;\n    INSERT INTO ft VALUES('Hello');\n    INSERT INTO ft VALUES('hello');\n    INSERT INTO ft VALUES('HELLO');\n    INSERT INTO ft VALUES('today');\n    INSERT INTO ft VALUES('today');\n    INSERT INTO ft VALUES('today');\n    INSERT INTO ft VALUES('World');\n    INSERT INTO ft VALUES('world');\n    INSERT INTO ft VALUES('WORLD');\n")
		}
	}
	{ // "1.11"
		r = db.Query(" SELECT rowid FROM ft('hello'); ")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, " SELECT rowid FROM ft('hello'); ")
			return
		}
		got := flatten(r)
		want := "1 2 3"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.12"
		r = db.Query(" SELECT rowid FROM ft('today'); ")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, " SELECT rowid FROM ft('today'); ")
			return
		}
		got := flatten(r)
		want := "4 5 6"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.13"
		r = db.Query(" SELECT rowid FROM ft('world'); ")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, " SELECT rowid FROM ft('world'); ")
			return
		}
		got := flatten(r)
		want := "7 8 9"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.14"
		r = db.Query(" SELECT rowid FROM ft('hello') ORDER BY rank; ")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, " SELECT rowid FROM ft('hello') ORDER BY rank; ")
			return
		}
		got := flatten(r)
		want := "1 2 3"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	// sqlite3_fts5_register_origintext db (unsupported command, not transpiled)
	// proc definition (not transpiled)
	// sqlite3_fts5_create_function db tokens tokens (unsupported command, not transpiled)
	{ // "2.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE x1 USING fts5(\n    v, tokenize=\"origintext unicode61\", tokendata=1, detail=none\n  );\n\n  INSERT INTO x1 VALUES('xxx Xxx XXX yyy YYY yyy');\n  INSERT INTO x1 VALUES('xxx yyy xxx yyy yyy yyy');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE x1 USING fts5(\n    v, tokenize=\"origintext unicode61\", tokendata=1, detail=none\n  );\n\n  INSERT INTO x1 VALUES('xxx Xxx XXX yyy YYY yyy');\n  INSERT INTO x1 VALUES('xxx yyy xxx yyy yyy yyy');\n")
		}
	}
	{ // "2.1"
		r = db.Query("\n  SELECT tokens(x1) FROM x1('xxx');\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT tokens(x1) FROM x1('xxx');\n")
			return
		}
		got := flatten(r)
		want := "xxx xxx.Xxx xxx.XXX xxx xxx"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "2.2"
		_res = db.Exec("\n  UPDATE x1_content SET c0 = 'xxx xxX xxx yyy yyy yyy' WHERE id=1;\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  UPDATE x1_content SET c0 = 'xxx xxX xxx yyy yyy yyy' WHERE id=1;\n")
		}
	}
	{ // "2.3"
		r = db.Query("\n  SELECT tokens(x1) FROM x1('xxx');\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT tokens(x1) FROM x1('xxx');\n")
			return
		}
		got := flatten(r)
		want := "xxx {} xxx xxx xxx"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
}
