// Code generated by tcl2go; DO NOT EDIT.
//go:build testgen
// +build testgen

package fts5unindexed2

import (
"github.com/pijalu/frigolite"
"github.com/pijalu/frigolite/internal/vtab"
"os"
"strings"
"testing"
)

func Test_fts5unindexed2(t *testing.T) {
	if err := os.Chdir(t.TempDir()); err != nil { t.Fatal(err) }
	db, err := frigolite.Open("test.db")
	if err != nil {
		t.Fatal(err)
	}
	defer db.Close()

	var _res *frigolite.Result
	var r *frigolite.Result
	var msg string
	var _r string
	var _berr error
	_ = _berr // suppress unused warning
	_ = msg // suppress unused warning
	_ = _res // suppress unused warning
	_ = r    // suppress unused warning
	_ = _r   // suppress unused warning
	tcl_nullvalue = "{}" // default NULL rendering

	var db1 *frigolite.DB
	_ = db1
	var db2 *frigolite.DB
	_ = db2
	var db3 *frigolite.DB
	_ = db3
	var db4 *frigolite.DB
	_ = db4
	var db5 *frigolite.DB
	_ = db5
	var db6 *frigolite.DB
	_ = db6
	var db7 *frigolite.DB
	_ = db7
	var db8 *frigolite.DB
	_ = db8
	var db9 *frigolite.DB
	_ = db9

	var testprefix string
	_ = testprefix // pre-declared from TCL source

	vtab.TclVarSet("testprefix", "", "fts5unindexed2")
	testprefix = "fts5unindexed2"
	_ = testprefix // suppress unused warning
	{ // "1.1"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE t1 USING fts5(\n      a, b UNINDEXED, content=, contentless_unindexed=1\n  );\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE t1 USING fts5(\n      a, b UNINDEXED, content=, contentless_unindexed=1\n  );\n")
		}
	}
	{ // "1.2"
		_res = db.Exec("\n  INSERT INTO t1 VALUES('abc def', 'ghi jkl');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO t1 VALUES('abc def', 'ghi jkl');\n")
		}
	}
	{ // "1.3"
		r = db.Query("\n  SELECT rowid, a, b FROM t1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, a, b FROM t1\n")
			return
		}
		got := flatten(r)
		want := "1 {} ghi jkl"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.4"
		_res = db.Exec("\n  INSERT INTO t1(rowid, a, b) VALUES(11, 'hello world', 'one two three');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO t1(rowid, a, b) VALUES(11, 'hello world', 'one two three');\n")
		}
	}
	{ // "1.5"
		_res = db.Exec("\n  INSERT INTO t1(t1, rowid, a, b) VALUES('delete', 1, 'abc def', 'ghi jkl');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO t1(t1, rowid, a, b) VALUES('delete', 1, 'abc def', 'ghi jkl');\n")
		}
	}
	{ // "1.6"
		r = db.Query("\n  SELECT rowid, a, b FROM t1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, a, b FROM t1\n")
			return
		}
		got := flatten(r)
		want := "11 {} one two three"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.7"
		r = db.Query("\n  PRAGMA integrity_check\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  PRAGMA integrity_check\n")
			return
		}
		got := flatten(r)
		want := "ok"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.8"
		_res = db.Exec("\n  INSERT INTO t1(rowid, a, b) VALUES(12, 'abc def', 'ghi jkl');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO t1(rowid, a, b) VALUES(12, 'abc def', 'ghi jkl');\n")
		}
	}
	{ // "1.9"
		r = db.Query("\n  SELECT rowid, a, b FROM t1('def')\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, a, b FROM t1('def')\n")
			return
		}
		got := flatten(r)
		want := "12 {} ghi jkl"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.10"
		r = db.Query("\n  SELECT rowid, a, b FROM t1('def OR hello') ORDER BY rank\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, a, b FROM t1('def OR hello') ORDER BY rank\n")
			return
		}
		got := flatten(r)
		want := "11 {} one two three 12 {} ghi jkl"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.11"
		r = db.Query("\n  SELECT rowid, a, b FROM t1 WHERE rowid=11\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, a, b FROM t1 WHERE rowid=11\n")
			return
		}
		got := flatten(r)
		want := "11 {} one two three"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.12"
		r = db.Query("\n  SELECT rowid, a, b FROM t1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, a, b FROM t1\n")
			return
		}
		got := flatten(r)
		want := "11 {} one two three 12 {} ghi jkl"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	// fts5_aux_test_functions db (unsupported command, not transpiled)
	{ // "1.12.2"
		r = db.Query("\n  SELECT rowid, fts5_test_columntext(t1) FROM t1('def OR hello')\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, fts5_test_columntext(t1) FROM t1('def OR hello')\n")
			return
		}
		got := flatten(r)
		want := "11 {} {one two three} 12 {} {ghi jkl}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.13"
		_res = db.Exec("\n  INSERT INTO t1(t1) VALUES('delete-all');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO t1(t1) VALUES('delete-all');\n")
		}
	}
	{ // "1.14"
		r = db.Query("\n  SELECT rowid, a, b FROM t1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, a, b FROM t1\n")
		}
	}
	{ // "1.15"
		r = db.Query("\n  PRAGMA integrity_check\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  PRAGMA integrity_check\n")
			return
		}
		got := flatten(r)
		want := "ok"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "2.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE t4 USING fts5(\n      x, y UNINDEXED, z, columnsize=0, content='', contentless_unindexed=1\n  );\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE t4 USING fts5(\n      x, y UNINDEXED, z, columnsize=0, content='', contentless_unindexed=1\n  );\n")
		}
	}
	{ // "2.1"
		_res = db.Exec("\n  INSERT INTO t4(rowid, x, y, z) VALUES(1, 'a a', 'b b b', 'c');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO t4(rowid, x, y, z) VALUES(1, 'a a', 'b b b', 'c');\n")
		}
	}
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "3.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE x1 USING fts5(\n      a UNINDEXED, b, c UNINDEXED, d, content=, contentless_delete=1,\n      contentless_unindexed=1\n  );\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE x1 USING fts5(\n      a UNINDEXED, b, c UNINDEXED, d, content=, contentless_delete=1,\n      contentless_unindexed=1\n  );\n")
		}
	}
	{ // "3.1"
		_res = db.Exec("\n  INSERT INTO x1(rowid, a, b, c, d) VALUES(131, 'aaa', 'bbb', 'ccc', 'ddd');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO x1(rowid, a, b, c, d) VALUES(131, 'aaa', 'bbb', 'ccc', 'ddd');\n")
		}
	}
	{ // "3.2"
		r = db.Query("\n  SELECT * FROM x1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT * FROM x1\n")
			return
		}
		got := flatten(r)
		want := "aaa {} ccc {}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "3.3"
		_res = db.Exec("\n  INSERT INTO x1(rowid, a, b, c, d) VALUES(1000, 'AAA', 'BBB', 'CCC', 'DDD');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  INSERT INTO x1(rowid, a, b, c, d) VALUES(1000, 'AAA', 'BBB', 'CCC', 'DDD');\n")
		}
	}
	{ // "3.4"
		r = db.Query("\n  SELECT rowid, * FROM x1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, * FROM x1\n")
			return
		}
		got := flatten(r)
		want := "131 aaa {} ccc {} 1000 AAA {} CCC {}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "3.5"
		r = db.Query("\n  DELETE FROM x1 WHERE rowid=131;\n  SELECT rowid, * FROM x1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  DELETE FROM x1 WHERE rowid=131;\n  SELECT rowid, * FROM x1\n")
			return
		}
		got := flatten(r)
		want := "1000 AAA {} CCC {}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "3.6"
		r = db.Query("\n  INSERT INTO x1(rowid, a, b, c, d) VALUES(112, 'aaa', 'bbb', 'ccc', 'ddd');\n  SELECT rowid, * FROM x1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  INSERT INTO x1(rowid, a, b, c, d) VALUES(112, 'aaa', 'bbb', 'ccc', 'ddd');\n  SELECT rowid, * FROM x1\n")
			return
		}
		got := flatten(r)
		want := "112 aaa {} ccc {} 1000 AAA {} CCC {}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "3.7"
		_res = db.Exec("\n  UPDATE x1 SET b='hello', d='world', rowid=1120 WHERE rowid=112\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  UPDATE x1 SET b='hello', d='world', rowid=1120 WHERE rowid=112\n")
		}
	}
	{ // "3.8"
		r = db.Query("\n  SELECT rowid, * FROM x1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, * FROM x1\n")
			return
		}
		got := flatten(r)
		want := "1000 AAA {} CCC {} 1120 aaa {} ccc {}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "3.9"
		r = db.Query("\n  SELECT rowid, * FROM x1('hello');\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, * FROM x1('hello');\n")
			return
		}
		got := flatten(r)
		want := "1120 aaa {} ccc {}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "3.9"
		r = db.Query("\n  SELECT rowid, * FROM x1('bbb');\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, * FROM x1('bbb');\n")
			return
		}
		got := flatten(r)
		want := "1000 AAA {} CCC {}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	// fts5_aux_test_functions db (unsupported command, not transpiled)
	{ // "3.10"
		r = db.Query("\n  SELECT rowid, fts5_test_columntext(x1) FROM x1('b*')\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, fts5_test_columntext(x1) FROM x1('b*')\n")
			return
		}
		got := flatten(r)
		want := "1000 AAA {} CCC {}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "4.0"
		r = db.Query("\n  CREATE VIRTUAL TABLE ft USING fts5(a, b, c UNINDEXED, content='');\n  INSERT INTO ft VALUES('one', 'two', 'three');\n  SELECT rowid, * FROM ft;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  CREATE VIRTUAL TABLE ft USING fts5(a, b, c UNINDEXED, content='');\n  INSERT INTO ft VALUES('one', 'two', 'three');\n  SELECT rowid, * FROM ft;\n")
			return
		}
		got := flatten(r)
		want := "1 {} {} {}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "4.1"
		r = db.Query("\n  SELECT name FROM sqlite_schema ORDER BY 1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT name FROM sqlite_schema ORDER BY 1\n")
			return
		}
		got := flatten(r)
		want := "ft ft_config ft_data ft_docsize ft_idx"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "4.2"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE ft2 USING fts5(\n      a, b, c UNINDEXED, contentless_unindexed=1\n  );\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "contentless_unindexed=1 requires a contentless table") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "contentless_unindexed=1 requires a contentless table", resErrString(_res), "\n  CREATE VIRTUAL TABLE ft2 USING fts5(\n      a, b, c UNINDEXED, contentless_unindexed=1\n  );\n")
		}
	}
	{ // "4.3"
		_res = db.Exec("\n  DELETE FROM ft WHERE rowid=1\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "cannot DELETE from contentless fts5 table: ft") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "cannot DELETE from contentless fts5 table: ft", resErrString(_res), "\n  DELETE FROM ft WHERE rowid=1\n")
		}
	}
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "5.0"
		r = db.Query("\n  CREATE VIRTUAL TABLE ft USING fts5(\n      a, b UNINDEXED, c, content='', contentless_unindexed=1\n  );\n  INSERT INTO ft VALUES('one', 'two', 'three');\n  INSERT INTO ft VALUES('four', 'five', 'six');\n  INSERT INTO ft VALUES('seven', 'eight', 'nine');\n  SELECT rowid, * FROM ft;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  CREATE VIRTUAL TABLE ft USING fts5(\n      a, b UNINDEXED, c, content='', contentless_unindexed=1\n  );\n  INSERT INTO ft VALUES('one', 'two', 'three');\n  INSERT INTO ft VALUES('four', 'five', 'six');\n  INSERT INTO ft VALUES('seven', 'eight', 'nine');\n  SELECT rowid, * FROM ft;\n")
			return
		}
		got := flatten(r)
		want := "1 {} two {} 2 {} five {} 3 {} eight {}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "5.1"
		r = db.Query("\n  PRAGMA integrity_check\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  PRAGMA integrity_check\n")
			return
		}
		got := flatten(r)
		want := "ok"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "5.2"
		_res = db.Exec("\n  DELETE FROM ft WHERE rowid=2\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "cannot DELETE from contentless fts5 table: ft") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "cannot DELETE from contentless fts5 table: ft", resErrString(_res), "\n  DELETE FROM ft WHERE rowid=2\n")
		}
	}
	{ // "5.3"
		r = db.Query("\n  SELECT rowid, * FROM ft('six')\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, * FROM ft('six')\n")
			return
		}
		got := flatten(r)
		want := "2 {} five {}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "5.4"
		_res = db.Exec("\n  UPDATE ft SET a='x', b='y', c='z' WHERE rowid=3\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "cannot UPDATE contentless fts5 table: ft") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "cannot UPDATE contentless fts5 table: ft", resErrString(_res), "\n  UPDATE ft SET a='x', b='y', c='z' WHERE rowid=3\n")
		}
	}
	// fts5_aux_test_functions db (unsupported command, not transpiled)
	{ // "5.5"
		r = db.Query("\n  SELECT fts5_test_columntext(ft) FROM ft WHERE rowid=3\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT fts5_test_columntext(ft) FROM ft WHERE rowid=3\n")
			return
		}
		got := flatten(r)
		want := "{} eight {}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "5.6"
		r = db.Query("\n  SELECT fts5_test_columntext(ft) FROM ft('three');\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT fts5_test_columntext(ft) FROM ft('three');\n")
			return
		}
		got := flatten(r)
		want := "{} two {}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	db.Close()
	os.Remove("test.db")
	os.Remove("test.db-journal")
	os.Remove("test.db-wal")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "6.0"
		_res = db.Exec("\n  CREATE VIRTUAL TABLE ft1 USING fts5(a, b UNINDEXED, c UNINDEXED, d,\n      contentless_unindexed=1, content=''\n  );\n\n  INSERT INTO ft1(rowid, a, b, c, d) VALUES\n      (100, 'x y', 'b1', 'c1', 'a b'),\n      (200, 'c d', 'b2', 'c2', 'a b'),\n      (300, 'e f', 'b3', 'c3', 'a b');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  CREATE VIRTUAL TABLE ft1 USING fts5(a, b UNINDEXED, c UNINDEXED, d,\n      contentless_unindexed=1, content=''\n  );\n\n  INSERT INTO ft1(rowid, a, b, c, d) VALUES\n      (100, 'x y', 'b1', 'c1', 'a b'),\n      (200, 'c d', 'b2', 'c2', 'a b'),\n      (300, 'e f', 'b3', 'c3', 'a b');\n")
		}
	}
	{ // "6.1"
		_res = db.Exec("\n  UPDATE ft1 SET b='b1.1', c='c1.1' WHERE rowid=100;\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  UPDATE ft1 SET b='b1.1', c='c1.1' WHERE rowid=100;\n")
		}
	}
	{ // "6.2"
		_res = db.Exec("\n  UPDATE ft1 SET b='b2.1' WHERE rowid=200;\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  UPDATE ft1 SET b='b2.1' WHERE rowid=200;\n")
		}
	}
	{ // "6.3"
		_res = db.Exec("\n  UPDATE ft1 SET c='c3.1' WHERE rowid=300;\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", resErrString(_res), "\n  UPDATE ft1 SET c='c3.1' WHERE rowid=300;\n")
		}
	}
	{ // "6.4"
		r = db.Query("\n  SELECT rowid, a, b, c, d FROM ft1\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT rowid, a, b, c, d FROM ft1\n")
			return
		}
		got := flatten(r)
		want := "100 {} b1.1 c1.1 {} 200 {} b2.1 c2 {} 300 {} b3 c3.1 {}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
}
