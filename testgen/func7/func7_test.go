// Code generated by tcl2go; DO NOT EDIT.
package func7

import (
"github.com/pijalu/frigolite"
"testing"
)

func Test_func7(t *testing.T) {
	db, err := frigolite.Open("")
	if err != nil {
		t.Fatal(err)
	}
	defer db.Close()

	var _res *frigolite.Result
	var r *frigolite.Result
	var msg string
	_ = msg // suppress unused warning
	_ = _res // suppress unused warning
	_ = r    // suppress unused warning

	var db1 *frigolite.DB
	_ = db1
	var db2 *frigolite.DB
	_ = db2
	var db3 *frigolite.DB
	_ = db3
	var db4 *frigolite.DB
	_ = db4
	var db5 *frigolite.DB
	_ = db5
	var db6 *frigolite.DB
	_ = db6
	var db7 *frigolite.DB
	_ = db7
	var db8 *frigolite.DB
	_ = db8
	var db9 *frigolite.DB
	_ = db9

	var testdir string
	_ = testdir // pre-declared from TCL source
	var argv0 string
	_ = argv0 // pre-declared from TCL source

	// set testdir: test directory (not used in Go test context)
	return
	{ // "func7-100"
		r = db.Query("\n  SELECT ceil(99.9), ceiling(-99.01), floor(17), floor(-17.99);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT ceil(99.9), ceiling(-99.01), floor(17), floor(-17.99);\n")
			return
		}
		got := flatten(r)
		want := "100.0 -99.0 17 -18.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-110"
		r = db.Query("\n  SELECT quote(ceil(NULL)), ceil('-99.99');\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT quote(ceil(NULL)), ceil('-99.99');\n")
			return
		}
		got := flatten(r)
		want := "NULL -99.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-200"
		r = db.Query("\n  SELECT round(ln(5),2), log(100.0), log(100), log(2,'256');\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT round(ln(5),2), log(100.0), log(100), log(2,'256');\n")
			return
		}
		got := flatten(r)
		want := "1.61 2.0 2.0 8.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-210"
		r = db.Query("\n  SELECT ln(-5), log(-5,100.0);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT ln(-5), log(-5,100.0);\n")
			return
		}
		got := flatten(r)
		want := "{} {}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-100"
		r = db.Query("\n  SELECT abs(-17.4)\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT abs(-17.4)\n")
			return
		}
		got := flatten(r)
		want := "17.4"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-110"
		r = db.Query("\n  SELECT ceil(42.2)\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT ceil(42.2)\n")
			return
		}
		got := flatten(r)
		want := "43.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-120"
		r = db.Query("\n  SELECT ceil(-42.2)\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT ceil(-42.2)\n")
			return
		}
		got := flatten(r)
		want := "-42.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-130"
		r = db.Query("\n  SELECT round(exp(1.0),7)\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT round(exp(1.0),7)\n")
			return
		}
		got := flatten(r)
		want := "2.7182818"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-140"
		r = db.Query("\n  SELECT floor(42.8)\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT floor(42.8)\n")
			return
		}
		got := flatten(r)
		want := "42.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-150"
		r = db.Query("\n  SELECT floor(-42.8)\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT floor(-42.8)\n")
			return
		}
		got := flatten(r)
		want := "-43.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-160"
		r = db.Query("\n  SELECT round(ln(2.0),7)\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT round(ln(2.0),7)\n")
			return
		}
		got := flatten(r)
		want := "0.6931472"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-170"
		r = db.Query("\n  SELECT log(100.0)\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT log(100.0)\n")
			return
		}
		got := flatten(r)
		want := "2.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-180"
		r = db.Query("\n  SELECT log10(1000.0)\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT log10(1000.0)\n")
			return
		}
		got := flatten(r)
		want := "3.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-181"
		r = db.Query("\n  SELECT format('%.30f', log10(100.0) );\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT format('%.30f', log10(100.0) );\n")
			return
		}
		got := flatten(r)
		want := "2.000000000000000000000000000000"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-182"
		r = db.Query("\n  SELECT format('%.30f', ln(exp(2.0)) );\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT format('%.30f', ln(exp(2.0)) );\n")
			return
		}
		got := flatten(r)
		want := "2.000000000000000000000000000000"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-190"
		r = db.Query("\n  SELECT log(2.0, 64.0)\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT log(2.0, 64.0)\n")
			return
		}
		got := flatten(r)
		want := "6.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-200"
		r = db.Query("\n   SELECT mod(9,4);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT mod(9,4);\n")
			return
		}
		got := flatten(r)
		want := "1.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-210"
		r = db.Query("\n   SELECT round(pi(),7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round(pi(),7);\n")
			return
		}
		got := flatten(r)
		want := "3.1415927"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-220"
		r = db.Query("\n   SELECT power(9,3);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT power(9,3);\n")
			return
		}
		got := flatten(r)
		want := "729.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-230"
		r = db.Query("\n   SELECT round(radians(45.0),7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round(radians(45.0),7);\n")
			return
		}
		got := flatten(r)
		want := "0.7853982"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-240"
		r = db.Query("\n   SELECT round(42.4);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round(42.4);\n")
			return
		}
		got := flatten(r)
		want := "42.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-250"
		r = db.Query("\n   SELECT round(42.4382,2);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round(42.4382,2);\n")
			return
		}
		got := flatten(r)
		want := "42.44"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-260"
		r = db.Query("\n   SELECT sign(-8.4);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT sign(-8.4);\n")
			return
		}
		got := flatten(r)
		want := "-1"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-270"
		r = db.Query("\n   SELECT round( sqrt(2), 7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round( sqrt(2), 7);\n")
			return
		}
		got := flatten(r)
		want := "1.4142136"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-280"
		r = db.Query("\n   SELECT trunc(42.8), trunc(-42.8);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT trunc(42.8), trunc(-42.8);\n")
			return
		}
		got := flatten(r)
		want := "42.0 -42.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-300"
		r = db.Query("\n   SELECT acos(1);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT acos(1);\n")
			return
		}
		got := flatten(r)
		want := "0.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-301"
		r = db.Query("\n   SELECT format('%f',degrees(acos(0.5)));\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT format('%f',degrees(acos(0.5)));\n")
			return
		}
		got := flatten(r)
		want := "60.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-310"
		r = db.Query("\n   SELECT round( asin(1), 7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round( asin(1), 7);\n")
			return
		}
		got := flatten(r)
		want := "1.5707963"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-311"
		r = db.Query("\n   SELECT format('%f',degrees( asin(0.5) ));\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT format('%f',degrees( asin(0.5) ));\n")
			return
		}
		got := flatten(r)
		want := "30.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-320"
		r = db.Query("\n   SELECT round( atan(1), 7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round( atan(1), 7);\n")
			return
		}
		got := flatten(r)
		want := "0.7853982"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-321"
		r = db.Query("\n   SELECT degrees( atan(1) );\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT degrees( atan(1) );\n")
			return
		}
		got := flatten(r)
		want := "45.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-330"
		r = db.Query("\n   SELECT round( atan2(1,0), 7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round( atan2(1,0), 7);\n")
			return
		}
		got := flatten(r)
		want := "1.5707963"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-331"
		r = db.Query("\n   SELECT degrees( atan2(1,0) );\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT degrees( atan2(1,0) );\n")
			return
		}
		got := flatten(r)
		want := "90.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-400"
		r = db.Query("\n   SELECT cos(0);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT cos(0);\n")
			return
		}
		got := flatten(r)
		want := "1.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-401"
		r = db.Query("\n   SELECT cos( radians(60.0) );\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT cos( radians(60.0) );\n")
			return
		}
		got := flatten(r)
		want := "0.5"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-400"
		r = db.Query("\n   SELECT cos(0);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT cos(0);\n")
			return
		}
		got := flatten(r)
		want := "1.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-410"
		r = db.Query("\n   SELECT round( sin(1), 7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round( sin(1), 7);\n")
			return
		}
		got := flatten(r)
		want := "0.841471"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-411"
		r = db.Query("\n   SELECT sin( radians(30) );\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT sin( radians(30) );\n")
			return
		}
		got := flatten(r)
		want := "0.5"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-420"
		r = db.Query("\n   SELECT round( tan(1), 7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round( tan(1), 7);\n")
			return
		}
		got := flatten(r)
		want := "1.5574077"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-421"
		r = db.Query("\n   SELECT round(tan( radians(45) ),10);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round(tan( radians(45) ),10);\n")
			return
		}
		got := flatten(r)
		want := "1.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-500"
		r = db.Query("\n   SELECT round( sinh(1), 7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round( sinh(1), 7);\n")
			return
		}
		got := flatten(r)
		want := "1.1752012"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-510"
		r = db.Query("\n   SELECT round( cosh(0), 7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round( cosh(0), 7);\n")
			return
		}
		got := flatten(r)
		want := "1.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-520"
		r = db.Query("\n   SELECT round( tanh(1), 7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round( tanh(1), 7);\n")
			return
		}
		got := flatten(r)
		want := "0.7615942"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-530"
		r = db.Query("\n   SELECT round( asinh(1), 7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round( asinh(1), 7);\n")
			return
		}
		got := flatten(r)
		want := "0.8813736"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-540"
		r = db.Query("\n   SELECT round( acosh(1), 7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round( acosh(1), 7);\n")
			return
		}
		got := flatten(r)
		want := "0.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-pg-550"
		r = db.Query("\n   SELECT round( atanh(0.5), 7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round( atanh(0.5), 7);\n")
			return
		}
		got := flatten(r)
		want := "0.5493061"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-100"
		r = db.Query("\n   SELECT acos(1);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT acos(1);\n")
			return
		}
		got := flatten(r)
		want := "0.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-110"
		r = db.Query("\n   SELECT acos(1.0001);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT acos(1.0001);\n")
			return
		}
		got := flatten(r)
		want := "{}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-120"
		r = db.Query("\n   SELECT round( acos(0.0), 7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round( acos(0.0), 7);\n")
			return
		}
		got := flatten(r)
		want := "1.5707963"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-130"
		r = db.Query("\n   SELECT round( asin(0.2), 7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round( asin(0.2), 7);\n")
			return
		}
		got := flatten(r)
		want := "0.2013579"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-140"
		r = db.Query("\n   SELECT asin('foo');\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT asin('foo');\n")
			return
		}
		got := flatten(r)
		want := "{}"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-150"
		r = db.Query("\n   SELECT round( atan(2), 7), round( atan(-2), 7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round( atan(2), 7), round( atan(-2), 7);\n")
			return
		}
		got := flatten(r)
		want := "1.1071487 -1.1071487"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-160"
		r = db.Query("\n   SELECT round( atan2(-2,2), 7), round( atan2(pi(),0), 7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round( atan2(-2,2), 7), round( atan2(pi(),0), 7);\n")
			return
		}
		got := flatten(r)
		want := "-0.7853982 1.5707963"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-170"
		r = db.Query("\n   SELECT ceiling(1.23), ceiling(-1.23);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT ceiling(1.23), ceiling(-1.23);\n")
			return
		}
		got := flatten(r)
		want := "2.0 -1.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-180"
		r = db.Query("\n   SELECT cos(pi());\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT cos(pi());\n")
			return
		}
		got := flatten(r)
		want := "-1.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-190"
		r = db.Query("\n   SELECT degrees(pi()), degrees(pi()/2);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT degrees(pi()), degrees(pi()/2);\n")
			return
		}
		got := flatten(r)
		want := "180.0 90.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-190"
		r = db.Query("\n   SELECT round( exp(2), 7), round( exp(-2), 7), exp(0);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round( exp(2), 7), round( exp(-2), 7), exp(0);\n")
			return
		}
		got := flatten(r)
		want := "7.3890561 0.1353353 1.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-200"
		r = db.Query("\n   SELECT floor(1.23), floor(-1.23);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT floor(1.23), floor(-1.23);\n")
			return
		}
		got := flatten(r)
		want := "1.0 -2.0"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-210"
		r = db.Query("\n   SELECT round(ln(2),7), quote(ln(-2));\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round(ln(2),7), quote(ln(-2));\n")
			return
		}
		got := flatten(r)
		want := "0.6931472 NULL"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-230"
		r = db.Query("\n   SELECT log(2,65536), log(10,100), quote(log(1,100)), quote(log(0,100));\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT log(2,65536), log(10,100), quote(log(1,100)), quote(log(0,100));\n")
			return
		}
		got := flatten(r)
		want := "16.0 2.0 NULL NULL"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-240"
		r = db.Query("\n   SELECT log2(65536), quote(log2(-100)), quote(log2(0));\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT log2(65536), quote(log2(-100)), quote(log2(0));\n")
			return
		}
		got := flatten(r)
		want := "16.0 NULL NULL"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-250"
		r = db.Query("\n   SELECT round(log10(2),7), log10(100), quote(log10(-100));\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round(log10(2),7), log10(100), quote(log10(-100));\n")
			return
		}
		got := flatten(r)
		want := "0.30103 2.0 NULL"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-260"
		r = db.Query("\n   SELECT mod(234,10), 253%7, mod(29,9), 29%9;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT mod(234,10), 253%7, mod(29,9), 29%9;\n")
			return
		}
		got := flatten(r)
		want := "4.0 1 2.0 2"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-270"
		r = db.Query("\n   SELECT mod(34.5,3);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT mod(34.5,3);\n")
			return
		}
		got := flatten(r)
		want := "1.5"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-280"
		r = db.Query("\n   SELECT pow(2,2), pow(2,-2);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT pow(2,2), pow(2,-2);\n")
			return
		}
		got := flatten(r)
		want := "4.0 0.25"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-281"
		r = db.Query("\n   SELECT power(2,2), power(2,-2);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT power(2,2), power(2,-2);\n")
			return
		}
		got := flatten(r)
		want := "4.0 0.25"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-290"
		r = db.Query("\n   SELECT round(radians(90),7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round(radians(90),7);\n")
			return
		}
		got := flatten(r)
		want := "1.5707963"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-300"
		r = db.Query("\n   SELECT sign(-32), sign(0), sign(234);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT sign(-32), sign(0), sign(234);\n")
			return
		}
		got := flatten(r)
		want := "-1 0 1"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-310"
		r = db.Query("\n   SELECT sin(pi()) BETWEEN -1.0e-15 AND 1.0e-15;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT sin(pi()) BETWEEN -1.0e-15 AND 1.0e-15;\n")
			return
		}
		got := flatten(r)
		want := "1"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-320"
		r = db.Query("\n   SELECT sqrt(4), round(sqrt(20),7), quote(sqrt(-16));\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT sqrt(4), round(sqrt(20),7), quote(sqrt(-16));\n")
			return
		}
		got := flatten(r)
		want := "2.0 4.472136 NULL"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-330"
		r = db.Query("\n   SELECT tan(pi()) BETWEEN -1.0e-15 AND 1.0e-15;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT tan(pi()) BETWEEN -1.0e-15 AND 1.0e-15;\n")
			return
		}
		got := flatten(r)
		want := "1"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "func7-mysql-331"
		r = db.Query("\n   SELECT round(tan(pi()+1),7);\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n   SELECT round(tan(pi()+1),7);\n")
			return
		}
		got := flatten(r)
		want := "1.5574077"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
}
