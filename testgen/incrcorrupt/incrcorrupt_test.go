// Code generated by tcl2go; DO NOT EDIT.
//go:build testgen
// +build testgen

package incrcorrupt

import (
"github.com/pijalu/frigolite"
"github.com/pijalu/frigolite/internal/vtab"
"os"
"testing"
)

func Test_incrcorrupt(t *testing.T) {
	if err := os.Chdir(t.TempDir()); err != nil { t.Fatal(err) }
	db, err := frigolite.Open("test.db")
	if err != nil {
		t.Fatal(err)
	}
	defer db.Close()

	var _res *frigolite.Result
	var r *frigolite.Result
	var msg string
	var _r string
	var _berr error
	_ = _berr // suppress unused warning
	_ = msg // suppress unused warning
	_ = _res // suppress unused warning
	_ = r    // suppress unused warning
	_ = _r   // suppress unused warning
	tcl_nullvalue = "{}" // default NULL rendering

	var db1 *frigolite.DB
	_ = db1
	var db2 *frigolite.DB
	_ = db2
	var db3 *frigolite.DB
	_ = db3
	var db4 *frigolite.DB
	_ = db4
	var db5 *frigolite.DB
	_ = db5
	var db6 *frigolite.DB
	_ = db6
	var db7 *frigolite.DB
	_ = db7
	var db8 *frigolite.DB
	_ = db8
	var db9 *frigolite.DB
	_ = db9

	var testdir string
	_ = testdir // pre-declared from TCL source
	var testprefix string
	_ = testprefix // pre-declared from TCL source
	var stmt string
	_ = stmt // pre-declared from TCL source
	var stmt2 string
	_ = stmt2 // pre-declared from TCL source
	var fd string
	_ = fd // pre-declared from TCL source
	var argv0 string
	_ = argv0 // pre-declared from TCL source

	// set testdir: test directory (not used in Go test context)
	vtab.TclVarSet("testprefix", "", "incrcorrupt")
	testprefix = "incrcorrupt"
	_ = testprefix // suppress unused warning
	{ // "1.0"
		r = db.Query("\n  PRAGMA auto_vacuum = 2;\n  CREATE TABLE t1(a PRIMARY KEY, b);\n\n  WITH data(i) AS (\n    SELECT 1 UNION ALL SELECT i+1 FROM data\n  )\n  INSERT INTO t1 SELECT i, randomblob(600) FROM data LIMIT 20;\n  PRAGMA page_count;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  PRAGMA auto_vacuum = 2;\n  CREATE TABLE t1(a PRIMARY KEY, b);\n\n  WITH data(i) AS (\n    SELECT 1 UNION ALL SELECT i+1 FROM data\n  )\n  INSERT INTO t1 SELECT i, randomblob(600) FROM data LIMIT 20;\n  PRAGMA page_count;\n")
			return
		}
		got := flatten(r)
		want := "24"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "1.1"
		r = db.Query("\n  PRAGMA incremental_vacuum;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  PRAGMA incremental_vacuum;\n")
		}
	}
	{ // do_test "1.2"
		// db_save: snapshot test.db* under sv_ prefix
		for _, _sf := range tclSplitList(tclGlob("test.db*")) {
			tclFileCopy(_sf, "sv_"+_sf)
		}
		tclHexioWrite("test.db", int64(36), "00000019")
		_res = db.Exec(" PRAGMA incremental_vacuum; ")
		_ = _res // catchsql
	}
	{ // "1.3" (prepare-step internals; SQL side effects only)
		// prepared stmt: PRAGMA incremental_vacuum (bind/step emulation)
		tclPrepareStep(db, "PRAGMA incremental_vacuum", "stmt")
		_ = stmt // prepared statement handle
		_res = db.Exec("PRAGMA incremental_vacuum")
		if _res.Error != nil { db.SetLastErr(_res.Error.Error(), db.ErrorCodeFor(_res.Error)) }
		_ = _res // step result (SQLITE_ROW/SQLITE_CONSTRAINT) is C-API state; side effect only
	}
	{ // do_test "1.4"
		_r = db.LastErrCode()
		if _r != "SQLITE_CORRUPT" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "SQLITE_CORRUPT", "1.4")
		}
	}
	{ // do_test "1.5"
		_r = tclErrMsg(db)
		if _r != "database disk image is malformed" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "database disk image is malformed", "1.5")
		}
	}
	{ // "1.6" (prepare-step internals; SQL side effects only)
		tclFinalizePrepared("stmt")
		// sqlite3_finalize $stmt
	}
	{ // do_test "1.7"
		_r = db.LastErrCode()
		if _r != "SQLITE_CORRUPT" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "SQLITE_CORRUPT", "1.7")
		}
	}
	{ // do_test "1.8"
		_r = tclErrMsg(db)
		if _r != "database disk image is malformed" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "database disk image is malformed", "1.8")
		}
	}
	{ // "1.9" (prepare-step internals; SQL side effects only)
		// prepared stmt: PRAGMA incremental_vacuum (bind/step emulation)
		tclPrepareStep(db, "PRAGMA incremental_vacuum", "stmt")
		_ = stmt // prepared statement handle
		_res = db.Exec("PRAGMA incremental_vacuum")
		if _res.Error != nil { db.SetLastErr(_res.Error.Error(), db.ErrorCodeFor(_res.Error)) }
		_ = _res // step result (SQLITE_ROW/SQLITE_CONSTRAINT) is C-API state; side effect only
	}
	{ // do_test "1.10"
		_r = db.LastErrCode()
		if _r != "SQLITE_CORRUPT" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "SQLITE_CORRUPT", "1.10")
		}
	}
	{ // do_test "1.11"
		_r = tclErrMsg(db)
		if _r != "database disk image is malformed" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "database disk image is malformed", "1.11")
		}
	}
	{ // "1.12" (prepare-step internals; SQL side effects only)
		// prepared stmt2: SELECT 1 (bind/step emulation)
		tclPrepareStep(db, "SELECT 1", "stmt2")
		_ = stmt2 // prepared statement handle
		tclFinalizePrepared("stmt2")
		// sqlite3_finalize $stmt2
	}
	{ // do_test "1.13"
		_r = db.LastErrCode()
		if _r != "SQLITE_OK" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "SQLITE_OK", "1.13")
		}
	}
	{ // do_test "1.14"
		_r = tclErrMsg(db)
		if _r != "not an error" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "not an error", "1.14")
		}
	}
	{ // "1.15" (prepare-step internals; SQL side effects only)
		tclFinalizePrepared("stmt")
		// sqlite3_finalize $stmt
	}
	{ // do_test "1.16"
		_r = db.LastErrCode()
		if _r != "SQLITE_CORRUPT" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "SQLITE_CORRUPT", "1.16")
		}
	}
	{ // do_test "1.17"
		_r = tclErrMsg(db)
		if _r != "database disk image is malformed" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "database disk image is malformed", "1.17")
		}
	}
	db.Close()
	os.Remove("test.db")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	{ // "2.1"
		r = db.Query("\n  PRAGMA auto_vacuum = 1;\n  CREATE TABLE t1(a PRIMARY KEY, b);\n  WITH data(i) AS (\n    SELECT 1 UNION ALL SELECT i+1 FROM data\n  )\n  INSERT INTO t1 SELECT i, randomblob(600) FROM data LIMIT 20;\n  PRAGMA page_count;\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  PRAGMA auto_vacuum = 1;\n  CREATE TABLE t1(a PRIMARY KEY, b);\n  WITH data(i) AS (\n    SELECT 1 UNION ALL SELECT i+1 FROM data\n  )\n  INSERT INTO t1 SELECT i, randomblob(600) FROM data LIMIT 20;\n  PRAGMA page_count;\n")
			return
		}
		got := flatten(r)
		want := "24"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // do_test "2.2"
		// db_save: snapshot test.db* under sv_ prefix
		for _, _sf := range tclSplitList(tclGlob("test.db*")) {
			tclFileCopy(_sf, "sv_"+_sf)
		}
		fd = "test.db"
		_ = fd // suppress unused warning
		_ = os.Truncate(fd, int64(22*1024))
		// close $fd
		_res = db.Exec(" PRAGMA incremental_vacuum; ")
		_ = _res // catchsql
	}
	{ // "2.3" (prepare-step internals; SQL side effects only)
		// prepared stmt: PRAGMA auto_vacuum = INCREMENTAL (bind/step emulation)
		tclPrepareStep(db, "PRAGMA auto_vacuum = INCREMENTAL", "stmt")
		_ = stmt // prepared statement handle
		_res = db.Exec("PRAGMA auto_vacuum = INCREMENTAL")
		if _res.Error != nil { db.SetLastErr(_res.Error.Error(), db.ErrorCodeFor(_res.Error)) }
		_ = _res // step result (SQLITE_ROW/SQLITE_CONSTRAINT) is C-API state; side effect only
	}
	{ // do_test "2.4"
		_r = db.LastErrCode()
		if _r != "SQLITE_CORRUPT" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "SQLITE_CORRUPT", "2.4")
		}
	}
	{ // do_test "2.5"
		_r = tclErrMsg(db)
		if _r != "database disk image is malformed" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "database disk image is malformed", "2.5")
		}
	}
	{ // "2.6" (prepare-step internals; SQL side effects only)
		tclFinalizePrepared("stmt")
		// sqlite3_finalize $stmt
	}
	{ // do_test "2.7"
		_r = db.LastErrCode()
		if _r != "SQLITE_CORRUPT" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "SQLITE_CORRUPT", "2.7")
		}
	}
	{ // do_test "2.8"
		_r = tclErrMsg(db)
		if _r != "database disk image is malformed" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "database disk image is malformed", "2.8")
		}
	}
	{ // "2.9" (prepare-step internals; SQL side effects only)
		// prepared stmt: PRAGMA auto_vacuum = INCREMENTAL (bind/step emulation)
		tclPrepareStep(db, "PRAGMA auto_vacuum = INCREMENTAL", "stmt")
		_ = stmt // prepared statement handle
		_res = db.Exec("PRAGMA auto_vacuum = INCREMENTAL")
		if _res.Error != nil { db.SetLastErr(_res.Error.Error(), db.ErrorCodeFor(_res.Error)) }
		_ = _res // step result (SQLITE_ROW/SQLITE_CONSTRAINT) is C-API state; side effect only
	}
	{ // do_test "2.10"
		_r = db.LastErrCode()
		if _r != "SQLITE_CORRUPT" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "SQLITE_CORRUPT", "2.10")
		}
	}
	{ // do_test "2.11"
		_r = tclErrMsg(db)
		if _r != "database disk image is malformed" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "database disk image is malformed", "2.11")
		}
	}
	{ // "2.12" (prepare-step internals; SQL side effects only)
		// prepared stmt2: SELECT 1 (bind/step emulation)
		tclPrepareStep(db, "SELECT 1", "stmt2")
		_ = stmt2 // prepared statement handle
		tclFinalizePrepared("stmt2")
		// sqlite3_finalize $stmt2
	}
	{ // do_test "2.13"
		_r = db.LastErrCode()
		if _r != "SQLITE_OK" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "SQLITE_OK", "2.13")
		}
	}
	{ // do_test "2.14"
		_r = tclErrMsg(db)
		if _r != "not an error" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "not an error", "2.14")
		}
	}
	{ // "2.15" (prepare-step internals; SQL side effects only)
		tclFinalizePrepared("stmt")
		// sqlite3_finalize $stmt
	}
	{ // do_test "2.16"
		_r = db.LastErrCode()
		if _r != "SQLITE_CORRUPT" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "SQLITE_CORRUPT", "2.16")
		}
	}
	{ // do_test "2.17"
		_r = tclErrMsg(db)
		if _r != "database disk image is malformed" {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]\n  body: do_test %s", _r, "database disk image is malformed", "2.17")
		}
	}
}
