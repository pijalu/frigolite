// Code generated by tcl2go; DO NOT EDIT.
//go:build testgen
// +build testgen

package orderby5

import (
"github.com/pijalu/frigolite"
"github.com/pijalu/frigolite/internal/vtab"
"os"
"strings"
"testing"
)

func Test_orderby5(t *testing.T) {
	if err := os.Chdir(t.TempDir()); err != nil { t.Fatal(err) }
	db, err := frigolite.Open("test.db")
	if err != nil {
		t.Fatal(err)
	}
	defer db.Close()

	var _res *frigolite.Result
	var r *frigolite.Result
	var msg string
	var _r string
	var _berr error
	_ = _berr // suppress unused warning
	_ = msg // suppress unused warning
	_ = _res // suppress unused warning
	_ = r    // suppress unused warning
	_ = _r   // suppress unused warning
	tcl_nullvalue = "{}" // default NULL rendering

	var db1 *frigolite.DB
	_ = db1
	var db2 *frigolite.DB
	_ = db2
	var db3 *frigolite.DB
	_ = db3
	var db4 *frigolite.DB
	_ = db4
	var db5 *frigolite.DB
	_ = db5
	var db6 *frigolite.DB
	_ = db6
	var db7 *frigolite.DB
	_ = db7
	var db8 *frigolite.DB
	_ = db8
	var db9 *frigolite.DB
	_ = db9

	var testdir string
	_ = testdir // pre-declared from TCL source
	var testprefix string
	_ = testprefix // pre-declared from TCL source
	var argv0 string
	_ = argv0 // pre-declared from TCL source

	// set testdir: test directory (not used in Go test context)
	vtab.TclVarSet("testprefix", "", "orderby5")
	testprefix = "orderby5" // TCL namespace variable
	_ = testprefix // suppress unused warning
	{ // "orderby5-1.1" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  CREATE TABLE t1(a,b,c);\n  CREATE INDEX t1bc ON t1(b,c);\n\n  EXPLAIN QUERY PLAN\n  SELECT DISTINCT a, b, c FROM t1 WHERE a=0;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-1.2.1" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  EXPLAIN QUERY PLAN\n  SELECT DISTINCT a, c, b FROM t1 WHERE a=0;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-1.2.2" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  EXPLAIN QUERY PLAN\n  SELECT DISTINCT a, c, b FROM t1 WHERE a='xyz' COLLATE nocase;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-1.2.3" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  EXPLAIN QUERY PLAN\n  SELECT DISTINCT a COLLATE nocase, c, b FROM t1 WHERE a='xyz';\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-1.2.4" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  EXPLAIN QUERY PLAN\n  SELECT DISTINCT a COLLATE nocase, c, b FROM t1 WHERE a='xyz' COLLATE nocase;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-1.3" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  EXPLAIN QUERY PLAN\n  SELECT DISTINCT b, a, c FROM t1 WHERE a=0;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-1.4" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  EXPLAIN QUERY PLAN\n  SELECT DISTINCT b, c, a FROM t1 WHERE a=0;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-1.5" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  EXPLAIN QUERY PLAN\n  SELECT DISTINCT c, a, b FROM t1 WHERE a=0;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-1.6" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  EXPLAIN QUERY PLAN\n  SELECT DISTINCT c, b, a FROM t1 WHERE a=0;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-1.7" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  EXPLAIN QUERY PLAN\n  SELECT DISTINCT c, b, a FROM t1 WHERE +a=0;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-2.1a" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  CREATE TABLE t2(a,b,c);\n  CREATE INDEX t2bc ON t2(b,c);\n  ANALYZE;\n  INSERT INTO sqlite_stat1 VALUES('t1','t1bc','1000000 10 9');\n  INSERT INTO sqlite_stat1 VALUES('t2','t2bc','100 10 5');\n  ANALYZE sqlite_master;\n\n  EXPLAIN QUERY PLAN\n  SELECT * FROM t2 WHERE a=0 ORDER BY a, b, c;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-2.1b" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  EXPLAIN QUERY PLAN\n  SELECT * FROM t1 WHERE likelihood(a=0, 0.03) ORDER BY a, b, c;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-2.2" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  EXPLAIN QUERY PLAN\n  SELECT * FROM t1 WHERE +a=0 ORDER BY a, b, c;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-2.3" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  EXPLAIN QUERY PLAN\n  SELECT * FROM t1 WHERE a=0 ORDER BY b, a, c;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-2.4" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  EXPLAIN QUERY PLAN\n  SELECT * FROM t1 WHERE a=0 ORDER BY b, c, a;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-2.5" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  EXPLAIN QUERY PLAN\n  SELECT * FROM t1 WHERE a=0 ORDER BY a, c, b;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-2.6" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  EXPLAIN QUERY PLAN\n  SELECT * FROM t1 WHERE a=0 ORDER BY c, a, b;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-2.7" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  EXPLAIN QUERY PLAN\n  SELECT * FROM t1 WHERE a=0 ORDER BY c, b, a;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-3.0" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  CREATE TABLE t3(a INTEGER PRIMARY KEY, b, c, d, e, f);\n  CREATE INDEX t3bcde ON t3(b, c, d, e);\n  EXPLAIN QUERY PLAN\n  SELECT a FROM t3 WHERE b=2 AND c=3 ORDER BY d DESC, e DESC, b, c, a DESC;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-3.1" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  DROP TABLE t3;\n  CREATE TABLE t3(a INTEGER PRIMARY KEY, b, c, d, e, f) WITHOUT rowid;\n  CREATE INDEX t3bcde ON t3(b, c, d, e);\n  EXPLAIN QUERY PLAN\n  SELECT a FROM t3 WHERE b=2 AND c=3 ORDER BY d DESC, e DESC, b, c, a DESC;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "4.1.0"
		_res = db.Exec("\n  CREATE TABLE t4(b COLLATE nocase);\n  INSERT INTO t4 VALUES('abc');\n  INSERT INTO t4 VALUES('ABC');\n  INSERT INTO t4 VALUES('aBC');\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", _res.Error, "\n  CREATE TABLE t4(b COLLATE nocase);\n  INSERT INTO t4 VALUES('abc');\n  INSERT INTO t4 VALUES('ABC');\n  INSERT INTO t4 VALUES('aBC');\n")
		}
	}
	{ // "4.1.1"
		r = db.Query("\n  SELECT * FROM t4 ORDER BY b COLLATE binary\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT * FROM t4 ORDER BY b COLLATE binary\n")
			return
		}
		got := flatten(r)
		want := "ABC aBC abc"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "4.1.2"
		r = db.Query("\n  SELECT * FROM t4 WHERE b='abc' ORDER BY b COLLATE binary\n")
		if r.Error != nil {
			t.Errorf("query error: %v\n  sql: %s", r.Error, "\n  SELECT * FROM t4 WHERE b='abc' ORDER BY b COLLATE binary\n")
			return
		}
		got := flatten(r)
		want := "ABC aBC abc"
		if got != want {
			t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
		}
	}
	{ // "4.2.1"
		_res = db.Exec("\n  CREATE TABLE Records(typeID INTEGER, key TEXT COLLATE nocase, value TEXT);\n  CREATE INDEX RecordsIndex ON Records(typeID, key, value);\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", _res.Error, "\n  CREATE TABLE Records(typeID INTEGER, key TEXT COLLATE nocase, value TEXT);\n  CREATE INDEX RecordsIndex ON Records(typeID, key, value);\n")
		}
	}
	{ // "orderby5-4.2.2" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  explain query plan\n  SELECT typeID, key, value FROM Records \n  WHERE typeID = 2 AND key = 'x' \n  ORDER BY key, value;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-4.2.3" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  explain query plan\n  SELECT typeID, key, value FROM Records \n  WHERE typeID = 2 AND (key = 'x' COLLATE binary)\n  ORDER BY key, value;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	{ // "orderby5-4.2.4" — skipped: EXPLAIN QUERY PLAN ORDER BY not matched (G3.INDEX) (SQL side effects only)
		_res = db.Exec("\n  explain query plan\n  SELECT typeID, key, value FROM Records \n  WHERE typeID = 2 \n  ORDER BY key, value;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
	// db collate hello (not transpiled)
	{ // "4.3.1"
		_res = db.Exec("\n  CREATE TABLE t5(a INTEGER PRIMARY KEY, b COLLATE hello, c, d);\n")
		if _res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", _res.Error, "\n  CREATE TABLE t5(a INTEGER PRIMARY KEY, b COLLATE hello, c, d);\n")
		}
	}
	db.Close()
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	{ // "4.3.2"
		_res = db.Exec("\n  SELECT a FROM t5 WHERE b='def' ORDER BY b;\n")
		if _res.Error == nil || !strings.Contains(_res.Error.Error(), "no such collation sequence: hello") {
			t.Errorf("expected error containing %q, got: %v\n  sql: %s", "no such collation sequence: hello", _res.Error, "\n  SELECT a FROM t5 WHERE b='def' ORDER BY b;\n")
		}
	}
	{ // "orderby5-4.4.0" — skipped: DISTINCT/LEFT JOIN correlated-subquery test (setup skipped) (SQL side effects only)
		_res = db.Exec("\n  DROP TABLE t1;\n  CREATE TABLE t1(a);\n  DROP TABLE t2;\n  CREATE TABLE t2(b INTEGER PRIMARY KEY, c INT);\n  SELECT DISTINCT *\n    FROM t1 LEFT JOIN t2 ON b=c AND b=(SELECT a FROM t1)\n   WHERE c>10;\n")
		_ = _res.Error // tolerate unsupported-feature errors in skipped tests
	}
}
