// Code generated by tcl2go; DO NOT EDIT.
//go:build testgen
// +build testgen

package shell5

import (
"github.com/pijalu/frigolite"
"github.com/pijalu/frigolite/internal/vtab"
"os"
"testing"
)

func Test_shell5(t *testing.T) {
	if err := os.Chdir(t.TempDir()); err != nil { t.Fatal(err) }
	db, err := frigolite.Open("test.db")
	if err != nil {
		t.Fatal(err)
	}
	defer db.Close()

	var _res *frigolite.Result
	var r *frigolite.Result
	var msg string
	var _r string
	var _berr error
	_ = _berr // suppress unused warning
	_ = msg // suppress unused warning
	_ = _res // suppress unused warning
	_ = r    // suppress unused warning
	_ = _r   // suppress unused warning
	tcl_nullvalue = "{}" // default NULL rendering

	var db1 *frigolite.DB
	_ = db1
	var db2 *frigolite.DB
	_ = db2
	var db3 *frigolite.DB
	_ = db3
	var db4 *frigolite.DB
	_ = db4
	var db5 *frigolite.DB
	_ = db5
	var db6 *frigolite.DB
	_ = db6
	var db7 *frigolite.DB
	_ = db7
	var db8 *frigolite.DB
	_ = db8
	var db9 *frigolite.DB
	_ = db9

	var testdir string
	_ = testdir // pre-declared from TCL source
	var CLI string
	_ = CLI // pre-declared from TCL source
	var res string
	_ = res // pre-declared from TCL source
	var in string
	_ = in // pre-declared from TCL source
	var str string
	_ = str // pre-declared from TCL source
	var cols string
	_ = cols // pre-declared from TCL source
	var data string
	_ = data // pre-declared from TCL source
	var i string
	_ = i // pre-declared from TCL source
	var rows string
	_ = rows // pre-declared from TCL source
	var out string
	_ = out // pre-declared from TCL source
	var fd string
	_ = fd // pre-declared from TCL source
	var x string
	_ = x // pre-declared from TCL source
	var Id_ string
	_ = Id_ // pre-declared from TCL source
	var argv0 string
	_ = argv0 // pre-declared from TCL source

	// set testdir: test directory (not used in Go test context)
	CLI = "test_cli_invocation"
	_ = CLI // suppress unused warning
	db.Close()
	os.Remove("test.db")
	{ // "shell5-1.1.1" (CLI shell subprocess harness, not transpiled)
		// catchcmd test.db .import
	}
	{ // "shell5-1.1.2" (CLI shell subprocess harness, not transpiled)
		// catchcmd test.db .import FOO
	}
	{ // "shell5-1.1.3" (CLI shell subprocess harness, not transpiled)
		// catchcmd test.db .import FOO BAR BAD
	}
	{ // "shell5-1.2.1" (CLI shell subprocess harness, not transpiled)
		// catchcmd test.db .separator
	}
	{ // "shell5-1.2.2" (CLI shell subprocess harness, not transpiled)
		// catchcmd test.db .separator ONE
	}
	{ // "shell5-1.2.3" (CLI shell subprocess harness, not transpiled)
		// catchcmd test.db .separator ONE TWO
	}
	{ // "shell5-1.2.4" (CLI shell subprocess harness, not transpiled)
		// catchcmd test.db .separator ONE TWO THREE
	}
	{ // "shell5-1.3.1.1" (CLI shell subprocess harness, not transpiled)
		// set res [catchcmd "test.db" ".mode list\n.show"]
		// list [regexp {colseparator: \"\|\"} $res]
	}
	{ // "shell5-1.3.1.2" (CLI shell subprocess harness, not transpiled)
		// set res [catchcmd "test.db" ".mode list\n.show"]
		// list [regexp {rowseparator: \"\\n\"} $res]
	}
	{ // "shell5-1.3.2" (CLI shell subprocess harness, not transpiled)
		// set res [catchcmd "test.db" {.separator ,\n.show}]
		// list [regexp {separator: \",\"} $res]
	}
	{ // "shell5-1.4.1" (CLI shell subprocess harness, not transpiled)
		// forcedelete FOO
		// set res [catchcmd "test.db" {CREATE TABLE t1(a, b);\n.import FOO t1}]
	}
	{ // "shell5-1.4.2" (CLI shell subprocess harness, not transpiled)
		// forcedelete shell5.csv
		// set in [open shell5.csv w]
		// close $in
		// set res [catchcmd ":memory:" {\n.mode list\nATTACH 'test.db' AS test;\n.import -...
	}
	{ // "shell5-1.4.3" (CLI shell subprocess harness, not transpiled)
		// set in [open shell5.csv w]
		// puts $in 1
		// close $in
		// set res [catchcmd ":memory:" {ATTACH 'test.db' AS test;\n.import -schema test sh...
	}
	{ // "shell5-1.4.4" (CLI shell subprocess harness, not transpiled)
		// set in [open shell5.csv w]
		// puts $in 1|2|3
		// close $in
		// set res [catchcmd ":memory: --list" {ATTACH 'test.db' AS test;\n.import --schema...
	}
	{ // "shell5-1.4.5" (CLI shell subprocess harness, not transpiled)
		// set in [open shell5.csv w]
		// puts $in 1|2
		// close $in
		// set res [catchcmd "test.db -list" {DELETE FROM t1;\n.import shell5.csv t1\nSELEC...
	}
	{ // "shell5-1.4.6" (CLI shell subprocess harness, not transpiled)
		// set in [open shell5.csv w]
		// puts $in 2|3
		// puts $in 3|4
		// close $in
		// set res [catchcmd ":memory:" {\n.mode list\nATTACH 'test.db' AS test;\n.import -...
	}
	{ // "shell5-1.4.7" (CLI shell subprocess harness, not transpiled)
		// set in [open shell5.csv w]
		// puts $in 4,5
		// close $in
		// set res [catchcmd ":memory:" {\n.mode list\nATTACH 'test.db' AS test;\n.separato...
	}
	{ // "shell5-1.4.8.1" (CLI shell subprocess harness, not transpiled)
		// set in [open shell5.csv w]
		// puts $in 5|Now is the time for all good men to come to the aid of their country.
		// close $in
		// set res [catchcmd "test.db" {.mode list\n.import shell5.csv t1\nSELECT COUNT(*) ...
	}
	{ // "shell5-1.4.8.2" (CLI shell subprocess harness, not transpiled)
		// catchcmd test.db -list SELECT b FROM t1 WHERE a='5';
	}
	{ // "shell5-1.4.9.1" (CLI shell subprocess harness, not transpiled)
		// set in [open shell5.csv w]
		// puts $in 6|'Now is the time for all good men to come to the aid of their country...
		// close $in
		// set res [catchcmd "test.db -list" {.import shell5.csv t1\nSELECT COUNT(*) FROM t...
	}
	{ // "shell5-1.4.9.2" (CLI shell subprocess harness, not transpiled)
		// catchcmd test.db -list SELECT b FROM t1 WHERE a='6';
	}
	{ // "shell5-1.4.10.1" (CLI shell subprocess harness, not transpiled)
		// set in [open shell5.csv w]
		// puts $in 7|\"Now is the time for all good men to come to the aid of their countr...
		// close $in
		// set res [catchcmd "test.db -list" {.import shell5.csv t1\nSELECT COUNT(*) FROM t...
	}
	{ // "shell5-1.4.10.2" (CLI shell subprocess harness, not transpiled)
		// catchcmd test.db -list SELECT b FROM t1 WHERE a='7';
	}
	{ // "shell5-1.4.11" (CLI shell subprocess harness, not transpiled)
		// set in [open shell5.csv wb]
		// puts -nonewline $in \xef\xbb\xbf
		// puts $in 2|3
		// puts $in 4|5
		// close $in
		// set res [catchcmd "test.db -list" {CREATE TABLE t2(x INT, y INT);\n.import shell...
		// string map \n | \n\r | $res
	}
	{ // "shell5-1.4.12" (CLI shell subprocess harness, not transpiled)
		// set in [open shell5.csv wb]
		// puts $in \xef\xbb\xbf\"two\"|3
		// puts $in 4|5
		// close $in
		// set res [catchcmd "test.db -list" {DELETE FROM t2;\n.import shell5.csv t2\n.mode...
		// string map \n | \n\r | $res
	}
	{ // "shell5-1.5.1" (CLI shell subprocess harness, not transpiled)
		// set str [string repeat X 999]
		// set in [open shell5.csv w]
		// puts $in 8|$str
		// close $in
		// set res [catchcmd "test.db -list" {.import shell5.csv t1\nSELECT length(b) FROM ...
	}
	vtab.TclVarSet("cols", "", "999")
	cols = "999"
	_ = cols // suppress unused warning
	{ // "shell5-1.6.1" (CLI shell subprocess harness, not transpiled)
		// set data 
		// for set i 1 $i<$cols incr i \n    append data "c$i|"\n  
		// append data c$cols\n
		// for set i 1 $i<$cols incr i \n    append data "$i|"\n  
		// append data $cols
		// set in [open shell5.csv w]
		// puts $in $data
		// close $in
		// set res [catchcmd "test.db -list" {DROP TABLE IF EXISTS t2;\n.import shell5.csv ...
	}
	vtab.TclVarSet("rows", "", "9999")
	rows = "9999"
	_ = rows // suppress unused warning
	{ // "shell5-1.7.1" (CLI shell subprocess harness, not transpiled)
		// set in [open shell5.csv w]
		// puts $in a
		// for set i 1 $i<=$rows incr i \n    puts $in $i\n  
		// close $in
		// set res [catchcmd "test.db" {.mode csv\n.import shell5.csv t3\n.mode quote\nSELE...
	}
	if tcl_platform_platform == "unix" {
		{ // "shell5-1.8" (CLI shell subprocess harness, not transpiled)
			// forcedelete test.db
			// catchcmd test.db .mode csv\n.import "|awk 'END{print \"x,y\";for(i=1;i<=5;i++){p...
		}
	}
	{ // "shell5-1.9" (CLI shell subprocess harness, not transpiled)
		// set out [open shell5.csv w]
		// fconfigure $out -translation lf
		// puts $out 1,"",11
		// puts $out 2,"x",22
		// puts $out 3,"""",33
		// puts $out 4,"hello",44
		// puts $out 5,55,\"\"\r
		// puts $out 6,66,"x"
		// puts $out 7,77,""""
		// puts $out 8,88,"hello"
		// puts $out "",9,99
		// puts $out "x",10,110
		// puts $out """",11,121
		// puts $out "hello",12,132
		// close $out
		// catch db close
		// forcedelete test.db
		// catchcmd test.db .mode csv\n    CREATE TABLE t1(a,b,c);\n.import shell5.csv t1\n...
		// sqlite3 db test.db
		// db eval SELECT *, '|' FROM t1 ORDER BY rowid
	}
	db.Close()
	{ // "shell5-1.10" (CLI shell subprocess harness, not transpiled)
		// set out [open shell5.csv w]
		// fconfigure $out -translation lf
		// puts $out column1,column2,column3,column4
		// puts $out field1,field2,\"x3 \"\"\r\ndata\"\" 3\",field4
		// puts $out x1,x2,\"x3 \"\"\ndata\"\" 3\",x4
		// close $out
		// forcedelete test.db
		// catchcmd test.db .mode csv\n    CREATE TABLE t1(a,b,c,d);\n.import shell5.csv t1...
		// sqlite3 db test.db
		// db eval SELECT hex(c) FROM t1 ORDER BY rowid
	}
	{ // "shell5-1.10.1" (CLI shell subprocess harness, not transpiled)
		// set out [open shell5.csv w]
		// fconfigure $out -translation lf
		// puts $out column1,column2,column3,column4
		// puts $out x1,x2%\"x3,\"x3\\\"data\\\"3\",x4
		// close $out
		// db close
		// forcedelete test.db
		// catchcmd test.db \n    CREATE TABLE t1(a,b,c,d);\n.import --csv --qesc \\ --esc ...
		// sqlite3 db test.db
		// db eval SELECT b, c FROM t1 ORDER BY rowid
	}
	{ // "shell5-1.11" (CLI shell subprocess harness, not transpiled)
		// set out [open shell5.csv w]
		// fconfigure $out -translation binary
		// puts $out column1,column2,column3\r
		// puts $out a,b, \r
		// puts $out x,y,\r
		// puts $out p,q,r\r
		// close $out
		// catch db close
		// forcedelete test.db
		// catchcmd test.db .mode csv\n.import shell5.csv t1\n  
		// sqlite3 db test.db
		// db eval SELECT *, '|' FROM t1
	}
	db.Close()
	db.Close()
	os.Remove("test.db")
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	tcl_nullvalue = "{}" // fresh connection resets nullvalue
	db, err = frigolite.Open("test.db")
	if err != nil { t.Fatal(err) }
	{ // "shell5-2.1" (CLI shell subprocess harness, not transpiled)
		// set fd [open shell5.csv w]
		// puts $fd ,hello
		// close $fd
		// catchcmd test.db [string trim {\n.mode csv\nCREATE TABLE t1(a, b);\n.import shel...
		// db eval  SELECT * FROM t1 
	}
	{ // "shell5-2.2" (CLI shell subprocess harness, not transpiled)
		// set fd [open shell5.csv w]
		// puts $fd "",hello
		// close $fd
		// catchcmd test.db [string trim {\n.mode csv\nCREATE TABLE t2(a, b);\n.import shel...
		// db eval  SELECT * FROM t2 
	}
	{ // "shell5-2.3" (CLI shell subprocess harness, not transpiled)
		// set fd [open shell5.csv w]
		// puts $fd "x""y",hello
		// close $fd
		// catchcmd test.db [string trim {\n.mode csv\nCREATE TABLE t3(a, b);\n.import shel...
		// db eval  SELECT * FROM t3 
	}
	{ // "shell5-2.4" (CLI shell subprocess harness, not transpiled)
		// set fd [open shell5.csv w]
		// puts $fd "xy""",hello
		// close $fd
		// catchcmd test.db [string trim {\n.mode csv\nCREATE TABLE t4(a, b);\n.import shel...
		// db eval  SELECT * FROM t4 
	}
	{ // "shell5-2.5" (CLI shell subprocess harness, not transpiled)
		// set fd [open shell5.csv w]
		// puts $fd "one","2"
		// puts $fd 
		// close $fd
		// catchcmd test.db [string trim {\n.mode csv\nCREATE TABLE t4(a, b);\n.import shel...
		// db eval  SELECT * FROM t4 
	}
	{ // "shell5-3.1" (CLI shell subprocess harness, not transpiled)
		// set fd [open shell5.csv w]
		// fconfigure $fd -translation binary
		// puts -nonewline $fd \"test 1\"\x1F,test 2\r\n\x1E
		// puts -nonewline $fd test 3\x1Ftest 4\n
		// close $fd
		// catchcmd test.db \n.mode ascii\nCREATE TABLE t5(a, b);\n.import shell5.csv t5\n ...
		// db eval  SELECT * FROM t5 
	}
	{ // "shell5-3.2" (CLI shell subprocess harness, not transpiled)
		// set x [catchcmd test.db {\n.mode ascii\nSELECT * FROM t5;\n  }]
		// regsub -all [\n\r]?\n $x <EOL> x
		// set x
	}
	{ // "shell5-4.1" (CLI shell subprocess harness, not transpiled)
		// forcedelete shell5.csv
		// set fd [open shell5.csv w]
		// puts $fd 1,2,3
		// puts $fd 4,5
		// puts $fd 6,7,8
		// close $fd
		// catchcmd test.db [string trim {\n.mode csv\nCREATE TABLE t6(a, b, c);\n.import s...
		// db eval  SELECT * FROM t6 ORDER BY a 
	}
	{ // "shell5-4.2" (CLI shell subprocess harness, not transpiled)
		// forcedelete shell5.csv
		// set fd [open shell5.csv w]
		// puts $fd 1,2,3
		// puts $fd 4,5
		// puts $fd 6,7,8,9
		// close $fd
		// catchcmd test.db [string trim {\n.mode csv\nCREATE TABLE t7(a, b, c);\n.import s...
		// db eval  SELECT * FROM t7 ORDER BY a 
	}
	{ // "shell5-4.3" (CLI shell subprocess harness, not transpiled)
		// forcedelete shell5.csv
		// set fd [open shell5.csv w]
		// puts $fd ,,
		// puts $fd 1,2,3
		// close $fd
		// catchcmd test.db [string trim {\n.mode csv\nCREATE TABLE t8(a, b, c);\n.import -...
		// db eval  SELECT * FROM t8 
	}
	{ // "shell5-4.4" (CLI shell subprocess harness, not transpiled)
		// forcedelete shell5.csv
		// set fd [open shell5.csv w]
		// puts $fd 1,2,3
		// close $fd
		// catchcmd test.db [string trim {\n.mode csv\nCREATE TEMP TABLE t8(a, b, c);\n.imp...
	}
	db.Close()
	{ // "shell5-5.1" (CLI shell subprocess harness, not transpiled)
		// set out [open shell5.csv w]
		// fconfigure $out -translation lf
		// puts $out "","x","x","y","z","z_0","z_5","z"
		// puts $out 0,"x2","x3","y4","z5","z6","z7","z8"
		// close $out
		// forcedelete test.db
		// catchcmd test.db .import -csv shell5.csv t1\n.mode line --colsep ' = '\nSELECT *...
	}
	{ // "shell5-5.1b" (CLI shell subprocess harness, not transpiled)
		// set out [open shell5.csv w]
		// fconfigure $out -translation lf
		// puts $out "COW","cow","CoW","cOw"
		// puts $out "uuu","lll","ulu","lul"
		// close $out
		// forcedelete test.db
		// catchcmd test.db .import -csv shell5.csv t1\n.mode line\nSELECT * FROM t1;
	}
	// do_test_with_ansi_output shell5-6.1 {\n  set out [open shell5.csv w]\n  fconfigure $out...} {0 {   あい... (unsupported command, not transpiled)
	// do_test_with_ansi_output shell5-6.2 {\n  set out [open shell5.csv w]\n  fconfigure $out...} {0 {1: あい... (unsupported command, not transpiled)
	{ // "shell5-7.1" (CLI shell subprocess harness, not transpiled)
		// set out [open shell5.csv w]
		// fconfigure $out -translation lf
		// puts $out aaa|bbb
		// close $out
		// forcedelete test.db
		// catchcmd :memory: -list CREATE TABLE t1(a TEXT, b TEXT, c AS (a||b));\n.import s...
	}
	{ // "shell5-8.1" (CLI shell subprocess harness, not transpiled)
		// set out [open shell5.csv w]
		// fconfigure $out -translation lf
		// puts $out x
		// close $out
		// catchcmd :memory: .import --csv shell5.csv '""""""""""""""""""""""""""""""""""""...
	}
	{ // "shell5-9.1" (CLI shell subprocess harness, not transpiled)
		// catchcmd :memory: \n    CREATE TABLE t1(a,b,c INT CHECK(c<>5));\n.bail on\n.impo...
	}
	{ // "shell5-9.2" (CLI shell subprocess harness, not transpiled)
		// catchcmd :memory: \n    CREATE TABLE t1(a,b,c INT CHECK(c<>5));\n.bail off\n.imp...
	}
}
