// Code generated by tcl2go; DO NOT EDIT.
package tkt-bdc6bbbb

import (
"fmt"
"os"
"path/filepath"
"regexp"
"sort"
"strconv"
"strings"
"testing"

"github.com/pijalu/frigolite"
)

func flatten(res *frigolite.Result) string {
	var parts []string
	for _, row := range res.Rows {
		for _, val := range row {
			if val == nil {
				parts = append(parts, "NULL")
			} else {
				switch x := val.(type) {
				case int64:
					parts = append(parts, strconv.FormatInt(x, 10))
				case float64:
					parts = append(parts, strconv.FormatFloat(x, 'g', -1, 64))
				case string:
					parts = append(parts, x)
				case []byte:
					parts = append(parts, string(x))
				default:
					parts = append(parts, fmt.Sprintf("%v", x))
				}
			}
		}
	}
	return strings.Join(parts, " ")
}



// --- TCL runtime helpers ---

// tclListAppend appends items to a TCL-format list string.
func tclListAppend(list string, items ...string) string {
	if list == "" {
		return tclList(items)
	}
	existing := tclSplitList(list)
	existing = append(existing, items...)
	return tclList(existing)
}

// tclList joins items into a TCL-format list string.
func tclList(items []string) string {
	parts := make([]string, len(items))
	for i, item := range items {
		if tclNeedsBracing(item) {
			parts[i] = "{" + item + "}"
		} else {
			parts[i] = item
		}
	}
	return strings.Join(parts, " ")
}

// tclSplitList splits a TCL-format list string into elements.
func tclSplitList(s string) []string {
	var result []string
	pos := 0
	for pos < len(s) {
		for pos < len(s) && (s[pos] == ' ' || s[pos] == '\t' || s[pos] == '\n' || s[pos] == '\r') {
			pos++
		}
		if pos >= len(s) { break }
		switch s[pos] {
		case '{':
			depth := 1; start := pos + 1; pos++
			for pos < len(s) && depth > 0 {
				if s[pos] == '{' { depth++ }
				if s[pos] == '}' { depth-- }
				if depth > 0 { pos++ }
			}
			result = append(result, s[start:pos])
			if pos < len(s) { pos++ }
		case '"':
			start := pos + 1; pos++
			for pos < len(s) && s[pos] != '"' { pos++ }
			result = append(result, s[start:pos])
			if pos < len(s) { pos++ }
		default:
			start := pos
			for pos < len(s) && s[pos] != ' ' && s[pos] != '\t' && s[pos] != '\n' && s[pos] != '\r' { pos++ }
			result = append(result, s[start:pos])
		}
	}
	return result
}

func tclNeedsBracing(s string) bool {
	if s == "" { return true }
	for _, c := range s {
		switch c { case ' ', '\t', '\n', '\r', '{', '}', '"', ';': return true }
	}
	return false
}

func tclLIndex(list string, idx int) string {
	items := tclSplitList(list)
	if idx < 0 || idx >= len(items) { return "" }
	return items[idx]
}

func tclLLength(list string) int { return len(tclSplitList(list)) }

func tclLRange(list string, start, end int) string {
	items := tclSplitList(list)
	if start < 0 { start = 0 }
	if end < 0 || end >= len(items) { end = len(items) - 1 }
	if start > end || start >= len(items) { return "" }
	return tclList(items[start : end+1])
}

func tclLReplace(list string, first, count int, args ...string) string {
	items := tclSplitList(list)
	if first < 0 { first = 0 }
	if first > len(items) { first = len(items) }
	end := first + count
	if end > len(items) { end = len(items) }
	repl := args
	items = append(items[:first], append(repl, items[end:]...)...)
	return tclList(items)
}

func tclSort(list string) string {
	items := tclSplitList(list)
	sort.Strings(items)
	return tclList(items)
}

func tclRegexp(pattern, str string) string {
	matched, _ := regexp.MatchString(pattern, str)
	if matched { return "1" }
	return "0"
}

func tclRegsub(pattern, str, replacement string) string {
	re, err := regexp.Compile(pattern)
	if err != nil { return str }
	return re.ReplaceAllString(str, replacement)
}

func tclStringMatch(pattern, str string) bool {
	// Convert TCL glob pattern to Go regexp
	goPattern := ""
	for i := 0; i < len(pattern); i++ {
		c := pattern[i]
		switch c {
		case '*': goPattern += ".*"
		case '?': goPattern += "."
		case '.', '+', '(', ')', '|', '^', '$': goPattern += "\\" + string(c)
		default: goPattern += string(c)
		}
	}
	matched, _ := regexp.MatchString("^"+goPattern+"$", str)
	return matched
}

func tclFileCopy(src, dst string) {
	data, err := os.ReadFile(src)
	if err != nil { return }
	os.WriteFile(dst, data, 0644)
}

func tclGlob(pattern string) string {
	matches, _ := filepath.Glob(pattern)
	return tclList(matches)
}


func Test_tkt_bdc6bbbb38(t *testing.T) {
	db, err := frigolite.Open("")
	if err != nil {
		t.Fatal(err)
	}
	defer db.Close()

	var res *frigolite.Result
	var r *frigolite.Result

	// set testdir: test directory (not used in Go test context)
	testprefix := "tkt-bdc6bbbb38"
	sqlite_fts3_enable_parentheses := "1"
	// foreach {tn idxdir} "1 ASC 2 DESC"
	_items := []string{"1 ASC 2 DESC"}
	for _idx := 0; _idx+2 <= len(_items); _idx += 2 {
	tn := _items[_idx+0]
	idxdir := _items[_idx+1]
		res = db.Exec(" DROP TABLE IF EXISTS t2 ")
		if res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", res.Error, " DROP TABLE IF EXISTS t2 ")
		}
		{ // tn + ".1.1"
			res = db.Exec("CREATE VIRTUAL TABLE t2 USING fts4(x, order=" + idxdir + ")")
			if res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", res.Error, "CREATE VIRTUAL TABLE t2 USING fts4(x, order=" + idxdir + ")")
			}
		}
		{ // tn + ".1.2"
			res = db.Exec(" INSERT INTO t2 VALUES('a b c') ")
			if res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", res.Error, " INSERT INTO t2 VALUES('a b c') ")
			}
		}
		{ // tn + ".1.3"
			r := db.Query("\n    SELECT offsets(t2) FROM t2 WHERE t2 MATCH 'a AND d OR b' ORDER BY docid ASC\n  ")
			if r.Error != nil {
				t.Errorf("query error: %v\n  sql: %s", r.Error, "\n    SELECT offsets(t2) FROM t2 WHERE t2 MATCH 'a AND d OR b' ORDER BY docid ASC\n  ")
				return
			}
			got := flatten(r)
			want := "\n    {0 0 0 1 0 2 2 1}\n  "
			if got != want {
				t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
			}
		}
		{ // tn + ".1.4"
			r := db.Query("\n    SELECT snippet(t2,'[',']') FROM t2 WHERE t2 MATCH 'a AND d OR b' \n    ORDER BY docid ASC\n  ")
			if r.Error != nil {
				t.Errorf("query error: %v\n  sql: %s", r.Error, "\n    SELECT snippet(t2,'[',']') FROM t2 WHERE t2 MATCH 'a AND d OR b' \n    ORDER BY docid ASC\n  ")
				return
			}
			got := flatten(r)
			want := "\n    {[a] [b] c}\n  "
			if got != want {
				t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
			}
		}
		{ // tn + ".1.5"
			res = db.Exec(" INSERT INTO t2 VALUES('a c d') ")
			if res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", res.Error, " INSERT INTO t2 VALUES('a c d') ")
			}
		}
		{ // tn + ".1.6"
			r := db.Query("\n    SELECT offsets(t2) FROM t2 WHERE t2 MATCH 'a AND d OR b' ORDER BY docid ASC\n  ")
			if r.Error != nil {
				t.Errorf("query error: %v\n  sql: %s", r.Error, "\n    SELECT offsets(t2) FROM t2 WHERE t2 MATCH 'a AND d OR b' ORDER BY docid ASC\n  ")
				return
			}
			got := flatten(r)
			want := "\n    {0 0 0 1 0 2 2 1}\n    {0 0 0 1 0 1 4 1}\n  "
			if got != want {
				t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
			}
		}
		{ // tn + ".1.7"
			r := db.Query("\n    SELECT snippet(t2,'[',']') FROM t2 WHERE t2 MATCH 'a AND d OR b'\n    ORDER BY docid ASC\n  ")
			if r.Error != nil {
				t.Errorf("query error: %v\n  sql: %s", r.Error, "\n    SELECT snippet(t2,'[',']') FROM t2 WHERE t2 MATCH 'a AND d OR b'\n    ORDER BY docid ASC\n  ")
				return
			}
			got := flatten(r)
			want := "\n    {[a] [b] c}\n    {[a] c [d]}\n  "
			if got != want {
				t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
			}
		}
		res = db.Exec(" DROP TABLE IF EXISTS t3 ")
		if res.Error != nil {
			t.Errorf("exec error: %v\n  sql: %s", res.Error, " DROP TABLE IF EXISTS t3 ")
		}
		{ // tn + ".2.1"
			res = db.Exec("CREATE VIRTUAL TABLE t3 USING fts4(x, order=" + idxdir + ")")
			if res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", res.Error, "CREATE VIRTUAL TABLE t3 USING fts4(x, order=" + idxdir + ")")
			}
		}
		{ // tn + ".2.2"
			res = db.Exec(" INSERT INTO t3 VALUES('a c d') ")
			if res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", res.Error, " INSERT INTO t3 VALUES('a c d') ")
			}
		}
		{ // tn + ".2.3"
			r := db.Query("\n    SELECT offsets(t3) FROM t3 WHERE t3 MATCH 'a AND d OR b' ORDER BY docid DESC\n  ")
			if r.Error != nil {
				t.Errorf("query error: %v\n  sql: %s", r.Error, "\n    SELECT offsets(t3) FROM t3 WHERE t3 MATCH 'a AND d OR b' ORDER BY docid DESC\n  ")
				return
			}
			got := flatten(r)
			want := "\n    {0 0 0 1 0 1 4 1}\n  "
			if got != want {
				t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
			}
		}
		{ // tn + ".2.4"
			r := db.Query("\n    SELECT snippet(t3,'[',']') FROM t3 WHERE t3 MATCH 'a AND d OR b'\n      ORDER BY docid DESC\n  ")
			if r.Error != nil {
				t.Errorf("query error: %v\n  sql: %s", r.Error, "\n    SELECT snippet(t3,'[',']') FROM t3 WHERE t3 MATCH 'a AND d OR b'\n      ORDER BY docid DESC\n  ")
				return
			}
			got := flatten(r)
			want := "\n    {[a] c [d]}\n  "
			if got != want {
				t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
			}
		}
		{ // tn + ".2.5"
			res = db.Exec(" \n    INSERT INTO t3 VALUES('a b c');\n  ")
			if res.Error != nil {
				t.Errorf("exec error: %v\n  sql: %s", res.Error, " \n    INSERT INTO t3 VALUES('a b c');\n  ")
			}
		}
		{ // tn + ".2.6"
			r := db.Query("\n    SELECT offsets(t3) FROM t3 WHERE t3 MATCH 'a AND d OR b' ORDER BY docid DESC\n  ")
			if r.Error != nil {
				t.Errorf("query error: %v\n  sql: %s", r.Error, "\n    SELECT offsets(t3) FROM t3 WHERE t3 MATCH 'a AND d OR b' ORDER BY docid DESC\n  ")
				return
			}
			got := flatten(r)
			want := "\n    {0 0 0 1 0 2 2 1}\n    {0 0 0 1 0 1 4 1}\n  "
			if got != want {
				t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
			}
		}
		{ // tn + ".2.7"
			r := db.Query("\n    SELECT snippet(t3,'[',']') FROM t3 WHERE t3 MATCH 'a AND d OR b'\n      ORDER BY docid DESC\n  ")
			if r.Error != nil {
				t.Errorf("query error: %v\n  sql: %s", r.Error, "\n    SELECT snippet(t3,'[',']') FROM t3 WHERE t3 MATCH 'a AND d OR b'\n      ORDER BY docid DESC\n  ")
				return
			}
			got := flatten(r)
			want := "\n    {[a] [b] c}\n    {[a] c [d]}\n  "
			if got != want {
				t.Errorf("result mismatch\n  got:  [%s]\n  want: [%s]", got, want)
			}
		}
	}
	}
	sqlite_fts3_enable_parentheses := "0"
}
